# عدة مكوّنات الأندرويد (M.*) — مواصفة البناء في Pen

تُبنى في الصف y = 21000 من الملف نفسه، كل مكوّن إطار جذري `reusable:true` باسم يبدأ بـ`M.`، مرتبة أفقيًا من x = 0 بفواصل 40px، مع إطار عنوان صف `§ مكوّنات الأندرويد` عند (0, 20960). الرموز نفسها (`$…`). الخط `$font`. مقاسات Material 3 على عرض 412.

| المكوّن | المواصفة |
|---|---|
| M.StatusBar | 412×44، تعبئة `$surface`، الوقت "10:24" يسارًا، أيقونات البطارية والإشارة يمينًا (lucide: battery, signal, wifi-off صغيرة 14) بلون `$text`. |
| M.TopBar | 412×64، تعبئة `$surface`، حدّ سفلي `$border`؛ الأطفال من اليسار: زر أيقونة (search) 40×40، زر أيقونة (more-vertical)، فراغ يمتد، عنوان الشاشة 18/700 `$text` يمينًا، زر رجوع/قائمة (menu) 40×40 في أقصى اليمين. تحته سطر المؤسسة/المكتب 11 `$muted` اختياري داخل نفس المكوّن (Frame OrgLine قابل للتعطيل). |
| M.OrgHeader | 412×56، تعبئة `$sidebar`، شعار المؤسسة 32×32 (tile أبيض بأيقونة landmark) يمينًا، اسم المؤسسة 13/700 أبيض واسم المكتب 11 `$sidebar-muted`؛ يُستخدم في الرئيسية والإعدادات. |
| M.BottomNav | 412×80، تعبئة `$surface`، حدّ علوي؛ 5 عناصر بعرض متساوٍ (fill_container): أيقونة 24 + نص 11؛ العنصر النشط: كبسولة `$primary-soft` خلف الأيقونة ونص `$primary` 600؛ الشارة الحمراء (Badge) أعلى يمين الأيقونة، معطّلة افتراضيًا باسم `Badge<N>`. الترتيب من اليسار: البحث، الاجتماعات، المهام، المراسلات، الرئيسية (لتظهر الرئيسية في أقصى اليمين). |
| M.FAB | 56×56 دائرة `$primary-fill` بأيقونة plus بيضاء وظل خفيف؛ M.FAB.Extended بنص "إدخال سريع". |
| M.ListItem | 412×72، تعبئة `$surface`، فاصل سفلي `$border`؛ من اليسار: قيمة ثانوية/وقت 12 `$muted` + chevron-left 16، فراغ، نصوص (العنوان 15/600 `$text`، السطر الثاني 13 `$muted`) بمحاذاة يمين، أيقونة أو دائرة 40 بلون `$primary-soft` في أقصى اليمين. متغير M.ListItem.Attention بحافة يمنى 3px `$danger` وتعبئة `$attention-bg`. |
| M.Card | عرض 380، تعبئة `$surface`، زوايا 12، حدّ `$border`، حشو 16، عمودي بفجوة 10، عنوان 15/700 + جسم فتحة. متغيرات M.Card.Info/Warning/Danger/Success بألوان soft/border الدلالية وأيقونة. |
| M.Chip | كبسولة 32 ارتفاعًا، حشو [6,12]، نص 13؛ M.Chip.Selected بتعبئة `$primary-soft` ونص `$primary` وأيقونة check. |
| M.Badge | دائرة 18 `$danger` بنص أبيض 11/700. |
| M.Button.Filled | ارتفاع 44، زوايا 22، تعبئة `$primary-fill`، نص أبيض 15/600، أيقونة اختيارية يمين النص. M.Button.Outlined (حدّ `$primary`، نص `$primary`)، M.Button.Text، M.Button.Danger (`$danger-fill`). |
| M.TextField | عرض 380، عمودي: تسمية 12 `$muted`، صندوق 52 بزوايا 10 وحدّ `$border-strong` ونص القيمة 15، سطر مساعدة 12؛ M.TextField.Error بحدّ `$danger` ورسالة حمراء. M.TextField.Amount للمبلغ: رقم 32/700 مع "₪". |
| M.SearchBar | 380×48 زوايا 24 تعبئة `$surface2` أيقونة search يمينًا ونص "ابحث في هذه القائمة…" `$placeholder`. |
| M.Sheet | ورقة سفلية 412×(fit) بزوايا علوية 20، مقبض 32×4 `$border-strong` في الأعلى، عنوان، شبكة خيارات (أيقونة دائرية 48 + نص 12) صفوف من 3. |
| M.Dialog | 340 عرض، زوايا 20، حشو 24، عنوان 18/700، نص 14 `$muted`، أزرار نصية يسارًا (إلغاء، تأكيد بلون `$primary`؛ الخطِر `$danger`). |
| M.Snackbar | 380×48 تعبئة `$text` (داكن) نص أبيض 13 وإجراء `$primary-soft`. |
| M.Avatar | دائرة 40 `$primary-soft` بحرف `$primary` 700؛ M.Avatar.Photo بحجم 72 لواجهة الصورة. |
| M.Tabs | صف تبويبات بعرض 412: نص 14 مع خط سفلي 2 `$primary` للنشط وشارة عددية M.Badge بجانب النص. |
| M.SegmentedRow | صف مؤشرات صغيرة (KPI) 3 بطاقات بعرض متساوٍ: رقم 22/700 + تسمية 12. |
| M.StateView | حالة فارغة/خطأ: أيقونة 56 في دائرة `$neutral-soft`، عنوان 16/700، وصف 13 `$muted`، زر Outlined. |
| M.SyncRow | صف حالة المزامنة: أيقونة usb، نص "آخر مزامنة اليوم 09:10"، نقطة حالة. |

هيكل شاشة الهاتف: إطار 412×915 بتعبئة `$bg`، `clip:true`، عمودي: M.StatusBar، M.TopBar (أو M.OrgHeader ثم TopBar في الرئيسية)، Content (fill، عمودي، حشو [12,16] فجوة 12)، M.BottomNav في الأسفل. الـFAB بموضع مطلق (`layoutPosition:'absolute'`) عند x=16, y=815 (يسار الأسفل في RTL). الأوراق والحوارات طبقة مطلقة 412×915 بتعبئة `$scrim` والعنصر في الأسفل/المنتصف.
