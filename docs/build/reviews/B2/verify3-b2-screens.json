{
  "verdict": "accept",
  "build_ok": true,
  "tests_ok": true,
  "findings": [
    {
      "severity": "low",
      "file": "src/Wakeel.UI/Pages/W08AttentionCenter.razor",
      "issue": "The round-3 count mismatch is fixed for phone expenses, but the same defect is still latent for the two kinds the strip has no tab for: NeedsActionRows keeps AttentionEntityKind.Case and AttentionEntityKind.Decision, while NeedsActionTabs covers only correspondence/tasks/commitments. As soon as a case or decision needs action today, «الكل» will again exceed «مراسلات + مهام + التزامات» and those rows will be reachable through «الكل» only. The seeded folder holds none, so the host today reads «الكل 4 · مراسلات 0 · مهام 4 · التزامات 0» and the strip is correct.",
      "fix": "Either give NeedsActionTabs an «أخرى» entry whose Kinds are Case and Decision (counted like the others, label in Ar.Shell), or filter those two kinds out of NeedsActionRows the way PhoneExpense is filtered, since W09's own tabs already carry them. Whichever is chosen, a bUnit fact should seed a case row and assert that «الكل» equals the sum of the sub-tabs."
    },
    {
      "severity": "low",
      "file": "src/Wakeel.UI/Pages/W12HealthCenter.razor",
      "issue": "W12 draws a WBanner («هناك 4 مشكلات تحتاج تدخلاً») between the page header and the filter tabs; design/exports/preview/{light,dark}/W/W12 — مركز الصحة.jpg puts the tab strip directly under the header with no banner, so the host's grid starts roughly one banner-height lower than the export's. It is a Wakeel.Design component, correctly themed in both modes, and no tile is clipped (11 tiles, no horizontal overflow), so this is only an extra element, not a broken layout. It predates this round and earlier rounds did not record it either way.",
      "fix": "Either drop the banner and let the «أعطال 2 / تنبيهات 2» tab badges carry the same information the export relies on, or record it in docs/build/progress/b2-screens.md as an accepted addition with its reason (a summary line the export leaves to the tabs)."
    }
  ],
  "missing_spec_items": [
    "W08's monthly-report banner still shows 0% with «لم تُحسب بنود الجاهزية بعد» because ReadinessService lands in B5 — supervisor ruling 6, accepted; the banner is one row, so only the figure changes later.",
    "W94 still offers «المكلّف (اختياري)» where the export draws «الربط بمراسلة (اختياري)» — supervisor ruling 9, accepted until B4.",
    "W09's sort pill uses the clock glyph (registry has no sort glyph) — supervisor ruling 10, accepted. The same registry gap now also gives a case row the court-building glyph in W09's «النوع» cell, which is the only sensible glyph available."
  ],
  "notes": "BUILD / TESTS. `dotnet build src/Wakeel.Desktop` — 0 Warning / 0 Error, first attempt, no retry needed (the concurrent B3 documents work that broke round 3 now compiles). `dotnet build tests/Wakeel.UI.Tests --no-dependencies` — 0/0. `dotnet test tests/Wakeel.UI.Tests --no-build` — 340 passed, 0 failed, 0 skipped (339 last round, +1 new fact). TreatWarningsAsErrors is on and nothing is suppressed. I modified no file and did not write the progress file.\n\nROUND-3 FINDINGS, ONE BY ONE — ALL FIVE CLOSED, CONFIRMED IN THE CODE AND IN THE RUNNING HOST.\n1 (MEDIUM, WHealthCard head mirrored): closed. src/Wakeel.Design/Components/WHealthCard.razor now emits <span class=\"w-health-title\"> first and <WChip> second, with the icon block above the text inside the title; .w-health-head keeps justify-content: space-between. Measured live on W12's first tile, dark AND light, identical both times: card x 761..1106 (start = 1106), title 979..1090 (start edge), chip 778..838 (end edge) — the exact inverse of what round 3 measured, and the export's order on all eleven tiles. The footer (timestamp start, action end) is unchanged and still matches.\n2 (low, unisolated tile title): closed. The title is <bdi class=\"w-health-title-text\">@Title</bdi>; the host reports the first four tiles as BDI:قاعدة البيانات, BDI:الخزنة, BDI:Word, BDI:الماسح الضوئي, and «Word» reads correctly beside its Arabic neighbours in both themes.\n3 (low, W08 counts): closed for the case the review measured. NeedsActionRows drops AttentionEntityKind.PhoneExpense and the section-header count, CountFor and VisibleItems all read it; the host strip reads «الكل 4 · مراسلات 0 · مهام 4 · التزامات 0» and the two phone expenses appear only in their own card with «تأكيد»/«رفض» (item 50). The residual latent case is the first low finding above. The rewritten bUnit test W08_TheTypeTabs_CountAndFilterTheTodayList asserts real numbers (all = 3 = tasks 3 + correspondence 0 + commitments 0 and exactly three open buttons), not a tautology.\n4 (low, W09 kind glyph): closed. W08AttentionCenter.IconFor(AttentionEntityKind) is the single map (mail / check-square / flag / landmark / file-text / wallet) and W09's «النوع» cell draws a WIcon before the WChip; page CSS only arranges them. Measured in the host: cell 945..1104, icon 1073..1089 on the row's start edge, chip 1001..1062 beside it — the export's order. No component was hand-rolled and WChip was not disturbed.\n5 (low, extra header action): closed. W09's HeaderActions carries exactly «معالجة التالي» and «تصدير القائمة»; the host header reads those two and nothing else. RefreshAsync is gone, the screen still re-reads on ShellServices.Changed (subscribe at line 126, InvokeAsync dispatch at 140, unsubscribe in Dispose at 554) and after every export, and Ar.Attention.Refreshed is still used by W08's own «تحديث», so no Arabic string went dead.\n\nNO REGRESSIONS FOUND. Nothing this round touched the dispatch, the lock/sign-out stop, the exports or the Core note path; the two edited pages keep their subscription/dispose pair and the shared WHealthCard change is covered by the new fact. The only MarkupString in Wakeel.UI/Wakeel.Design remains WIcon's compile-time registry lookup — no user data becomes markup. Both file exports still go through IFileSaveService (a save dialog the person drives) and carry Arabic sentences only.\n\nHOST RUN (one launch, freshly seeded temporary data folder through HostDataFolderSeed, --remote-debugging-port=9333 --window-size=1366x768 --start-url=/login, 1366x768 device-metrics override, driven over CDP in tight batches). Signed in, then photographed and compared: W08 (dark + light) — KPI row, one-row cycle banner, tab strip whose counts add up, meetings/sync side column; W09 (dark + light) beside design/exports/preview/dark/W/W09 — same title, same two header actions, same tab order, same second strip (search · تصفية · sort pill · summary note), same seven columns, glyph+chip in «النوع», footer «عرض 1–1 من سجل واحد متأخر»; W12 (dark + light) beside design/exports/preview/dark/W/W12 — icon+title start, chip end, timestamp start, action end on all eleven tiles; W10 bell panel — one continuous list, «الكل 5 / غير المقروء 4», kind icon start, unread dot end, «تحديد الكل كمقروء» and the settings link, and it holds notifications the running host produced itself («حان وقت النسخ الاحتياطي · قبل دقيقتين») above the seeded meeting reminder «اجتماع قريب: لجنة المشتريات — عطاء صيانة المركبات», which is ReminderScheduler alive under the dispatcher; W94 opened by a real Ctrl+N key event from /w08 — 560 px dialog, single fields 520 px = full content width, the two-up row 260+260; W91 — .w-page 712/712 with all thirteen state cards, nothing below the fold; W92 — the five standard dialogs, all from WDialog/WButton/WInput. W11 was not re-photographed this round: nothing in the round-3 fixes touches it and round 3 measured it on the skewed-clock folder (header 78, banner 151, content 255). The host log holds exactly one line, «Wakeel desktop host starting up»; the process was killed and zero Wakeel.Desktop processes remain.\n\nRULES. No NuGet package added, nothing outside the package's allowed paths changed, every numeral on every screenshot a Western digit, no technical term, error code, port, server or database wording in any Arabic string, icon-only buttons all tooltipped, RTL with bdi isolation on the Latin runs. Verdict is accept: build and tests are green and the two remaining findings are low."
}