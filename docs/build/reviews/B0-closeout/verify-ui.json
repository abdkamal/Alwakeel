{
  "verdict": "accept",
  "build_ok": true,
  "tests_ok": true,
  "findings": [
    {
      "severity": "low",
      "file": "src/Wakeel.UI/Components/WSectionHeader.razor",
      "issue": "The new Count badge renders BEFORE the <h2> title inside .w-section-header-title-row, so under RTL it appears to the RIGHT of the title. The reference PNG (design/exports/light/W/W08 — مركز الانتباه.png) puts it to the LEFT: pixel-sampled at y=598 the primary-coloured badge glyph occupies x=848..866 while the title text runs x=879..1092 (RTL => title first, badge second). It also contradicts the repo's own established convention — WTabItem.razor and WSidebar.razor both render <span>@Label</span> first and <WBadge> after.",
      "fix": "In WSectionHeader.razor swap the two children of .w-section-header-title-row so the title comes first: `<h2 class=\"w-section-header-title w-text-section\">@Title</h2>` then `@if (Count is not null) { <WBadge Count=\"Count.Value\" /> }`."
    },
    {
      "severity": "low",
      "file": "src/Wakeel.UI/Pages/W08AttentionCenter.razor.css",
      "issue": "Only the overdue row's الاستحقاق cell is colour-coded. In the reference PNG the second row («اليوم 16:00») is rendered in the warning colour: pixel-sampled RGB 160,107,24 == #A06B18 == var(--w-warning) (row 1 sampled 164,62,67 == #A43E43 == var(--w-danger), row 3 is default text 29,39,45). The instruction's parenthetical named only «danger for متأخر», so the builder's reading was defensible, but the build still diverges from the approved design.",
      "fix": "Add `.w08-due--warning { color: var(--w-warning); font-weight: 600; }` to W08AttentionCenter.razor.css and apply `class=\"w08-due w08-due--warning\"` to the ActionRow2Due <bdi> (the «اليوم 16:00» row)."
    },
    {
      "severity": "low",
      "file": "tests/Wakeel.UI.Tests/ShellTests.cs",
      "issue": "The expenses table restructuring (item 3) has no assertion. ShellTests only checks the KPI sample values, the attention-row class and PageHeaderState. The exact defect the previous review found (a stray @Ar.LocalSearch.Placeholder occupying a <th>, header/body column counts disagreeing) would still pass the suite today.",
      "fix": "Add a bUnit test rendering W08AttentionCenter that asserts the expenses table has 5 <th> cells whose visible texts are الموظف/البيان/التاريخ/المبلغ plus a .w-visually-hidden label, that every body row of that table has 5 <td>, and that the date cells are <bdi> containing Ar.AttentionCenter.Expense1Date/Expense2Date."
    },
    {
      "severity": "low",
      "file": "src/Wakeel.UI/Text/Ar.cs",
      "issue": "`Ar.AttentionCenter.PendingExpensesCountBadge = \"6\"` is now dead: W08AttentionCenter.razor passes the literal `Count=\"6\"` to WSectionHeader instead. A stale unused user-facing string invites future drift between the constant and the markup.",
      "fix": "Either delete PendingExpensesCountBadge, or change it to `public const int PendingExpensesCount = 6;` and use `Count=\"@Ar.AttentionCenter.PendingExpensesCount\"` in W08AttentionCenter.razor."
    },
    {
      "severity": "low",
      "file": "tests/Wakeel.UI.Tests/BidiWrapTests.cs",
      "issue": "All 9 bidi-test.html cases are asserted with exact full-string equality (verified line by line against docs/design/bidi-test.html lines 49/54/59/64/69/74/79/84/89) except W04, where the suite deliberately asserts Isolated(\"1\") in «الجهاز 1» while the reference page leaves that digit outside <bdi>. The deviation is documented in the file's doc comment and is the self-consistent behaviour (the page isolates the equally solitary «D» a few cases above), but code and the normative design reference are now knowingly out of step.",
      "fix": "Raise the inconsistency to the supervisor and fix it in docs/design/bidi-test.html (wrap «1» in <bdi> in the W04 «مع العزل» line) so the reference and the suite agree; record the decision in docs/build/BUILD-PLAN.md. No code change needed."
    },
    {
      "severity": "low",
      "file": "src/Wakeel.UI/Pages/W08AttentionCenter.razor",
      "issue": "Two further gaps vs the reference PNG that were never in the item list: the «يحتاج إجراء اليوم» WSectionHeader carries no count badge (the PNG shows a «9» badge beside the title, now supportable via the new Count parameter), and the الموضوع cells of that table lack the meta sub-line the PNG shows under each subject («بلدية نابلس • 20260912/12046», «مهمة مرتبطة بالمراسلة 20260908/12019», «التزام شهري • دائرة المالية»).",
      "fix": "Pass `Count=\"9\"` to the TodayActionTitle WSectionHeader, and add a `<div class=\"w08-row-meta\"><bdi>…</bdi></div>` sub-line inside each الموضوع <td> reusing the existing `.w08-expense-meta` styling (or a shared `.w08-row-meta` rule); add the three strings to Ar.AttentionCenter."
    }
  ],
  "unresolved_items": [
    "docs/build/BUILD-PLAN.md still documents `dotnet test tests/Wakeel.UI.Tests --no-dependencies` as the acceptance command; I re-confirmed the SDK forwards the switch to MSBuild, which rejects it. The builder correctly could not fix this (docs/ is outside its allowed paths), so it remains open for the supervisor: change the documented command to `dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build`.",
    "ARCHITECTURE.md §قرار 2026-09-16 (line 131) moves the design system (components, icons, tokens/css/js, Bidi, Ar.* texts) into a shared `Wakeel.Design` RCL with `Ar` split into per-package partial files. Everything still lives in Wakeel.UI; src/Wakeel.Design currently holds only a .csproj and _Imports.razor (another agent's in-flight work). Out of scope for this closeout — flagged so it is not lost for B0.5."
  ],
  "notes": "Commands run exactly as mandated, full output read: `dotnet build src/Wakeel.UI --no-dependencies` -> 0 warnings / 0 errors; `dotnet build src/Wakeel.Desktop --no-dependencies` -> 0/0; `dotnet build tests/Wakeel.UI.Tests --no-dependencies` -> 0/0; `dotnet test tests/Wakeel.UI.Tests --no-build` -> Passed! 136/136. I then re-ran the full suite 5 more consecutive times myself: 136/136 every time (631, 621, 723, 632, 646 ms). Item 1 is genuinely fixed — the flaky Task.Delay is gone, replaced by `cut.WaitForState(() => !cut.Find(\"button\").HasAttribute(\"disabled\"))` (WButtonTests.cs lines ~93-97). No file was modified by me; Wakeel.Core was never built.\n\nItem-by-item verification:\n1. DONE — deterministic wait in place, 6 green full-suite runs observed by me (1 mandated + 5 repeats).\n2. DONE — Bidi.cs TokenPattern is now `[A-Za-z0-9](?:[A-Za-z0-9./\\\\_:@+,\\- ]*[A-Za-z0-9])?`; the space is inside the interior class only, and the both-ends alphanumeric anchor keeps trailing «.»، «،»، «·»، «—» and the ₪ sign outside the isolate. I diffed all 9 «مع العزل» lines of docs/design/bidi-test.html (lines 49,54,59,64,69,74,79,84,89) against the test file's expected strings: every one matches exactly, including \"184 GB\", \"512 GB\", \"Canon DR-C240\", \"Microsoft Word 365\", \"1.8 GB\", \"D:\\Backups\\AlWakeel\", \"20260912/12046\", \"1,250\" — one isolate each. The only divergence is the W04 lone \"1\" (low finding above). The tests are real Assert.Equal on fully reconstructed strings, not Assert.Contains. The only production caller of Bidi.Wrap is MainLayout.razor line 24 (HeaderState.Sub); the W08 sub string «… السبت 12/09/2026، مكتب نابلس» still isolates exactly \"12/09/2026\" because «،» is not in the class — no regression.\n3. DONE (with the two low fidelity nits above). I opened design/exports/light/W/W08 — مركز الانتباه.png. Expenses table now has الموظف | البيان | التاريخ | المبلغ | visually-hidden «إجراءات الموافقة»; the stray Ar.LocalSearch.Placeholder <th> is gone; header and both body rows are 5 cells; dates are `<bdi>11/09/2026</bdi>` / `<bdi>10/09/2026</bdi>`; the البيان meta sub-lines («فاتورة جوال • 0599-482-117», «إيصال شحن • 09-2381-440») render inside `.w08-expense-meta`; the WSectionHeader carries Count=6 + a local WSearch with BindGlobalShortcut=false (the Ctrl+F hint chip still shows, matching the PNG) + the «عرض كل المصروفات» text action; تأكيد/رفض are WButtonVariant.Text with check/x and `::deep` success/danger colours; the action table has the trailing chevron column with the visually-hidden «فتح التفاصيل» label and the overdue الاستحقاق in danger. All strings are in Ar.AttentionCenter. «دورة سبتمبر» is preserved in MonthlyReportBanner per the supervisor ruling — correct.\n4. DONE — WMenu.razor splits the trigger: the TriggerContent branch is untouched (WTopBar avatar unaffected), the default branch is wrapped in `<WTooltip Text=\"@Ar.Menu.MoreActions\">` with a matching aria-label. `Ar.Menu.MoreActions = \"خيارات إضافية\"` added. Real assertions in ListsAndTableTests.cs lines 150-151 (aria-label equality + tooltip bubble text). The tooltip wrapper is inline-flex inside the still-relative .w-menu-wrap, so popover/backdrop positioning is unchanged.\n5. DONE — Ar.Pager.Label/Previous/Next and Ar.Breadcrumb.Label exist (Ar.cs lines 171-182) and both components reference them; no Arabic literal remains in either file.\n6. DONE — Gallery.razor lines 114-127: all six official-number/date cells are now `<bdi>`.\n7. DONE — .w-sidebar-org-icon is 44x44 with `background: var(--w-white); color: var(--w-primary)`; --w-white (#FFFFFF) is defined in all three token blocks of tokens.css. The English `@* TODO(B2) … *@` Razor comment compiles away, so nothing user-visible was added.\n8. DONE — `FormattableString.Invariant($\"width:{Size}px;height:{Size}px;font-size:{(int)Math.Round(Size * 0.42)}px;\")`.\n9. DONE — wwwroot/icons/ban.svg is byte-consistent with the other vendored Lucide files (same license comment, 24x24 viewBox, fill/stroke/linecap/linejoin attributes); IconRegistry.g.cs line 18 registers [\"ban\"] in correct alphabetical position with the exact path data; ResolvedConfirmIcon's danger default is \"ban\"; WDialogTests asserts the glyph's path data (\"m4.9 4.9 14.2 14.2\") is inside the danger confirm button's InnerHtml.\n10. DONE — WTopBar 56px (height + min-height), WSidebar 232px (width + min-width), and a grep over every .css under src/Wakeel.UI for physical `left:`/`right:`/`margin|padding|border-left|right` returns nothing (only logical inset-inline/margin-inline properties).\n\nNo regression found: 0 warnings under TreatWarningsAsErrors, no NuGet package added, Directory.Packages.props / Directory.Build.props / Wakeel.slnx / docs/ untouched by this package (git status shows only concurrent-agent edits elsewhere), no git commit/checkout/stash/reset. Wakeel.Desktop was not modified by this round and builds clean. No high or medium defect was introduced — all six findings above are low, so the package is accepted with them recorded as polish for a later round."
}