{
  "status": "partial",
  "files_changed": [
    "src/Wakeel.Design/Components/WInput.razor",
    "tests/Wakeel.UI.Tests/Components/WInputTests.cs",
    "docs/build/progress/design-verify.md"
  ],
  "tests_total": 188,
  "tests_passed": 188,
  "build_ok": true,
  "open_issues": [
    "Finding 1 (high, external, not this package's files): src/Wakeel.Desktop/App.xaml.cs still does not call AddWakeelAccount() (and now also lacks a TimeProvider registration reached via /first-run -> W02SetupFile). tests/Wakeel.UI.Tests/WakeelTestContext.cs was fixed by the concurrent B1 first-run package during this round (confirmed via a transient CS1061 mid-edit, then green one retry later), which is why all 188 UI tests are now green — but App.xaml.cs itself is unfixed, so a live launch of Wakeel.Desktop.exe still crashes at startup (System.InvalidOperationException: no registered service of type 'System.TimeProvider', logged at C:\\ProgramData\\Wakeel\\logs\\wakeel-20260916_002.log 14:41:00), and even when it doesn't crash there is no route from --start-url=/w08 to the W08 screen without an activated installation (MainWindow.DefaultStartPath forces /first-run). Retried the host launch 4x at 60s intervals per protocol; the masked-date field / W08 badge could not be re-verified live in the real host this round either. Nothing in this package's four allowed files can fix it -- owner of the B1 first-run package needs to add services.AddWakeelAccount() (and a TimeProvider registration) to App.xaml.cs."
  ],
  "notes": "Fixed both low findings that were genuinely inside this package's allowed paths: (1) WInput.razor OnDateInputAsync's catch(JSException) widened to catch (Exception ex) when (ex is JSException or JSDisconnectedException or ObjectDisposedException), matching OnAfterRenderAsync's existing widening, so a getSelectionStart interop call racing window/renderer teardown no longer throws out of a Blazor event handler. (2) Added WInputTests.cs DateType_OutOfOrderInteropReplies_SupersededKeystrokeIsDroppedNotEmittedStale, which pins the _inputSeq out-of-order guard: it leaves wakeelUi.getSelectionStart's JSInterop setup \"planned\" (no SetResult), fires two Input() calls so both keystrokes suspend on their own pending interop call, then resolves the shared handler so the older (\"1\") keystroke's continuation resumes before the newer (\"12\") one's (matching the reviewer's suggested scenario) -- asserting the superseded keystroke's ValueChanged is dropped entirely (only \"12\" is emitted) and only the newer (value, caret) pair reaches wakeelUi.setInputValue. Needed cut.WaitForAssertion(...) after SetResult since the interop continuation resumes via bUnit's renderer dispatcher rather than synchronously on the test thread.\n\nRan the exact mandated chain (dotnet build src/Wakeel.Design --no-dependencies && dotnet build src/Wakeel.UI --no-dependencies && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build) fresh at the end: all four commands green, 188/188 tests passed (0 failed) -- this includes the 4 MainLayout/Gallery tests the review reported failing, which are now green because the concurrent B1 first-run package fixed tests/Wakeel.UI.Tests/WakeelTestContext.cs's missing AccountSession registration while this round ran (I did not touch that file). Filtered rerun of this package's own tests: WInputTests|W08AttentionCenter -> 26/26 passed.\n\nAttempted the mandated live host re-check per the RULES (a package touching Blazor pages is not done until the host runs): rebuilt src/Wakeel.Desktop --no-dependencies and launched Wakeel.Desktop.exe --remote-debugging-port=9333 --window-size=1366x768 --start-url=/w08, retried 4x at 60s intervals. The host still crashes at startup on a fresh installation (lands on /first-run -> W02SetupFile, which needs an unregistered System.TimeProvider -- App.xaml.cs registers AddWpfBlazorWebView/AddWakeelDesign/PageHeaderState only, never AddWakeelAccount() or a TimeProvider), and even on the one attempt that didn't crash, --start-url=/w08 could not reach W08 because MainWindow.DefaultStartPath forces /first-run for a non-activated installation and the app's own routing/auth gate overrides the requested start path. This is the same class of external, this-package-can't-fix issue the review's finding 1 already documented (App.xaml.cs is outside my four allowed files: src/Wakeel.Design/Components/WInput.razor, src/Wakeel.Design/Components/WInput.razor.css, src/Wakeel.Design/wwwroot/js/app.js, src/Wakeel.UI/Pages/W08AttentionCenter.razor, tests/Wakeel.UI.Tests). Every Wakeel.Desktop.exe process I launched (3 across the attempts, PIDs differing from bash's reported $! each time) was found via tasklist and killed; confirmed 0 remain at the end.\n\nW08's own spec item (the «الكل» tab using Ar.AttentionCenter.TodayActionCount) needed no further change -- it was already correct from the prior round and stays covered by the green W08AttentionCenter_AllTabBadge_MatchesTodayActionCount bUnit test plus the design preview jpg comparison already recorded in the previous verification round's notes.\n\nScope discipline: touched only src/Wakeel.Design/Components/WInput.razor and tests/Wakeel.UI.Tests/Components/WInputTests.cs this round (plus this progress file). No csproj/Directory.Build.props/Directory.Packages.props/Wakeel.slnx/docs (other than the progress file) edited, no NuGet package added, no git commit/checkout/stash/reset/clean run. git status confirms tests/Wakeel.UI.Tests/WakeelTestContext.cs, ShellTests.cs, App.xaml.cs and other modified/untracked files belong to the concurrent B1 first-run package, not this session.\n\nStatus is \"partial\" only because the mandated live-host acceptance step could not be completed due to the external App.xaml.cs gap (finding 1) -- every fix and test within this package's own scope is done and green."
}