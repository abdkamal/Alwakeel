# B7-2a — android-core (أندرويد: المشروع والسمة والمكوّنات M.* والنواة)

- 2026-09-17: Gradle skeleton written: android/settings.gradle.kts, build.gradle.kts, gradle.properties, gradle/libs.versions.toml (AGP 8.13.2, Kotlin 2.2.10, KSP 2.2.10-2.0.2, Gradle 8.13), modules app/core/design/ocr/camera, app manifest without INTERNET, local.properties, Gradle wrapper generated.
- 2026-09-17: design module done: WakeelColors/Typography/Theme (tokens.json, RTL by default), Bidi helper (BidiFormatter + isolates), 33 Lucide vector drawables, every M.* composable (bars, list item, cards, buttons/chips/FAB, text fields, sheet/dialog/snackbar/state view, tabs, segmented row, sync row, avatar, badge), MPhoneScreen frame, @Preview per component, KitGallery; Roborazzi + Robolectric screenshot tests green, 4 images in android/app/build/outputs/roborazzi.
- 2026-09-17: core crypto written and compiling: Base64Url/Base64Standard, Sha256, HmacSha256, HKDF, AES-256-GCM (buffer + framed stream, byte compatible with Wakeel.Crypto/Primitives/Aead.cs), RandomBytes, DomainSeparation, DeviceSeeds/DeviceIdentity (Ed25519 + X25519 over Bouncy Castle), KeyVault (Android Keystore + in-memory), canonical JSON (own parser and writer matching System.Text.Json UnsafeRelaxedJsonEscaping + camelCase + ordinal member order).
- 2026-09-17: core containers, certificates, pairing, Room database (15 tables of DATA-MODEL §14 + DAOs, SQLCipher on the phone / in memory in tests), WakeelFolder (java.io.File + Storage Access Framework), PhoneFileStore (app-private files encrypted with a Keystore-wrapped key), PhoneSyncEngine (ordered discovery, atomic apply per packet, acknowledgement, outbox packaging with the 20 MB split) — :core:compileDebugKotlin green.
- 2026-09-17: interoperability vectors done: tests/Wakeel.Crypto.Tests/AndroidVectorTests.cs writes fixed-key vectors (keys, primitives, canonical JSON, certificates + revocations, a signed to-phone-1.wakeel-phone with its expected manifest, the pairing QR payload, pairing-371212.wakeel-phone, request and accept) into android/core/src/test/resources/vectors/; `dotnet test --filter AndroidVector` green (2 tests).
- 2026-09-17: core JUnit suite green: 80 tests over 7 classes (canonical JSON, primitives, containers, certificates incl. a revoked device, pairing messages, the 15 DAOs, and the sync engine: ordering, gap, atomic apply, foreign signature, partly written file, truncated packet, files into private storage, receipt, outbox packaging, the 20 MB split); the Android-made samples are written to android/core/build/vectors-out/.
- 2026-09-17: shell test fixed (missing onAllNodesWithText/assertCountEquals imports) and :app:testDebugUnitTest green; shell-light/dark/tasks written.
- 2026-09-17: design review against the previews: MPhoneScreen's action button moved to the reading end (bottom left in Arabic, as M06 draws it) instead of the bottom start, an orgHeader slot added between the status strip and the bar, the kit gallery window raised to 3400dp so the sheet and the bottom bar are no longer clipped, MSheetOption given a tone so the quick-entry circles carry the colours of M11, and the «+٩٩» comment in MFoundation.kt replaced by Western digits.
- 2026-09-17: full build green: :design:testDebugUnitTest :core:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug — 91 Android tests, 0 failures; package B7-2a complete.
