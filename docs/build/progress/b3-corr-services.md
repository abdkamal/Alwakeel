# b3-corr-services — progress

- CoreAr.cs: appended the B3-1 correspondence Arabic strings (states, steps, readiness, refusals, follow-up, referral, duplicate, exchange, audit summaries).
- Services/Correspondence: CorrespondenceModels.cs (views, inputs, refusal exception, Arabic maps), CorrespondenceStateMachine.cs (allowed transitions), DuplicateDetector.cs (exact/same-number/Jaccard 0.8), CorrespondenceService.cs (drafts, validation, register-in, readiness, approve-out, delete/cancel/close/archive, links, recipient-only). Core builds green.
- Services/Correspondence: ReferralService.cs (+IDerivedDocumentBuilder contract for the letter package), FollowUpService.cs (timeline, awaiting-reply cards, due reminders), CorrectionService.cs (post-numbering corrections keeping both versions), ExchangeService.cs (.wakeel-msg export/preview/import).
- Wakeel.Core.csproj: added ProjectReference to Wakeel.Crypto (containers); ServiceCollectionExtensions.cs: registered all six correspondence services. Core builds green.
- Migrations/0003_correspondence_number_index.sql: replaced the GLOBAL unique index on correspondence.official_number with a per-direction one (the two sequences of ARCHITECTURE §5 legitimately produce the same number text on one day); SchemaTests.cs updated to the new index name.
- Migrations/0004_correspondence_indexes.sql: indexes for the duplicate check, the awaiting-reply cards, the follow-up reminder pass, the duplicate reviews and the exchange log.
- ExchangeService: refuses a package already imported here, and a message whose formatVersion is newer than this build.
- Tests: CorrespondenceWorld.cs + 7 test classes (112 new tests). dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests => 449/449 passed, 0 warnings.
- PACKAGE COMPLETE.
- Review fixes: CoreAr (field labels, correction refusals, cancel-draft, exchange cancelled/external signature, recipient-only and links audit), CorrespondenceAr.FieldLabel, CorrectionService two-pass apply + Arabic-ready CorrectionFieldView, DuplicateDetector typed-name SameParty, CorrespondenceService audit rows + cancel wording, ExchangeService cancelled export + import rollback + external signature wording, FollowUpService narrowed reminder queries. Core builds green.
- Review round 1 applied: 13 new tests (correction batch atomicity + Arabic-ready record + value refusals, typed-name duplicates x3, cancel-draft wording, recipient-only/links audit, cancelled export, external signature wording, import rollback). dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests => 462/462 passed, 0 warnings.
- OPEN (outside this package's paths): ReminderScheduler.RunOnceAsync still has no correspondence follow-up pass, so IFollowUpService.RunDueRemindersAsync is never called.
- Review round 2 applied: FollowUpService.AddAsync now refuses «ملغى»/«مغلق»/«مؤرشف» (CoreAr.CorrRefusedFollowupStatus) so the cancel reason, the closing note and the archiving instant can only be written by their own command; ExchangeService resolves the sender unit once per read and the already-imported probe matches on that unit for an internal letter (the party name is overwritten with the local unit name at numbering time); ReferralService derives «متأخرة» through ArabicRelativeTime.ToUtc on both sides; CorrespondenceService.ListAsync reads a bounded TextScanLimit window before matching the free text and only then takes the page.
- Review round 2 tests: 7 new (3 refused timeline states + the dedicated commands' stored fields, a Local-kind referral deadline on either side, a subject below the returned page found by a ة/ى variant) and the twice-imported exchange test now sets up the internal item-49 configuration. dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests => 469/469 passed, 0 warnings.
- Review round 3 applied (supervisor rulings): (1) ReminderScheduler.RunOnceAsync now resolves IFollowUpService and folds RunDueRemindersAsync into its aggregate ReminderRun — the constructor gained the dependency and DailyShellWorld/CorrespondenceWorld wire it; (2) soft delete of an unnumbered draft kept as ratified — no change; (3) migration 0003 and the SchemaTests index name kept as ratified — no change; (4) CoreAr gained a public Isolate(value) helper (U+2068 … U+2069) and every CorrAudit* formatter plus the Health* sentences that embed a size, version, name, date or file name now isolate the embedded run; (5) ChangeStatusAsync and FollowUpService.AddAsync both refuse a Draft source (CoreAr.CorrRefusedDraftNeedsNumbering), ExchangeService ignores a caller-supplied RecipientPublicKey for a «للمستلم فقط» letter and resolves the addressee's key from the structure/party or refuses, ExchangeMessage carries RecipientOnly and the import keeps it, ReferralService audits every referral status change (CoreAr.CorrAuditReferralStatus) and refuses a referral on a cancelled or archived letter (CoreAr.CorrRefusedReferralStatus), and RollbackAsync sweeps only the entities the operation touched (the row itself plus OfficialNumber/DuplicateReview/AuditLogEntry/ChangeLogEntry).
- Review round 3 tests: 10 new (draft refused by ChangeStatusAsync and by a follow-up entry, a due follow-up ringing exactly once through the whole ReminderScheduler pass, referral status audit rows, referral refused on a cancelled and on an archived letter, recipient-only export refusing a caller key, recipient-only surviving the exchange round trip, a failed registration leaving another screen's unsaved work alone, stored audit lines carrying the bidi isolates); HealthServiceTests' two composed-string assertions now read through the CoreAr formatters. dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests => 479/479 passed, 0 warnings.
