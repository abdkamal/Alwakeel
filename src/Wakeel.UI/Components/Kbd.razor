@namespace Wakeel.UI.Components

<bdi class="w-kbd">@Text</bdi>

@code {
    /// <summary>The shortcut text to display, e.g. "Ctrl+F". Always Latin, so it is rendered inside a &lt;bdi&gt; isolate.</summary>
    [Parameter, EditorRequired] public string Text { get; set; } = string.Empty;
}
