export const meta = {
  name: 'alwakeel-b7-android',
  description: 'B7-2 the Android companion app (android/ only): core (Gradle project, theme and M.* components, Room + SQLCipher, containers binary-compatible with Wakeel.Crypto, pairing and sync over a folder) → shell (pairing, lock, home, settings, notifications) → capture (camera, OCR, quick capture, expenses, voice notes) → records (correspondence, tasks, meetings, calendar, decisions, parties, search, pinned files) → sync screens and the end-to-end scenario; one sequential chain of Opus packages, each reviewed, fixed and re-verified by Opus; resumable through args.resume/args.skip',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }, { title: 'Fix3' }, { title: 'Verify3' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short android' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a module, a screen, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (they overload the connection). View the small JPEG previews under design/exports/preview/light/M/<name>.jpg and design/exports/preview/dark/M/<name>.jpg, only the screens of your own package, each at most once per comparison, right before you build or check that screen. (5) Screenshots you produce must be PNG or JPEG at most 412 px wide per phone screen and viewed once. (6) When a report is handed to you as {"see": "<path>"}, read that JSON file first — it is the archived report of the previous stage.

DESIGN CONSISTENCY (owner's rule, 2026-09-16 — «انتبه للتصميم: لا يوجد تناسق وتنظيم في المكونات»): every phone screen is composed from the M.* composables of the android/design module (the Android kit of docs/design/ANDROID-KIT.md: MStatusBar is the system's, MTopBar, MOrgHeader, MBottomNav, MFab, MListItem, MCard, MChip, MBadge, MButton, MTextField, MSearchBar, MSheet, MDialog, MSnackbar, MAvatar, MTabs, MSegmentedRow, MStateView, MSyncRow). Hand-rolled lists, cards, chips, tabs, dialogs or text fields inside a screen are forbidden; a screen file only arranges kit composables with spacing from the theme. If a composable is missing, add it to android/design first (composable + preview + screenshot test), then use it. A screen is done only when its rendered screenshot, put beside the preview, shows the same anatomy: same regions, same controls in the same places, same chips and states, no overlap, no clipping, aligned rows, consistent spacing, correct RTL. A builder writes the side-by-side comparison of every screen in its notes (deviation → fixed / accepted with reason). A reviewer opens the builder's screenshot and the preview together and reports every visible deviation as a finding: high for a hand-rolled component, medium for a layout, alignment, overlap or missing-control deviation, low for a spacing nuance.

RULES: work only under android/ (plus the paths your package names). Never edit the .NET solution, Directory.*.props, Wakeel.slnx or docs/ (except your progress file). Never run git commit/checkout/stash/reset/clean. Dependency versions are pinned in android/gradle/libs.versions.toml (Kotlin DSL, version catalog) — add a library only when the package names it or it is truly required (report it in open_issues), never a dynamic version. JDK 17 and the Android SDK are installed (ANDROID_HOME or %LOCALAPPDATA%\\Android\\Sdk; platforms android-35, build-tools 35.0.0); write android/local.properties if needed (it is git-ignored). Use the Gradle wrapper (android/gradlew.bat; generate it in the core package) with --offline when the caches allow; .NET agents build in the same machine concurrently (4 cores) — run Gradle with --max-workers=2 and org.gradle.jvmargs=-Xmx2g, never start an emulator unless your package says so, and stop the Gradle daemon (gradlew --stop) when you finish. minSdk 29, targetSdk 35, Kotlin + Jetpack Compose (Material 3), LocalLayoutDirection = Rtl for the whole app, BidiFormatter (or Unicode isolates) on every mixed Arabic/Latin/numeric text (item 55), Noto Sans Arabic bundled as a font resource, Lucide icons as vector drawables, light and dark themes generated from design/tokens.json. All user-facing text is Arabic in res/values/strings.xml with no technical terms or error codes (item 15), numerals are always Western digits 0–9 and never Arabic-Indic (design guide «الأرقام غربية», item 20) and no mention of servers, ports, internet or PostgreSQL; the app requests NO internet permission at all (item 9: USB file transfer only). Secrets: private keys are generated on the phone and kept wrapped by an Android Keystore key; the database is SQLCipher with a key wrapped by Keystore; files in app-private storage are encrypted; passwords, PINs, keys and seeds never reach logs, the database in clear text, or UI state longer than needed. Identifiers and comments in English. Screens are verified on the JVM: Roborazzi + Robolectric screenshot tests render every screen at 412x915 in light and dark into android/app/build/outputs/roborazzi/ — LOOK at each PNG beside its preview. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON = `Repository: ${REPO} (branch main). The Android companion of الوكيل v0.21 lives in android/ (Gradle Kotlin DSL; modules app, core, design, ocr, camera). Governing documents: docs/build/packages/B7-android.md (section B7-2 — the Windows side B7-1 is another package), docs/AGREEMENT.md (items 4, 9, 11, 15, 27, 28, 50, 53 (a), 55, 56) and docs/v0.2.md (items 6–17), docs/design/SCREENS.md (rows M01–M32), docs/design/ANDROID-KIT.md (the M.* kit), docs/design/DESIGN-GUIDE.md and design/tokens.json (colours, type, radii, spacing), docs/build/ARCHITECTURE.md (§1, §3, §4 container format, §7 pairing and USB file sync), docs/build/DATA-MODEL.md (§11, §14 the phone database). The binary formats are defined by the .NET implementation — read it, do not guess: src/Wakeel.Crypto (Containers/ContainerWriter+Reader+Manifest+Kind, Json/CanonicalJson+DomainSeparation, Primitives/Aead+Hkdf+Sha256+Argon2Kdf, Certificates/DeviceCertificate+CertificateChain+RevocationList, Pairing/PairingQrPayload+PairingToken+PairingMessages, Base64Url).\n\n${PROTOCOL}`

const PACKAGES = [
  {
    key: 'android-core', title: 'B7-2a (أندرويد: المشروع والسمة والمكوّنات M.* والنواة — القاعدة والتشفير والحاويات والاقتران والمزامنة على مجلد)', model: 'opus',
    paths: 'android/ (new Gradle project), .gitignore (android build outputs, local.properties, android/keystore/ — additions only), tests/Wakeel.Crypto.Tests (one new test class AndroidVectorTests that writes interoperability vectors to android/core/src/test/resources/vectors/ — build it with: dotnet build tests/Wakeel.Crypto.Tests && dotnet test tests/Wakeel.Crypto.Tests --no-build --filter AndroidVector)',
    build: 'cd android && gradlew.bat --max-workers=2 :design:testDebugUnitTest :core:testDebugUnitTest :app:assembleDebug',
    security: 'Ed25519/X25519/AES-GCM/HKDF/SHA-256 through one vetted library (BouncyCastle bcprov-jdk18on, pure Java, works on API 29) with private keys wrapped by an Android Keystore AES key behind an interface that unit tests replace; SQLCipher key wrapped the same way; container verification order identical to the .NET reader (signature and certificate chain before any decrypted byte is used); a package is applied atomically or not at all; no INTERNET permission in any manifest; nothing secret in logs',
    extraCheck: 'containers written by .NET open on Android and containers written by Android match what the .NET reader expects (vectors in both directions: canonical JSON bytes, domain-separated signatures, AEAD chunks, manifest hash); the pairing request/accept messages interoperate with Wakeel.Crypto Pairing; the theme values equal design/tokens.json in light and dark; every M.* composable of the kit exists with a screenshot test',
    spec: `Create the Android project and its foundation. (1) Gradle Kotlin DSL project android/ with the wrapper, version catalog, modules app (single-activity Compose shell with an empty navigation host and the five-item bottom navigation), core, design, ocr and camera (the last two as empty library modules wired into settings.gradle.kts for later packages); namespace ps.wakeel.phone (applicationId the same); minSdk 29, targetSdk 35, compileSdk 35; no INTERNET permission. (2) android/design: WakeelTheme generated from design/tokens.json (light/dark colour schemes, typography with bundled Noto Sans Arabic, shapes, spacing), RTL by default, a Bidi helper wrapping BidiFormatter for mixed text, Lucide vector drawables for the icons the previews use, and every composable of docs/design/ANDROID-KIT.md (MTopBar, MOrgHeader, MBottomNav with badges, MFab + extended, MListItem + Attention variant, MCard + Info/Warning/Danger/Success, MChip + Selected, MBadge, MButton Filled/Outlined/Text/Danger, MTextField + Error + Amount with ₪, MSearchBar, MSheet, MDialog, MSnackbar, MAvatar + Photo, MTabs with badges, MSegmentedRow, MStateView, MSyncRow) with @Preview functions and Roborazzi screenshot tests in light and dark (a kit gallery image per theme). (3) android/core: the Room + SQLCipher database of DATA-MODEL §14 (correspondence_summary, tasks, meetings, appointments, decisions, commitments, contacts, notifications, phone_expenses, captures, notes, pinned_files, settings, outbox, sync_state) with DAOs; crypto (KeyStore-wrapped device keys behind a KeyVault interface, Ed25519 sign/verify, X25519 agreement, HKDF, AES-GCM, SHA-256, Base64Url, canonical JSON identical to Wakeel.Crypto CanonicalJson, domain separation strings identical to DomainSeparation); the container reader/writer for kind phone (.wakeel-phone) byte-compatible with Wakeel.Crypto ContainerWriter/Reader; certificates (device certificate parsing, chain authority → computer → phone verification, revocation list check); pairing (parse the QR payload and the pairing-<code>.wakeel-phone file, build the signed pairing-request with the HMAC of the token, read pairing-accept and store the session key, office key and chain); the sync engine over a folder abstraction (WakeelFolder interface with a java.io.File implementation for tests and a Storage Access Framework DocumentFile implementation for the app): discover to-phone-<seq> packages, verify, apply each package atomically in one Room transaction, write the acknowledgement with the last applied sequence, build from-phone-<seq> packages (≤ 20 MB each) from the outbox, survive interruption (a partly written file is ignored and rewritten). (4) Interoperability vectors: add tests/Wakeel.Crypto.Tests/AndroidVectorTests.cs that writes fixed-key vectors (canonical JSON samples, a signed manifest, a complete small .wakeel-phone container with its keys, the pairing QR payload, pairing file, request and accept) into android/core/src/test/resources/vectors/, and JUnit tests in android/core that read them and also write Android-made samples into android/core/build/vectors-out/ in the same layout (a later .NET test will read those). JUnit tests for the DAOs (Robolectric or in-memory with the SQLCipher test artefact), crypto primitives against the vectors, container round trips, chain verification incl. a revoked device, pairing messages, the sync engine (ordering, atomic apply, ack, interrupted file, outbox packaging and the 20 MB split).`,
  },
  {
    key: 'android-shell', title: 'B7-2b (أندرويد: الاقتران والقفل والرئيسية والتنقل والإعدادات والإشعارات — M01–M06, M24–M27, M30, M31)', model: 'opus',
    paths: 'android/',
    build: 'cd android && gradlew.bat --max-workers=2 :core:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug',
    security: 'the biometric lock (BiometricPrompt, class 3 or device credential fallback per the spec: an app PIN hashed with Argon2id or PBKDF2 behind Keystore) only opens the app — it never approves correspondence; the PIN never lives in state after verification; the QR scanner reads only the pairing payload and rejects anything else; SAF folder permission is persisted and re-checked; exact alarms scheduled only from synced data; no INTERNET permission',
    extraCheck: 'M01–M06, M24–M27, M30 and M31 match their previews side by side in light and dark; pairing works end to end against a local test folder prepared with the .NET vectors (QR path and short-code path); meeting/appointment/due reminders are scheduled locally at their time from the synced settings (item 56) and survive a reboot; the home screen shows organisation, office, employee with photo, attention cards, pending expenses, last sync, today list and the FAB',
    spec: `Build the app shell on top of android-core: M01 welcome, M02 QR scan (CameraX + ZXing core decoding; camera permission flow with the Arabic rationale), M03 short code (six digits; reads pairing-<code>.wakeel-phone from the chosen folder), folder choice through the Storage Access Framework once with a persisted permission (the screen explains in plain Arabic to choose the «Wakeel» folder), M04 pairing success, M24 re-pairing after data loss, M05 lock and M31 biometric setup (BiometricPrompt with the app PIN as the alternative), M06 home (MOrgHeader, employee header with photo, attention cards, expenses awaiting confirmation, last sync row, today's list, extended FAB opening the quick-capture sheet placeholder), the bottom navigation with five items and badges, M25 settings (account, photo, biometric lock, appearance light/dark/system, notifications, sync, about), M26 profile photo with square crop, M27 notifications list by type and time with local scheduling of meeting/appointment/due reminders (AlarmManager exact with the permission flow, WorkManager fallback; rescheduled after boot and after each sync; item 56 default and per-meeting override come from the synced settings), M30 the standard states (empty, no USB connection, error, not paired). ViewModels on the core repositories; navigation-compose; every screen from the kit composables. Tests: Roborazzi screenshots of every screen and state light and dark; ViewModel unit tests (pairing flows incl. expired token and wrong code, lock attempts, reminder scheduling computation, badge counts); a Robolectric test of the SAF permission persistence wrapper.`,
  },
  {
    key: 'android-capture', title: 'B7-2c (أندرويد: التقاط المستندات وOCR والإدخال السريع والمصروفات والملاحظات الصوتية — M09–M13, M28, M32)', model: 'opus',
    paths: 'android/',
    build: 'cd android && gradlew.bat --max-workers=2 :core:testDebugUnitTest :ocr:testDebugUnitTest :camera:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug',
    security: 'captured images, OCR text and recordings live only in app-private encrypted storage until they are packed into a from-phone container; nothing is written to shared storage in clear; amounts are integer agorot; the microphone and camera permissions are requested in context with Arabic rationale',
    extraCheck: 'M09–M13, M28 and M32 match their previews side by side in light and dark; edge detection, crop, deskew and contrast run on a bundled test image in a JVM or instrumented-free test where possible; OCR returns Arabic and English words from a generated test image (tessdata ara+eng bundled as assets from tools/tessdata — tessdata_fast size if the APK would exceed 150 MB, reported in notes); an expense records the date and time automatically and shows the «يُؤكَّد على الحاسوب» note; every capture lands in the outbox with the state «بانتظار المزامنة التالية»',
    spec: `Build the capture features: M09 document capture (CameraX preview, automatic edge detection with OpenCV, manual corner adjustment, crop, deskew, contrast enhancement, multiple pages), M10 OCR review (Tesseract4Android with ara+eng, the text editable, language choice, «إرسال عند المزامنة التالية»), M11 quick-capture sheet (note, voice, expense, document photo, task, report note), M12 expense entry in shekels with automatic date/time, category, receipt photo, note and the confirmation-on-the-computer notice (item 50), M13 phone expense states with the rejection reason and correction before sync, M28 voice note (record, waveform, link to a record; on-device transcription only when an offline SpeechRecognizer is available, otherwise it stays audio), M32 notes for the monthly report (item 53 a). Each capture writes its rows and files and an outbox entry in one transaction. Tests: Roborazzi screenshots of every screen and state light and dark; unit tests for the image pipeline on a bundled sample (corner ordering, perspective transform size, deskew angle), OCR on a generated image (skipped with a clear message when the native library cannot load on the JVM — then covered by an instrumented test source set that compiles), expense validation and agorot arithmetic, outbox entries per capture type.`,
  },
  {
    key: 'android-records', title: 'B7-2d (أندرويد: المراسلات والمهام والاجتماعات والتقويم والقرارات والجهات والبحث والملفات المثبّتة — M07, M08, M14–M21, M29)', model: 'opus',
    paths: 'android/',
    build: 'cd android && gradlew.bat --max-workers=2 :core:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug',
    security: 'only synced summaries are shown — «للمستلم فقط» items never reach the phone unless it belongs to the recipient; pinned files are decrypted to memory or a private cache wiped on lock; QR scanning of an entity resolves only tokens known to the local database',
    extraCheck: 'M07, M08, M14–M21 and M29 match their previews side by side in light and dark; swipe-to-complete on tasks writes an outbox entry; the Meeting Brief quick buttons (+ قرار، + مهمة، + التزام، + ملاحظة، + متابعة) create rows linked to the meeting; the calendar shows dots per type and the day list and adds an appointment; local search finds OCR snippets; pinned files list sizes and removal',
    spec: `Build the records features on the synced data: M07 correspondence (tabs, local search, list), M08 correspondence details (pinned files usable without a connection, referrals, follow-up, «تصوير مستند» linking a capture), M14 tasks with swipe to complete and M15 task details, M16 meetings and M17 Meeting Brief with the quick buttons, M18 calendar with dots, the day list and add-appointment, M19 decisions and commitments, M20 parties with search, list and QR scan of an entity (asset/party/correspondence opens its details), M21 local search over the synced data with OCR snippets (normalised Arabic matching: alef forms, ta marbuta, alef maqsura, tatweel, diacritics), M29 pinned files with size and removal. Every edit writes an outbox entry. Tests: Roborazzi screenshots of every screen and state light and dark; ViewModel tests (tabs and counts, swipe completion, brief quick actions, calendar month building with the configured first day, search normalisation and snippets, pinned file removal).`,
  },
  {
    key: 'android-sync', title: 'B7-2e (أندرويد: شاشات المزامنة والسيناريو الكامل وتجميع APK — M22, M23 + القبول)', model: 'opus',
    paths: 'android/, tests/Wakeel.Crypto.Tests (one new test class AndroidRoundTripTests reading android/core/build/vectors-out/ when present, skipped with a message otherwise)',
    build: 'cd android && gradlew.bat --max-workers=2 test :app:assembleDebug :app:assembleRelease',
    security: 'the release build is signed with a release key generated into android/keystore/ (git-ignored, never committed; passwords in android/keystore/keystore.properties, git-ignored); R8 keeps only what reflection needs; no INTERNET permission in the merged manifest (assert it in a test that parses the merged manifest); logs are stripped of data in release',
    extraCheck: 'M22 and M23 match their previews side by side; the end-to-end scenario on a local test folder passes: pair by QR and by short code → receive to-phone packages → capture a document with OCR and an expense → from-phone package written → acknowledgement → an interrupted transfer loses nothing and resumes (item 35); the debug and release APKs build; the app was installed and launched once on the emulator (AVD listed by emulator -list-avds; create «Medium Phone API 29» with avdmanager if none) with screenshots of M01, M06 and M22 taken through adb and viewed',
    spec: `Finish the app: M22 USB sync screen (connection state, what will be sent with counts, step progress, outbox), M23 USB interruption (what completed and what remains, continue on reconnect), detection of new files in the Wakeel folder on app open and on USB attach (ACTION_POWER_CONNECTED / UsbManager state as a hint only — the folder scan is the truth), atomic apply per package, acknowledgement. Then the acceptance of docs/build/packages/B7-android.md for B7-2: the end-to-end scenario as a JVM test over a local folder with containers produced by the .NET vectors and by the Android writer; Compose/Roborazzi tests for the main screens; signing config reading android/keystore/keystore.properties when present (generate the release keystore with keytool into android/keystore/ and document the command in android/README.md — Arabic-free developer note); assembleRelease produces the signed APK at android/app/build/outputs/apk/release/ for the installer of B8. This package alone may start the emulator: install the debug APK, launch, take three screenshots with adb exec-out screencap (scaled to 412 px wide before viewing), then shut the emulator down. Report APK sizes in notes.`,
  },
]

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed (directories are enough for generated trees), tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, library versions chosen, the per-screen side-by-side comparison table).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read the code under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output; stop the Gradle daemon afterwards). VISUAL FIDELITY CHECK: open every Roborazzi screenshot of the package's screens (light and dark) together with its preview and report every visible deviation as a finding (high: a hand-rolled component where a kit composable exists or should exist; medium: layout, alignment, overlap, clipping, missing control/chip/state, wrong RTL order; low: spacing nuance). Also check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies; tests that skip must say why), (6) Arabic-only user text without technical terms, bidi-safe mixed text, no INTERNET permission. From round 2 on, first confirm that each finding of the previous round is really closed, then look for regressions; do not reopen supervisor rulings. Do NOT modify any file (you may not write the progress file either). Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${COMMON}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green, regenerate the screenshots you changed and compare again. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
const ORDER = ['build', 'review1', 'fix', 'verify', 'fix2', 'verify2', 'fix3', 'verify3']
const PHASE = { build: 'Build', review1: 'Review', fix: 'Fix', verify: 'Verify', fix2: 'Fix2', verify2: 'Verify2', fix3: 'Fix3', verify3: 'Verify3' }
function isReview(st) { return st.startsWith('review') || st.startsWith('verify') }
// An agent that dies on a connection error returns null: start it again (the restart protocol makes the
// new attempt continue from the working tree and the progress file). Three attempts, then give up.
async function tryAgent(prompt, opts) {
  for (let n = 1; n <= 3; n++) {
    const r = await agent(prompt, n === 1 ? opts : { ...opts, label: `${opts.label}#${n}` })
    if (r) return r
    log(`${opts.label}: attempt ${n} returned nothing${n < 3 ? ' — starting it again' : ' — giving up'}`)
  }
  return null
}
// args = { skip: [keys already accepted], resume: { '<key>': { stage, file, state, rulings, extraPaths } } }
//   stage: build|review1|fix|verify|fix2|verify2|fix3|verify3 — the stage to run first;
//   file: archived report of the stage before it (handed over as {see: file}).
async function chainFrom(p0, r0) {
  const r = r0 || {}
  const p = { ...p0,
    paths: p0.paths + (r.extraPaths ? ', ' + r.extraPaths : ''),
    spec: p0.spec + (r.state ? '\n\nSTATE OF THIS PACKAGE: ' + r.state : '') + (r.rulings ? '\n\nSUPERVISOR RULINGS FOR THIS ROUND (final, do not reopen): ' + r.rulings : '') }
  const s = { key: p.key, accepted: false }
  let prev = r.file ? { see: r.file } : null
  for (let i = r.stage ? ORDER.indexOf(r.stage) : 0; i < ORDER.length; i++) {
    const st = ORDER[i]
    if (isReview(st)) {
      const round = ORDER.slice(0, i + 1).filter(isReview).length
      const rev = await tryAgent(reviewPrompt(p, prev, round), { label: `${st}:${p.key}`, phase: PHASE[st], schema: REVIEW_SCHEMA, model: 'opus', effort: round === 1 ? 'high' : 'medium' })
      s[st] = rev
      if (!rev) return { ...s, failed: st }
      if (rev.verdict === 'accept') return { ...s, accepted: true }
      prev = rev
    } else {
      const prompt = st === 'build' ? builderPrompt(p) : fixPrompt(p, prev)
      const out = await tryAgent(prompt, { label: `${st}:${p.key}`, phase: PHASE[st], schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s[st] = out
      if (!out) return { ...s, failed: st }
      prev = out
    }
  }
  return s
}
const RESUME = (args && args.resume) || {}
const SKIP = (args && args.skip) || []
log('Android: core → shell → capture → records → sync and the end-to-end scenario')
const results = []
for (const p of PACKAGES) {
  if (SKIP.includes(p.key)) { results.push({ key: p.key, skipped: true }); continue }
  log(`Android: ${p.key}${RESUME[p.key] ? ' (resuming at ' + RESUME[p.key].stage + ')' : ''}`)
  const s = await chainFrom(p, RESUME[p.key])
  results.push(s)
  if (!s.accepted) { log(`Android: ${p.key} not accepted${s.failed ? ' (stage ' + s.failed + ' died)' : ''} — stopping the sequence for the supervisor`); break }
}
return results
