export const meta = {
  name: 'alwakeel-b1d-b2c-resume',
  description: 'Third continuation after a stalled agent: admin-2 finalised and reviewed then admin-3; B2 screens resumed from the partial work then the walkthrough/E2E; and the shared WTree component (design-consistency pass, stage 1) — three chains, at most three agents at a time; every package reviewed, fixed and re-verified by Opus',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a component, a screen, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (1–2 MB each; they overload the connection). View the small JPEG previews under design/exports/preview/light/<group>/<name>.jpg and design/exports/preview/dark/<group>/<name>.jpg (same file names as the PNGs, .jpg extension; groups A, W, M, I), only the screens of your own package, each at most once per comparison, right before you build or check that screen. (5) Screenshots you take must be JPEG or PNG at most 1366 px wide and viewed once.

DESIGN CONSISTENCY (owner's rule, 2026-09-16 — «انتبه للتصميم: لا يوجد تناسق وتنظيم في المكونات»): every screen is composed from Wakeel.Design components (src/Wakeel.Design/Components). Hand-rolled lists, trees, tables, tabs, chips, forms, cards or dialogs inside a page are forbidden; page CSS may only arrange design components on the page grid and set page-specific spacing from the tokens. If a component is missing, add it to Wakeel.Design first (component + css + gallery entry + bUnit test), then use it. The screen is done only when its host screenshot, put beside the preview, shows the same anatomy: same regions and columns, same controls in the same places, same chips and states, no overlap, no clipping, no staircase indentation, aligned rows, consistent spacing. A builder writes the side-by-side comparison of every screen in its notes (deviation → fixed / accepted with reason). A reviewer opens the builder's screenshot and the preview together and reports every visible deviation as a finding: high for a hand-rolled component, medium for a layout, alignment, overlap or missing-control deviation, low for a spacing nuance.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset/clean. Other agents edit other projects in the same working tree concurrently: build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. A package that touches a WPF host or Blazor pages is not done until the host actually runs: launch the built exe (Wakeel.Desktop: src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe; Wakeel.Admin: src/Wakeel.Admin/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Admin.exe) with --remote-debugging-port=<port> (9333 for Wakeel.Desktop in the B2 packages, 9335 for Wakeel.Desktop in the design-tree package, 9334 for Wakeel.Admin), --window-size=1366x768, --data-folder=<a temporary folder> and --start-url=/route where useful, confirm http://127.0.0.1:<port>/json/version answers, screenshot it over CDP (Page.captureScreenshot), LOOK at the screenshot, read the host log in <data-folder>/logs for errors, then kill the process immediately — a host left running is a defect. All user-facing text is Arabic with no technical terms or error codes (item 15), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere. Identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or UI state longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON_ADMIN = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B1-admin-firstrun.md (the B1 specification, section B1-A), docs/build/ARCHITECTURE.md (§2–§5, §9, §10, §12 and the .wakeel-setup format paragraph in §4; the org-root certificate decision in §3), docs/build/DATA-MODEL.md (§1, §13), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items referenced by the spec), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Crypto (keys, wraps, certificates, containers, Setup/SetupPackageWriter+Reader+PackagePassword, pairing), src/Wakeel.Core (WakeelDb, DbSession, SchemaMigrator, services), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast services), src/Wakeel.Admin.UI and src/Wakeel.Admin (what admin-1 built — see docs/build/progress/admin-1.md — and what admin-2 built — see docs/build/progress/admin-2.md).\n\n${PROTOCOL}`

const COMMON_B2 = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B2-daily-shell.md (the B2 specification), docs/build/ARCHITECTURE.md (§9–§12), docs/build/DATA-MODEL.md (§1, §5), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items 15, 20, 21, 23, 26, 32, 41, 50, 52, 56), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Core (WakeelDb, DbSession, services, ClockCheckService, ArabicText, InstallationService, and the ACCEPTED B2 services: AttentionService, BadgeService, NotificationService, ReminderScheduler, ClockGuard, HealthService, QuickCaptureService, BackgroundPass, CoreAr — see docs/build/progress/b2-services.md), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast), src/Wakeel.UI (shell, first-run/login/lock/recovery from B1 under Pages/FirstRun, Pages/Account and Services/Account; Services/Shell from the b2-screens attempt), src/Wakeel.Desktop (host; flags --remote-debugging-port, --window-size, --start-url, --data-folder; boots into /first-run on an empty data folder and /login otherwise; Windows probes under Services/), tests/Wakeel.Walkthrough.Tests (FirstRunWorld activates a temporary installation) and tests/Wakeel.E2E (Playwright-over-CDP harness). Arabic strings only in src/Wakeel.Design/Text/Ar.*.cs partial files (UI) and src/Wakeel.Core/Services/CoreAr.cs (Core layer, ratified in ARCHITECTURE §12).\n\n${PROTOCOL}`

const COMMON_DESIGN = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/design/DESIGN-GUIDE.md (tokens, components, states), docs/design/SCREENS.md, docs/build/ARCHITECTURE.md §10 and §12, docs/AGREEMENT.md items 15 and 55. The design system is src/Wakeel.Design (components, tokens/css/js, Bidi, Ar strings); the gallery page src/Wakeel.UI/Pages/Gallery.razor shows every component and its states (route /gallery of Wakeel.Desktop, reachable without an installation). Another package (b2-screens) edits src/Wakeel.Design/Components/WButton.razor, src/Wakeel.Design/Text/Ar.Shell.cs and src/Wakeel.UI pages at the same time — do not touch those files.\n\n${PROTOCOL}`

const ADMIN_COMMON = `The admin tool «مدير نظام الوكيل» is built in three sequential sub-packages (admin-1: host, shell, storage, account, A01–A03 — ACCEPTED; admin-2: organisation, structure, offices, devices, keys, A04–A07 — BUILT, awaiting review; admin-3: export, maintenance, distribution, audit, dialogs, A08–A12). Shared conventions: src/Wakeel.Admin (WPF + BlazorWebView host: BlazorWebViewInitializing user-data folder, Serilog file log, --remote-debugging-port, --data-folder, --start-url, --window-size, titled «مدير نظام الوكيل», 1366x768, DPAPI protector, file dialogs, print-to-PDF, ProgramData folder C:\\ProgramData\\WakeelAdmin\\ with admin.db, keys, exports, staging, logs). src/Wakeel.Admin.UI (Blazor pages/components/services) references Wakeel.Design, Wakeel.Core and Wakeel.Crypto; admin.db is SQLCipher with the DATA-MODEL §13 schema as its own migration set (Migrations/). Arabic strings live in src/Wakeel.Admin.UI/Text/ as partial static class AdminAr split by area (add AdminAr.Export.cs, AdminAr.Maintenance.cs, … for your own areas). Services are registered per area through extension methods called from AddWakeelAdmin() (src/Wakeel.Admin.UI/ServiceCollectionExtensions.cs); routes are declared in src/Wakeel.Admin.UI/AdminRoutes.cs — add your routes to its Built set when the page exists. Shell: dark top bar + top tabs, no sidebar. Roles are manager|secretary|custodian (SetupRoles). Tests in tests/Wakeel.Admin.Tests use AdminTestContext (temp folder, TestClock, cheap Argon2). KNOWN, DO NOT REDO: the A05 structure tree is hand-rolled and will be rebuilt on the shared WTree component by a later design-consistency package — an admin-2 reviewer records it as one finding and does not block acceptance on it alone; admin-3 must not add any hand-rolled list/tree/table. SUPERVISOR RULINGS (final, do not reopen): where the A previews disagree with the specification, the specification wins — one 21-character recovery code plus QR, no «تذكر هذا الجهاز» option, lock-out 30 s doubling; the previews will be corrected in the design later.`

const PACKAGES = {
  'admin-2': {
    key: 'admin-2', title: 'B1-A2 (مدير النظام: المؤسسة والهيكلية والمكاتب والأجهزة والمفاتيح A04–A07)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'device certificate issuance with a key seed that is stored only until the first export, office key issue/rotation, revocation list signing with the org root, logo size/type validation, letter/report template validation (docx only, placeholders detected by reading the XML — no macro execution), no secrets in admin.db clear text, logs or UI state, and the six admin-1 low findings closed',
    extraCheck: 'A04–A07 match their previews (light and dark) apart from the known A05 tree, structure rules (4 fixed levels, no duplicates per level, no delete with active devices), device numbering 1–9 unique per office, the letter template upload lists detected/unknown @ placeholders and shows an A4 and A5 PDF preview, the A03 dashboard reflects the real tables, and the Wakeel.Admin host was launched and rendered',
    spec: `${ADMIN_COMMON}\nSTATE: docs/build/progress/admin-2.md records steps 1–8 as complete (services, A04–A07 pages, the six admin-1 lows, service tests, 21 bUnit screen tests; 155 tests green). The previous attempt was cut off during its final host run. THIS ATTEMPT FINALISES: run the build/test command, launch Wakeel.Admin.exe (port 9334, temp --data-folder), drive A01 → A03 → A04/A05/A06/A07, screenshot each in light and dark, compare each with its preview and write the side-by-side table in notes (the A05 tree is a known deviation handled later — list it, do not rebuild it), fix anything that is actually broken in the host, kill the host, append the final step to the progress file and return the report. Original scope for reference: (a) A04 «المؤسسة والهوية»: org name, logo upload ≤ 2 MB with preview and square crop, cycle start day 1–28 (item 52), numbering format YYYYMMDD/DESSS with a danger warning (item 5), optional report template (item 53), official letter template per item 57 with the nine placeholders, unknown ones flagged, A4/A5 preview, built-in default when none uploaded; (b) A05 «الهيكلية»: four fixed levels, add/edit/move/disable, head title and name, office inventory code (item 45), no duplicate names per level, no delete with active devices; (c) A06 «المكاتب والأجهزة»: devices 1–9 per office, roles, employee name/number, sync scope, phones read-only; (d) A07 «الحسابات والمفاتيح»: device account creation with seed kept until first export, custodian activation, account recovery, office key issue/rotate with warning, revoke (item 24) → signed revocation list; (e) routes registered, A03 counts live; (f) tests as listed in the progress file; (g) the six admin-1 lows.`,
  },
  'admin-3': {
    key: 'admin-3', title: 'B1-A3 (مدير النظام: التصدير والصيانة والتوزيع والسجل والحوارات A08–A12)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'setup package password shown once and never logged, staging directory under the product folder (never %TEMP%) wiped after export, key seed removed from admin.db after the first export, AdminWrap use when opening a Wakeel backup or ProgramData, audit log has no secrets, CSV export escapes formulas',
    extraCheck: 'A08–A12 match their previews (light and dark), the exported .wakeel-setup opens with Wakeel.Crypto SetupPackageReader with every check Ok (including letter-template.docx when set), distribution regenerates files for every affected office, the whole admin tool A01–A12 works end to end in the running host (launched and rendered), no hand-rolled component, and nothing from admin-1/admin-2 regressed',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-3) builds on admin-1 and admin-2 (read docs/build/progress/admin-1.md and admin-2.md and their code before extending): (a) SetupExportService + A08 «تصدير ملف الإعداد»: 4-step wizard (WStepper) — office and device ← content (structure, logo, office key, device certificate and its seed, cycle day, numbering format, report template, letter template as letter-template.docx via SetupContent.includes.letterTemplate, guide PDF, revocation list) ← package password (PackagePassword.New(), 16 random characters shown once with a copy button and a warning) ← save <office>-<device>-<date>.wakeel-setup written by Wakeel.Crypto SetupPackageWriter with a staging directory under ProgramData\\WakeelAdmin\\staging; record the export in setup_exports and delete the device key seed from admin.db after the first export; provisional guide: if docs/guide/دليل-الوكيل.pdf is absent generate a one-page placeholder PDF at runtime into the exports folder (never write under docs/); (b) MaintenanceService + A09 «الصيانة»: open a Wakeel copy (ProgramData folder or a .wakeel-backup file) with the org key (AdminWrap) for viewing and verification, open a .wakeel-msg, recover a user account (new password wrap delivered through a recovery setup file), every dangerous action behind a red button and confirmation, with a log; (c) DistributionService + A10 «توزيع التحديثات»: list of undistributed structure/office changes (AdminPendingChanges from admin-2), regenerate updated setup files for all affected offices in one batch (each with its own password); (d) A11 «سجل العمليات»: WTable who/what/when without secrets, local search, CSV export; (e) A12: every remaining state and dialog (device revocation danger confirm, numbering-format change warning, corrupt file / wrong password error, export success, logo too large); register the routes in AdminRoutes.Built and enable the A03 header actions; (f) tests: export produces a file that SetupPackageReader opens with all checks Ok (with and without the letter template), seed removed after the first export, distribution regenerates files for affected offices only, maintenance opens a backup produced by Wakeel.Crypto test helpers, audit CSV has no secrets, A12 dialogs render. Final pass: launch Wakeel.Admin.exe (port 9334, temp --data-folder), walk A01→A12 once, screenshot A08/A09/A10/A11 and two A12 states light and dark, compare with the previews side by side (table in notes), kill it.`,
  },
  screens: {
    key: 'b2-screens', title: 'B2-SCREENS (W08–W12, W91, W92, W94)', model: 'opus',
    paths: 'src/Wakeel.UI, src/Wakeel.Design (Text/Ar.Shell.cs; Components/WButton.razor tooltip fix; new components only if the spec needs them, e.g. WNotificationPanel, WBanner additions, WHealthCard), tests/Wakeel.UI.Tests, src/Wakeel.Desktop (only: opening ms-settings:dateandtime, saving the health report file, Ctrl+N global shortcut wiring, DI lines in App.xaml.cs)',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'no MarkupString with user data, no technical text in any state card or banner, health report saved only where the user chooses, background passes dispatched to the UI through ComponentBase.InvokeAsync and stopped on sign-out and lock',
    extraCheck: 'each of the eight screens matches its preview (light and dark) in anatomy, texts and states; empty/loaded/error states exist; no hand-rolled component; every icon-only button has a tooltip; ClockGuard and ReminderScheduler actually run in the host (a seeded meeting reminder appears in the bell); the host was launched on each route with an activated temporary installation and screenshots viewed and compared side by side',
    spec: `STATE: docs/build/progress/b2-screens.md records the done part of a previous attempt (ShellServices scope per open session, PhoneExpenseReview with confirm/reject, DI registration, Ar.Shell.cs with all texts, WButton tooltip for every variant, Desktop build green) — read it and the files, keep them, continue with the remaining steps: shell wiring in MainLayout (ClockGuard/ReminderScheduler start on sign-in/unlock and stop on lock/sign-out with ComponentBase.InvokeAsync as the BackgroundPassDispatcher, badge subscription, bell, Ctrl+N), then the screens, tests, carry-over findings and the host run. Build section «الشاشات (Wakeel.UI/Pages)» of docs/build/packages/B2-daily-shell.md completely on top of the accepted B2 services: W08 attention center with live data (replace the B0 placeholder; KPI cards open W09 on the right tab; monthly-report banner from ReadinessService if it exists else 0; «يحتاج إجراءً اليوم» mixed rows with type chip, subject/number, party, assignee, semantic due colour, local search and badge tabs; phone expenses pending confirmation with employee name and direct confirm/reject (item 50); side column: today's meetings, last sync, backup state; «إدخال سريع» and «تحديث» buttons), W09 overdue/near/stale/pending tabs with badges, local search, WTable and «معالجة» opening the record, W10 notification panel (380 px dropdown from the bell, tabs all/unread N from the real counts, items with type icon and relative time, click opens the record, mark all read, settings link to W86), W11 clock banner (WBannerClock above content with the exact texts, «تصحيح الساعة» opens ms-settings:dateandtime through the desktop host, «تجاهل مؤقتًا» with tooltip), W12 health center (WHealthCard grid from HealthService, «فحص شامل الآن», «تصدير تقرير الصحة» saving an Arabic text file, filter tabs, local search), W91 standard states reference page (every WStateCard with final Ar.States texts), W92 standard dialogs reference page, W94 quick capture dialog on Ctrl+N (sections task/note/report-note/expense/appointment, validation, immediate save, toast with «تراجع»). Every screen from Wakeel.Design components only. ALSO close the three low findings of docs/build/reviews/B2/verify-b2-carryover.json (W07 confirm label sheet-aware with Ar.Common.Close; two bUnit tests for the RecoveredNotSignedIn paths; the E2E note needs no change). bUnit tests for the eight screens and their states (empty/loaded/error) with mixed Arabic/English content; navigation from KPI cards/rows/notifications asserted. Host: activate a temporary data folder with tests/Wakeel.Walkthrough.Tests' FirstRunWorld (or a small test helper), launch Wakeel.Desktop.exe on it (port 9333) with --start-url per route, sign in over CDP, screenshot light and dark, compare each with its preview side by side (table in notes), kill. The W08 banner in the preview says «دورة أكتوبر»; the correct text follows the current financial cycle.`,
  },
  walkthrough: {
    key: 'b2-walkthrough', title: 'B2-WALKTHROUGH (service scenario + E2E screenshots)', model: 'sonnet',
    paths: 'tests/Wakeel.Walkthrough.Tests, tests/Wakeel.E2E',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Walkthrough.Tests --no-dependencies && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet build tests/Wakeel.E2E --no-dependencies && dotnet test tests/Wakeel.E2E --no-build',
    security: 'tests use temporary folders only and never touch C:\\ProgramData\\Wakeel of the real installation',
    extraCheck: 'the walkthrough asserts real counts and timings through the public services, and the E2E screenshots of W08–W12 are produced against the design PNGs with recorded diff percentages and per-screen upper bounds',
    spec: `Build section «القبول» of docs/build/packages/B2-daily-shell.md: (1) tests/Wakeel.Walkthrough.Tests scenario on a temporary activated installation (reuse FirstRunWorld): create overdue, near-due and stale records plus pending phone expenses → AttentionService/BadgeService return the exact counts → a meeting with a reminder fires the notification in the bell at the right minute (drive the scheduler clock; check the item-56 default and a per-meeting override) → the clock guard blocks numbering when the clock is set back and releases after correction → quick capture saves and undo removes. (2) tests/Wakeel.E2E: extend the harness with screenshot tests for W08, W09, W10 (panel open), W11 (banner shown via a test hook or seeded state), W12, W91, W92, W94 at 1366x768 light and dark, comparing programmatically with design/exports/{light,dark}/W/*.png (the test code may read the PNGs; you yourself look only at the small previews and at your own screenshots) and recording diff percentages in the test output and tests/Wakeel.E2E/artifacts/ (git-ignored); assert an upper bound per screen; the host honours --data-folder — point it at a temporary folder activated and seeded through the walkthrough helpers and sign in over CDP (port 9333). Report the diff percentages per screen in notes.`,
  },
  tree: {
    key: 'design-tree', title: 'DESIGN-TREE (shared WTree component in Wakeel.Design)', model: 'opus',
    paths: 'src/Wakeel.Design/Components (new WTree*.razor/.css and, only if needed, additions to Enums.cs), src/Wakeel.Design/Text/Ar.Common.cs (additions only), src/Wakeel.UI/Pages/Gallery.razor and Gallery.razor.css (a new WTree section only), tests/Wakeel.UI.Tests/Components/WTreeTests.cs',
    build: 'dotnet build src/Wakeel.Design --no-dependencies && dotnet build src/Wakeel.UI --no-dependencies && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build --filter "FullyQualifiedName~WTree" && dotnet build src/Wakeel.Desktop',
    security: 'no MarkupString with caller data; search highlighting built from text nodes, never from concatenated HTML',
    extraCheck: 'the gallery section renders a four-level sample tree in light and dark exactly with the row anatomy of the A05 preview (design/exports/preview/light/A/A05 — الهيكلية.jpg): handle, caret, icon, name, level chip, trailing head name, states; keyboard navigation works in the running host',
    spec: `Build a generic tree component for the design system, modelled on the tree pane of the A05 preview and reusable by the organisation structure (A05), unit pickers, document folders and any later hierarchy. API: WTree<TItem> with Items (roots), ChildrenOf, KeyOf, a Row render fragment or the built-in row anatomy — trailing drag handle (optional, shown when Movable), caret for branches (hidden for leaves, rotates when open), leading icon (per node), name (Bidi-wrapped), level chip (WChip variant per level), trailing meta text (e.g. the head's name), optional trailing WBadge/state chip (e.g. «بلا رئيس معيّن» in the warning colour, out-of-service dimmed), Selected key + SelectedChanged, Expanded keys + ExpandAll()/CollapseAll(), Search text with match highlighting and a «لا عنصر بهذا الاسم» state from Ar strings, RTL indentation by depth through one CSS custom property (no inline pixel maths), hover/selected/focus states from the tokens, full-width rows aligned on one grid (no staircase), ARIA tree/treeitem/group roles, keyboard: ArrowUp/Down move, ArrowLeft/Right open/close in RTL, Enter selects, Home/End. Also a small WTreeToolbar or documented pattern for the header row of the preview: title + count badge, «توسيع الكل» / «طيّ الكل» text buttons, search input with the Ctrl+F kbd hint. Add the gallery section («الشجرة») with a four-level sample (هيئة → دائرة → قسم → وحدة) showing every state, in light and dark. bUnit tests: rendering roots and nested groups, expand/collapse and ExpandAll/CollapseAll, selection callback, search filter keeps ancestors of matches and highlights the match, empty-search state text, keyboard moves selection, mixed Arabic/English names isolated, level chip per depth. Launch Wakeel.Desktop with --start-url=/gallery on an empty temp --data-folder (port 9335), screenshot the tree section light and dark, compare with the A05 preview's tree pane side by side, kill.`,
  },
}

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p, common) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${common}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, measurements, the per-screen side-by-side comparison table).`
}
function reviewPrompt(p, common, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${common}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output). VISUAL FIDELITY CHECK: launch the host yourself, screenshot every screen the package covers in light and dark, open each screenshot together with its preview, and report every visible deviation as a finding (high: a hand-rolled component where a Wakeel.Design one exists or should exist; medium: layout, alignment, overlap, clipping, missing control/column/chip/state; low: spacing nuance). Also check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. Do NOT modify any file (you may not write the progress file either). Kill the host when done. Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, common, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${common}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green, re-screenshot what you changed and compare again. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
function accepted(s) { const last = s && (s.verify2 || s.verify || s.review1); return !!(last && last.verdict === 'accept') }
async function chain(p, common) {
  const build = await agent(builderPrompt(p, common), { label: `build:${p.key}`, phase: 'Build', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
  if (!build) return { key: p.key, failed: 'build' }
  const review1 = await agent(reviewPrompt(p, common, build, 1), { label: `review1:${p.key}`, phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })
  const s = { key: p.key, build, review1 }
  if (review1 && review1.verdict === 'fix') {
    s.fix = await agent(fixPrompt(p, common, review1), { label: `fix:${p.key}`, phase: 'Fix', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
    s.verify = await agent(reviewPrompt(p, common, s.fix, 2), { label: `verify:${p.key}`, phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    if (s.verify && s.verify.verdict === 'fix') {
      s.fix2 = await agent(fixPrompt(p, common, s.verify), { label: `fix2:${p.key}`, phase: 'Fix2', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s.verify2 = await agent(reviewPrompt(p, common, s.fix2, 3), { label: `verify2:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    }
  }
  return s
}
async function sequence(name, keys, common) {
  const out = []
  for (const key of keys) {
    log(`${name}: starting ${key}`)
    const r = await chain(PACKAGES[key], common)
    out.push(r)
    if (!accepted(r)) { log(`${name}: ${key} not accepted — stopping the sequence for the supervisor`); break }
  }
  return out
}
log('admin (admin-2 finalise → admin-3) ∥ B2 (screens resumed → walkthrough) ∥ design-tree')
const results = await parallel([
  () => sequence('admin tool', ['admin-2', 'admin-3'], COMMON_ADMIN),
  () => sequence('B2', ['screens', 'walkthrough'], COMMON_B2),
  () => chain(PACKAGES.tree, COMMON_DESIGN),
])
return { admin: results[0], b2: results[1], tree: results[2] }
