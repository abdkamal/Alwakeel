export const meta = {
  name: 'alwakeel-b1-resume',
  description: 'B1 resume after the stalled run: design-polish final verify, first run (W02–W07) continued from the partial work on disk, and the admin tool built as three sequential sub-packages (A01–A03, A04–A07, A08–A12); every package reviewed, fixed and re-verified by Opus; restart-safe prompts with progress files and small design previews',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }],
}
const REPO = 'D:/AI/Administration2'
const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B1-admin-firstrun.md (the B1 specification), docs/build/ARCHITECTURE.md (§2–§5, §9, §10, §12 and the .wakeel-setup format paragraph in §4; the org-root certificate decision in §3), docs/build/DATA-MODEL.md (§1, §13), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items referenced by the spec), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Crypto (keys, wraps, certificates, containers, Setup/SetupPackageWriter+Reader+PackagePassword, pairing), src/Wakeel.Core (WakeelDb, DbSession, SchemaMigrator, services), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast services, AddWakeelDesign()), src/Wakeel.UI (app shell), src/Wakeel.Desktop (host; flags --remote-debugging-port, --window-size, --start-url, --data-folder), tests/Wakeel.E2E (Playwright-over-CDP harness).

RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a service, a screen, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (1–2 MB each; they overload the connection). View the small JPEG previews under design/exports/preview/light/<group>/<name>.jpg and design/exports/preview/dark/<group>/<name>.jpg (same file names as the PNGs, .jpg extension; groups A, W, M, I), only the screens of your own package, each at most once, right before you build or check that screen. (5) Screenshots you take must be JPEG or PNG at most 1366 px wide and viewed once.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset/clean. Other agents edit other projects in the same working tree concurrently (their uncommitted files are normal — leave them alone): build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. A package that touches a WPF host or Blazor pages is not done until the host actually runs: launch the built exe (Wakeel.Desktop: src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe; Wakeel.Admin: src/Wakeel.Admin/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Admin.exe) with --remote-debugging-port=9333 (Wakeel.Desktop also accepts --start-url=/route, --window-size=1366x768 and --data-folder=<path>), confirm http://127.0.0.1:9333/json/version answers, screenshot it (Playwright over CDP, or PowerShell CopyFromScreen), LOOK at the screenshot, read the host log for errors, then kill the process immediately — a build that compiles but does not render is a failure, and a host left running is a defect. All user-facing text is Arabic with no technical terms or error codes (item 15), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere. Identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or UI state longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const ADMIN_COMMON = `The admin tool «مدير نظام الوكيل» is built in three sequential sub-packages (admin-1: host, shell, storage, account, A01–A03; admin-2: organisation, structure, offices, devices, keys, A04–A07; admin-3: export, maintenance, distribution, audit, dialogs, A08–A12). Shared conventions for all three: src/Wakeel.Admin (WPF + BlazorWebView host modelled on Wakeel.Desktop — same BlazorWebViewInitializing user-data-folder pattern, Serilog file log, --remote-debugging-port flag, titled «مدير نظام الوكيل», 1366x768, DPAPI IPlatformProtector, file open/save dialogs, print-to-PDF through CoreWebView2.PrintToPdfAsync, ProgramData folder C:\\ProgramData\\WakeelAdmin\\ with admin.db, keys, exports, staging, logs; honour --data-folder=<path> so tests and screenshots can use a temporary folder). src/Wakeel.Admin.UI (Blazor pages/components/services) references Wakeel.Design (components + Ar), Wakeel.Core (reuse DbConnectionFactory/SchemaMigrator/SQLCipher helpers; the admin schema of DATA-MODEL §13 is its own migration set inside Wakeel.Admin.UI) and Wakeel.Crypto. Admin-tool Arabic strings live in src/Wakeel.Admin.UI/Text/ as partial static class AdminAr split by area (AdminAr.Common.cs, AdminAr.Account.cs, AdminAr.Structure.cs, AdminAr.Export.cs, …) so sub-packages never edit each other's files; each sub-package registers its services in its own extension method (e.g. AddAdminAccount(), AddAdminStructure(), AddAdminExport()) called from one AddWakeelAdmin() composition root created by admin-1. Admin-tool shell: dark top bar + top tabs, no sidebar (see the A previews). Roles are manager|secretary|custodian (SetupRoles). Tests in tests/Wakeel.Admin.Tests (bUnit for screens/states, service tests on a temp admin.db under a temp folder).`

const PACKAGES = {
  'design-verify': {
    key: 'design-verify', title: 'B1-POLISH-DESIGN (final verification)', model: 'sonnet',
    paths: 'src/Wakeel.Design/Components/WInput.razor, src/Wakeel.Design/Components/WInput.razor.css, src/Wakeel.Design/wwwroot/js/app.js, src/Wakeel.UI/Pages/W08AttentionCenter.razor, tests/Wakeel.UI.Tests',
    build: 'dotnet build src/Wakeel.Design --no-dependencies && dotnet build src/Wakeel.UI --no-dependencies && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'no unsafe JS interop; the caret helper only touches the element reference it is given',
    extraCheck: 'masked date input behaves correctly for null initial value, rejected characters and middle insertion (bUnit + a manual check in the running host on the gallery), and the W08 tab count uses the shared constant',
    spec: `Package B1-POLISH-DESIGN already went through build → review → fix → verify → fix2. Its history is archived: docs/build/reviews/B1/verify-design-polish.json (the last verification: one high finding that belonged to another package and four low findings) and docs/build/reviews/B1/fix2-design-polish.json (the builder's report of the fix2 round). Original scope: findings 1–3 of docs/build/reviews/B0.5/verify-design-split.json — (1) OnDateInputAsync guard compares against (Value ?? string.Empty); (2) the «الكل» tab in W08AttentionCenter uses Ar.AttentionCenter.TodayActionCount instead of the literal 9; (3) the caret position is stored on every OnDateInputAsync and re-applied from OnAfterRenderAsync through a caret-only JS helper so middle insertion keeps the caret; bUnit tests for the null-value first keystroke, a rejected character, and middle insertion; keep all UI tests green. The first-run package edits src/Wakeel.UI concurrently: if 'dotnet build src/Wakeel.UI --no-dependencies' fails inside src/Wakeel.UI/Pages/Account or Pages/FirstRun, that is the other package's transient state — wait 60 seconds and retry up to five times, and judge THIS package on its own files.`,
  },
  firstrun: {
    key: 'firstrun', title: 'B1-W (التشغيل الأول والدخول: W02–W07)', model: 'opus',
    paths: 'src/Wakeel.UI, src/Wakeel.Desktop, src/Wakeel.Core (additions only: services/entities the activation needs), src/Wakeel.Design/Text/Ar.FirstRun.cs (new partial file only), tests/Wakeel.Walkthrough.Tests, tests/Wakeel.Core.Tests (additions only), tests/Wakeel.UI.Tests (additions only)',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Walkthrough.Tests && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet test tests/Wakeel.Core.Tests --no-build && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'account password → PasswordWrap/RecoveryWrap/MachineWrap (DPAPI)/AdminWrap creation exactly as ARCHITECTURE §3, DbKey/VaultKey never written in clear, setup package verified before anything is trusted (org key pinned on activation, staging directory under ProgramData\\Wakeel\\staging never %TEMP%), lock-out counters, recovery code entry, temp setup file deletion, no secrets in logs or UI state',
    extraCheck: 'W02–W07 match their previews (light and dark), the first-run window has no sidebar and shows the approved brand, the walkthrough test drives the whole path on a temporary ProgramData folder, and the desktop host boots into W02 when no installation exists and into W05 otherwise (launched and screenshotted)',
    spec: `Build section «B1-W» of docs/build/packages/B1-admin-firstrun.md completely (W02–W07). PARTIAL WORK ALREADY ON DISK from earlier attempts of this same package (uncommitted; read before extending): src/Wakeel.UI/Pages/FirstRun/W02SetupFile, W03SetupCheck, W04Account (.razor + .razor.css), src/Wakeel.UI/Pages/Account/W05Login, W06LockOverlay, W07RecoveryDialog (.razor + .razor.css), src/Wakeel.UI/Layout/BareLayout.razor(.css), src/Wakeel.UI/Services/Account/ (AccountOptions, AccountServiceCollectionExtensions, AccountSession, ActivationService, Formats, IPrintService, LockService, LoginService, PasswordStrength, QrImageReader, RecoveryService, RecoverySheet, SetupFileDrop, SetupIds, SetupInspectionService, SetupRefusals, SignInProfile, VaultStore), src/Wakeel.Core/Services/InstallationService.cs, src/Wakeel.Design/Text/Ar.FirstRun.cs, src/Wakeel.Desktop/Services/DpapiPlatformProtector.cs and WebView2PrintService.cs, edits to src/Wakeel.Core/Data/WakeelPaths.cs, src/Wakeel.Core/Services/ServiceCollectionExtensions.cs, src/Wakeel.Desktop/Services/WakeelPaths.cs, src/Wakeel.UI/Wakeel.UI.csproj and _Imports.razor. tests/Wakeel.Walkthrough.Tests exists as a project but may have no tests yet. Inventory this work first (build it, list what compiles and what is missing against the spec), record the inventory in docs/build/progress/firstrun.md, then complete the package. Placement: Wakeel.UI references Wakeel.Crypto; the account services live in src/Wakeel.UI/Services/Account (SetupInspectionService wrapping SetupPackageReader.Inspect for W02–W03 with a staging directory under the product folder, ActivationService creating the database from the setup content — installation (role manager|secretary|custodian), org_units, devices, settings, pinned org key — and the InstallationKeyFile with the three wraps + AdminWrap, LoginService with attempt counter and temporary lock-out, LockService with the idle timer from settings (default 10 min) and the MachineWrap unlock, RecoveryService re-creating PasswordWrap from the recovery code with an optional new recovery sheet); Wakeel.Core gets only what is generic (InstallationService/SettingsService). Wakeel.Desktop supplies DpapiPlatformProtector (IPlatformProtector), ProgramData paths (C:\\ProgramData\\Wakeel\\{data,vault,keys,models,packages,backups,logs,staging}, created with user write access; honour --data-folder=<path> so tests and the E2E harness can point the host at a temporary folder), PrintToPdf via CoreWebView2 for the recovery sheet, drag-and-drop of a .wakeel-setup file onto W02, and the route decision at startup (no installation → /first-run, else → /login; the gallery stays reachable at /gallery). Screens: W02 (setup file + package password + «فحص الحزمة» + «أين أجد الملف؟» tooltip), W03 (live check list from SetupCheckResult, org card with logo, «تفعيل هذه النسخة», all error states listed in the spec), W04 (account password with strength meter + confirm, recovery sheet shown once with code + QR (QRCoder), print/PDF, «طبعتها وحفظتها» checkbox gating «ابدأ العمل»), W05 (login with org logo/name/office, employee name/avatar, password show/hide, «نسيت كلمة المرور؟ استخدم ورقة الاسترداد», clock status line from ClockCheckService), W06 (lock overlay over the current screen after idle, password, «متابعة», drafts-saved note), W07 (wrong password state + attempt counter + temporary lock, recovery dialog with manual code or QR image via ZXing, new password, new PasswordWrap, optional new recovery sheet). Arabic strings in src/Wakeel.Design/Text/Ar.FirstRun.cs as partial class Ar. The setup file may carry letter-template.docx (SetupContent.includes.letterTemplate, added by the crypto-polish package): store it with the report template on activation. Tests: tests/Wakeel.Walkthrough.Tests drives the full path on a temporary folder: a setup package produced with Wakeel.Crypto SetupPackageWriter (standing in for the admin tool) → inspection shows every check Ok → activation creates the database and key file → login succeeds/fails/locks out → lock and unlock → recovery with the code sets a new password and the old one stops working; each step asserts database rows, files on disk and audit_log entries, and that no secret appears in the log file or the database in clear text. bUnit tests for the six screens and their states (including the mixed Arabic/English lines). Launch the host with --data-folder pointing at an empty temp folder (W02) and at an activated one (W05), screenshot light and dark, kill it, and report the screen-by-screen fidelity in notes.`,
  },
  'admin-1': {
    key: 'admin-1', title: 'B1-A1 (مدير النظام: المضيف والقشرة والتخزين والحساب A01–A03)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'admin password and recovery code handling (Argon2id wraps via Wakeel.Crypto, no clear-text secrets in admin.db, logs or UI state), org key generation and storage, org-root certificate issuance, DPAPI use, ProgramData folder under --data-folder for tests, lock-out after 5 failed logins (30 s doubling)',
    extraCheck: 'A01, A02 and A03 match their previews (light and dark) with the admin-tool shell (dark top bar + top tabs, no sidebar), all texts Arabic via AdminAr, the Wakeel.Admin host was actually launched and rendered, and the composition root AddWakeelAdmin() plus the AdminAr file split leave clean extension points for admin-2 and admin-3',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-1): (a) complete the Wakeel.Admin WPF host (src/Wakeel.Admin already has App/MainWindow skeletons — extend them) and the Wakeel.Admin.UI shell (Routes, layout with dark top bar + top tabs for the 12 areas, no sidebar, theme light/dark, toasts, AdminAr.Common.cs); (b) AdminDb: SQLCipher admin.db with the DATA-MODEL §13 schema as Wakeel.Admin.UI's own migration set, opened with a key derived from the admin password wraps (AdminAccountService: password → Argon2id wraps, recovery sheet with a 20-character code + QR (QRCoder) shown once, «طبعتها وحفظتها» gating «ابدأ», login with attempt counter and lock-out 30 s doubling after 5 failures, recovery by code or QR image via ZXing then a new password), audit entries for every action (AuditService writing rows with no secrets — the writer only; A11's screen comes in admin-3); (c) KeyService core: org signing/encryption keys generated at A01 and stored wrapped, org-root certificate via DeviceCertificate.IssueOrgRoot; (d) screens A01 (create admin account + recovery sheet, print/PDF), A02 (login, lock-out, recovery), A03 (dashboard reading live counts — org units per level, activated/non-activated offices, devices, last export, alerts: offices without a setup file, undistributed changes, revoked devices — from the tables even while admin-2/3 have not filled them yet), plus the shared confirm/danger dialog and error-state components of A12 that these screens use; (e) tests: account creation + recovery sheet, login lock-out timing, recovery with code, audit rows contain no secrets, admin.db is unreadable without the key. Launch Wakeel.Admin.exe with --data-folder on a temp folder, screenshot A01/A02/A03 light and dark, kill it, report fidelity in notes.`,
  },
  'admin-2': {
    key: 'admin-2', title: 'B1-A2 (مدير النظام: المؤسسة والهيكلية والمكاتب والأجهزة والمفاتيح A04–A07)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'device certificate issuance with a key seed that is stored only until the first export, office key issue/rotation, revocation list signing with the org root, logo size/type validation, letter/report template validation (docx only, placeholders detected by reading the XML — no macro execution), no secrets in admin.db clear text, logs or UI state',
    extraCheck: 'A04–A07 match their previews (light and dark), structure rules (4 fixed levels, no duplicates per level, no delete with active devices), device numbering 1–9 unique per office, the letter template upload lists detected/unknown @ placeholders and shows an A4 and A5 PDF preview, and the Wakeel.Admin host was launched and rendered',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-2) builds on admin-1 (already reviewed and accepted — read its progress file docs/build/progress/admin-1.md and its code before extending): (a) OrgService + A04 «المؤسسة والهوية»: full org name, logo upload (PNG/JPG ≤ 2 MB with preview and square crop; «شعار كبير جدًا» state), financial-cycle start day 1–28 with explanation (item 52), numbering format (default YYYYMMDD/DESSS) with a danger warning on change (item 5), optional monthly report template (docx, item 53), official letter template (docx with @ placeholders per AGREEMENT item 57: on upload list the detected placeholders against the nine known ones — @التاريخ الهجري @التاريخ الميلادي @رقم الصادر @اسم رئيس المكتب @اسم مكتب المستقبل @اسم الموضوع @نص المراسلة @اسم المرسل @اسم مكتب المرسل — flag unknown ones, render a PDF preview in A4 and A5 through the host's print-to-PDF of an HTML rendering or through Word COM if available (fall back gracefully), and use the built-in default docs/templates/correspondence/نموذج-المراسلة.docx when none is uploaded); (b) StructureService + A05 «الهيكلية»: editable tree with four fixed levels (هيئة ← دائرة ← قسم ← وحدة): add/edit/move/disable, head job title and name, office inventory code (item 45), no duplicate names within a level, no delete of a unit with active devices; (c) DeviceService + A06 «المكاتب والأجهزة»: per office the devices (device number 1–9 unique inside the office, role manager|secretary|custodian, employee name and number 1–9, sync scope full|custody-only), phones shown read-only once paired; (d) KeyService + A07 «الحسابات والمفاتيح»: create a device account (issues the device certificate with a key seed kept until the first export), activate a custodian employee account, recover an account (re-issue certificate + new wrap), office key issue/rotate with the warning that every device needs a new setup file, revoke a specific device (item 24) through a danger dialog → signed revocation list; (e) tests: structure rules, device numbering, certificate issuance + revocation list verification, template placeholder detection on docs/templates/correspondence/نموذج-المراسلة.docx (nine placeholders, none unknown) and on a docx with a misspelt placeholder, logo validation. Launch Wakeel.Admin.exe with --data-folder on a temp folder, screenshot A04/A05/A06/A07 light and dark, kill it, report fidelity in notes.`,
  },
  'admin-3': {
    key: 'admin-3', title: 'B1-A3 (مدير النظام: التصدير والصيانة والتوزيع والسجل والحوارات A08–A12)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'setup package password shown once and never logged, staging directory under the product folder (never %TEMP%) wiped after export, key seed removed from admin.db after the first export, AdminWrap use when opening a Wakeel backup or ProgramData, audit log has no secrets, CSV export escapes formulas',
    extraCheck: 'A08–A12 match their previews (light and dark), the exported .wakeel-setup opens with Wakeel.Crypto SetupPackageReader with every check Ok (including letter-template.docx when set), distribution regenerates files for every affected office, the whole admin tool A01–A12 works end to end in the running host (launched and rendered), and nothing from admin-1/admin-2 regressed',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-3) builds on admin-1 and admin-2 (already reviewed and accepted — read docs/build/progress/admin-1.md and admin-2.md and their code before extending): (a) SetupExportService + A08 «تصدير ملف الإعداد»: 4-step wizard — office and device ← content (structure, logo, office key, device certificate and its seed, cycle day, numbering format, report template, letter template as letter-template.docx via SetupContent.includes.letterTemplate, guide PDF, revocation list) ← package password (PackagePassword.New(), 16 random characters shown once with a copy button and a warning) ← save <office>-<device>-<date>.wakeel-setup written by Wakeel.Crypto SetupPackageWriter with a staging directory under ProgramData\\WakeelAdmin\\staging; record the export in setup_exports and delete the device key seed from admin.db after the first export; provisional guide: if docs/guide/دليل-الوكيل.pdf is absent generate a one-page placeholder PDF at runtime into the exports folder (never write under docs/); (b) MaintenanceService + A09 «الصيانة»: open a Wakeel copy (ProgramData folder or a .wakeel-backup file) with the org key (AdminWrap) for viewing and verification, open a .wakeel-msg, recover a user account (new password wrap delivered through a recovery setup file), every dangerous action behind a red button and confirmation, with a log; (c) DistributionService + A10 «توزيع التحديثات»: list of undistributed structure/office changes, regenerate updated setup files for all affected offices in one batch (each with its own password); (d) A11 «سجل العمليات»: table who/what/when without secrets, local search, CSV export; (e) A12: every remaining state and dialog (device revocation danger confirm, numbering-format change warning, corrupt file / wrong password error, export success, logo too large); (f) tests: export produces a file that SetupPackageReader opens with all checks Ok (with and without the letter template), seed removed after the first export, distribution regenerates files for affected offices only, maintenance opens a backup produced by Wakeel.Crypto test helpers, audit CSV has no secrets, A12 dialogs render. Final pass: launch Wakeel.Admin.exe with --data-folder on a temp folder, walk A01→A12 once, screenshot A08/A09/A10/A11 and two A12 states light and dark, kill it, report fidelity in notes.`,
  },
}

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, screen-by-screen fidelity).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches, view the small previews of every screen involved (light and dark), and run exactly: ${p.build} (read the full output). Check: (1) every spec item and screen is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. Do NOT modify any file (you may not write the progress file either). Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${COMMON}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
async function chain(p) {
  const build = await agent(builderPrompt(p), { label: `build:${p.key}`, phase: 'Build', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
  if (!build) return { key: p.key, failed: 'build' }
  const review1 = await agent(reviewPrompt(p, build, 1), { label: `review1:${p.key}`, phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })
  const s = { key: p.key, build, review1 }
  if (review1 && review1.verdict === 'fix') {
    s.fix = await agent(fixPrompt(p, review1), { label: `fix:${p.key}`, phase: 'Fix', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
    s.verify = await agent(reviewPrompt(p, s.fix, 2), { label: `verify:${p.key}`, phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    if (s.verify && s.verify.verdict === 'fix') {
      s.fix2 = await agent(fixPrompt(p, s.verify), { label: `fix2:${p.key}`, phase: 'Fix2', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s.verify2 = await agent(reviewPrompt(p, s.fix2, 3), { label: `verify2:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    }
  }
  return s
}
// design-polish: only the final verification is left (fix2 already applied); one more fix round if needed.
async function designVerify() {
  const p = PACKAGES['design-verify']
  const fix2 = { see: 'docs/build/reviews/B1/fix2-design-polish.json', previous_verification: 'docs/build/reviews/B1/verify-design-polish.json' }
  const s = { key: p.key }
  s.verify2 = await agent(reviewPrompt(p, fix2, 3), { label: `verify2:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
  if (s.verify2 && s.verify2.verdict === 'fix') {
    s.fix3 = await agent(fixPrompt(p, s.verify2), { label: `fix3:${p.key}`, phase: 'Fix2', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
    s.verify3 = await agent(reviewPrompt(p, s.fix3, 4), { label: `verify3:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
  }
  return s
}
async function adminSequence() {
  const out = []
  for (const key of ['admin-1', 'admin-2', 'admin-3']) {
    log(`admin tool: starting ${key}`)
    const r = await chain(PACKAGES[key])
    out.push(r)
    const last = r.verify2 || r.verify || r.review1
    if (!last || last.verdict !== 'accept') { log(`admin tool: ${key} not accepted — stopping the sequence for the supervisor`); break }
  }
  return out
}
log('B1 resume: design final verify ∥ first run (continued) ∥ admin tool admin-1 → admin-2 → admin-3')
const results = await parallel([() => designVerify(), () => chain(PACKAGES.firstrun), () => adminSequence()])
return { design: results[0], firstrun: results[1], admin: results[2] }
