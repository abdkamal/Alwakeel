export const meta = {
  name: 'alwakeel-b6-sync-backup-settings',
  description: 'B6 and the Windows side of B7: computer-to-computer sync services (Wakeel.Sync), backup/restore with the official-sequence integrity check, the settings screens W83–W90, the sync and backup screens W75–W77 and W81–W82, phone pairing and USB file sync on Windows (MTP, W78–W80), then the two-database walkthrough + E2E; every package reviewed, fixed and re-verified by Opus; resumable through args.resume/args.skip',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }, { title: 'Fix3' }, { title: 'Verify3' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a screen, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (1–2 MB each; they overload the connection). View the small JPEG previews under design/exports/preview/light/W/<name>.jpg and design/exports/preview/dark/W/<name>.jpg (same file names as the PNGs, .jpg extension), only the screens of your own package, each at most once per comparison, right before you build or check that screen. (5) Screenshots you take must be JPEG or PNG at most 1366 px wide and viewed once. (6) When a report is handed to you as {"see": "<path>"}, read that JSON file first — it is the archived report of the previous stage.

DESIGN CONSISTENCY (owner's rule, 2026-09-16 — «انتبه للتصميم: لا يوجد تناسق وتنظيم في المكونات»): every screen is composed from Wakeel.Design components (src/Wakeel.Design/Components: WButton, WInput, WSelect, WCard, WChip, WBadge, WTabs, WTable, WKvRow, WStateCard, WDialog, WMenu, WStepper, WTree, WTimelineItem, WDocumentRow, WPager, WSearch, …). Hand-rolled lists, trees, tables, tabs, chips, forms, cards or dialogs inside a page are forbidden; page CSS may only arrange design components on the page grid and set page-specific spacing from the tokens. If a component is missing, add it to Wakeel.Design first (component + css + gallery entry + bUnit test), then use it. The screen is done only when its host screenshot, put beside the preview, shows the same anatomy: same regions and columns, same controls in the same places, same chips and states, no overlap, no clipping, aligned rows, consistent spacing. A builder writes the side-by-side comparison of every screen in its notes (deviation → fixed / accepted with reason). A reviewer opens the builder's screenshot and the preview together and reports every visible deviation as a finding: high for a hand-rolled component, medium for a layout, alignment, overlap or missing-control deviation, low for a spacing nuance.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset/clean. Another agent edits other pages in the same working tree concurrently: build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a file you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that file. A package that touches Blazor pages is not done until the host actually runs: activate a temporary data folder with tests/Wakeel.Walkthrough.Tests' FirstRunWorld (or a small test helper), launch src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe on it with --remote-debugging-port=<port> (use the port named in your package so concurrent packages never collide), --window-size=1366x768, --data-folder=<that folder> and --start-url=/route, confirm http://127.0.0.1:<port>/json/version answers, sign in over CDP, screenshot every screen light and dark (Page.captureScreenshot), LOOK at each beside its preview, read the host log in <data-folder>/logs for errors, then kill the process immediately — a host left running is a defect, and so is relaunching it more often than the work needs (the owner sees every window). Never touch C:\\ProgramData\\Wakeel of the real installation. All user-facing text is Arabic with no technical terms or error codes (item 15), numerals are always Western digits 0–9 and never Arabic-Indic (design guide «الأرقام غربية», item 20), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere; Arabic strings only in src/Wakeel.Design/Text/Ar.*.cs partial files (add Ar.Sync.cs, Ar.Backup.cs, Ar.Settings.cs, Ar.Phone.cs); Core and Sync layer strings in the static partial class CoreAr (src/Wakeel.Core/Services/CoreAr.<Area>.cs). Identifiers, comments and XML docs in English. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B6-sync-backup-settings.md (the B6 specification) and docs/build/packages/B7-android.md section B7-1 (the Windows side of the phone link), docs/AGREEMENT.md (items 7, 8, 9, 14, 17, 23, 24, 27, 30, 35, 37, 38, 43, 44, 52, 55, 56), docs/design/SCREENS.md (rows W75–W90) and docs/design/DESIGN-GUIDE.md, docs/build/ARCHITECTURE.md (§3 keys and certificates, §4 containers, §6 sync, §7 phone pairing and USB file transfer, §8, §10), docs/build/DATA-MODEL.md (§0 common columns incl. base_version and the sync scope, §1, §11 change_log / sync_log / sync_conflicts / devices / backups, §12, §14 what the phone mirrors). Foundation (read the public API of what you actually use, when you use it): src/Wakeel.Crypto (ContainerWriter/Reader with kinds sync, backup, phone; DeviceCertificate, CertificateChain, RevocationList; Pairing/PairingQrPayload+PairingToken+PairingMessages; Argon2Kdf, Aead, Hkdf; KeyWrap incl. AdminWrap and PasswordWrap), src/Wakeel.Core (WakeelDb — change_log is written by its SaveChanges interceptor —, DbSession, OfficialNumberService, ClockCheckService/ClockGuard, SettingsService, InstallationService, AuditService, Services/Documents IDocumentStore/VaultStore, every accepted module service of B2–B5), src/Wakeel.Sync (empty class library referencing Core and Crypto), src/Wakeel.UI (shell, account services from B1: password change re-wraps through the account service; theme service; the accepted pages of B2–B5 as patterns), src/Wakeel.Desktop (host, file dialogs, printers, Windows probes), the Android implementation of the same formats under android/core (read-only reference: the phone reads what you write). «للمستلم فقط» records (item 7) are excluded from every package except for their recipient.\n\n${PROTOCOL}`

const PACKAGES = {
  syncServices: {
    key: 'b6-sync-services', title: 'B6-1a (خدمات المزامنة بين الحواسيب: التتبع والحزمة والمعاينة والتطبيق والتعارضات والسجل)', model: 'opus', port: 9333,
    paths: 'src/Wakeel.Sync (new code), src/Wakeel.Core (Services/Sync interfaces + CoreAr.Sync.cs + DI lines; Data/Entities or Migrations/000N_*.sql only for a missing index/column against DATA-MODEL §11), tests/Wakeel.Core.Tests (a ProjectReference to Wakeel.Sync is allowed)',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Sync && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build',
    security: 'a package is verified completely (signature, certificate chain to the organisation root, revocation list, same office, package id not seen before, sequence per target device) before any row is previewed or applied; application is one database transaction — a failure in the middle applies nothing; conflicts are stored and never written automatically (items 43/44); «للمستلم فقط» rows and their vault files are excluded except for the recipient; vault files are streamed, never buffered whole; package timestamps feed ClockCheckService',
    extraCheck: 'the preview counts (to add / to update / conflict / excluded) are exact on crafted data with collapsible details per table group; a conflict is exactly base_version mismatch plus a local change; scope full vs custody-only; every synced table of DATA-MODEL is covered (list the tables in notes and prove it with a reflection test over the SyncedEntity types); the sync log feeds the «آخر مزامنة … منذ N أيام» state that AttentionService/HealthService already read',
    spec: `Build section «B6-1» services in Wakeel.Sync with interfaces in Core: ChangeTracker (reads change_log for a date range, the scope full/custody-only, excludes «للمستلم فقط» except for the recipient, collects the referenced vault files), SyncPackageWriter (.wakeel-sync container: office key + device signature; manifest with counts per table, records as JSON in batches, files streamed, a unique package id and a sequence number per target device), SyncPackageReader (verify → preview with the four classes and collapsible details per group → apply in one transaction updating base_version; conflicts stored in sync_conflicts with nothing written automatically; timestamps feed ClockCheckService), ConflictResolver (manual, field by field or choose a version, one or all, with a log), SyncLog (history per device: last sync, direction, counts; the stale-sync state for the attention centre and the report). View models for W75–W77. Tests: tracking, scope and exclusion; package signature/verification incl. tampered, revoked device, wrong office and replay; preview classes; conflict detection and no automatic write; atomic apply (inject a failure in the middle → nothing applied); conflict resolution by field and by version; the reflection test over synced entities; a round trip between two temporary installations (manager and secretary worlds). Register in DI (Core + desktop host lines are for the screens package).`,
  },
  backupServices: {
    key: 'b6-backup-services', title: 'B6-2a (خدمات النسخ الاحتياطي والاستعادة وفحص سلامة التسلسل الرسمي)', model: 'opus', port: 9333,
    paths: 'src/Wakeel.Sync (Backup/* new) or src/Wakeel.Core (Services/Backup/* new — choose one home and say why), src/Wakeel.Core (CoreAr.Backup.cs, DI lines, OfficialNumberService only to add the numbering hold after a restore), tests/Wakeel.Core.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Sync && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build',
    security: 'the backup password is chosen at every backup with the strength meter rules, stretched with Argon2id and used for AES-GCM, plus the AdminWrap so the organisation can open it in the admin tool; the password lives in memory only for the operation; the database copy is consistent (VACUUM INTO or the SQLite backup API on the SQLCipher connection) and the temporary copy is wiped; a safety backup is taken automatically before a restore replaces data; a wrong password or a corrupt file changes nothing and says so in plain Arabic',
    extraCheck: 'restore preview shows date, version and content before anything is replaced; after a restore the official-sequence integrity check (item 37) compares official_numbers with the highest number actually used in correspondence and in exported packages and either reports «سليم» or «يحتاج مراجعة قبل إصدار أرقام» and holds numbering until the user confirms; the periodic reminder and the overdue notification use the settings; the backup log is kept',
    spec: `Build section «B6-2» services: BackupService (one .wakeel-backup file: database, vault optional, key file, settings; progress; default destination backups\\ or a folder the user chooses; periodic reminder and a notification when overdue; backup log) and RestoreService (choose file and password, preview, replacement warning with the automatic safety backup, restore, then the official-sequence integrity check of item 37 with the numbering hold until the user confirms). View models for W81–W82. The admin tool's maintenance already opens a backup through AdminWrap — keep the container layout it expects (read src/Wakeel.Admin.UI/Services/Maintenance if it exists). Tests: backup/restore round trip into a third temporary installation, wrong password, corrupt file, vault optional, safety backup taken, sequence check detecting a later number used in an exported package, numbering hold and release, reminder timing with TestClock.`,
  },
  settingsScreens: {
    key: 'b6-settings-screens', title: 'B6-3 (الإعدادات — W83–W90)', model: 'opus', port: 9336,
    paths: 'src/Wakeel.UI/Pages/Settings (new), src/Wakeel.UI/Services/Settings (new), src/Wakeel.UI/Routes.razor and Layout (route wiring, the user-menu/top-bar avatar), src/Wakeel.Design/Text/Ar.Settings.cs (new), src/Wakeel.Design/Components (new components only if the spec needs them, e.g. WSettingsNav, WImageCropper shared with the admin logo crop, each with css + gallery entry + bUnit test), src/Wakeel.Core (Services/SettingsService.cs additions: typed keys with validation and defaults only), src/Wakeel.Desktop (only: printers/scanners enumeration, open-folder, font-size/density application hooks), tests/Wakeel.UI.Tests, tests/Wakeel.Core.Tests (settings keys only)',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'changing the password asks for the current one, re-creates the PasswordWrap and keeps the recovery wraps; passwords never stay in component state after submit; the profile photo is validated (type, size) and stored through the vault; read-only items (name, cycle start day, numbering format) cannot be edited and say who sets them; no file editing, no commands (item 38)',
    extraCheck: 'W83–W90 match their previews side by side; every setting has an explanation, validation and immediate save with a confirmation message, and is really applied (auto-lock minutes reach LockService, thresholds reach AttentionService, the item-56 reminder default reaches ReminderScheduler and is flagged for the phone sync, theme/font size/density apply instantly and persist, OCR languages and re-index reach the search services, payroll defaults reach PayrollService); W87 lists the discovered models with their four states from ModelsFolderWatcher; W88 lists paired computers and phones with «إلغاء الاقتران» for a phone',
    spec: `Build section «B6-3 الإعدادات»: in-page side tabs (الحساب، الصورة، المظهر، التنبيهات، البحث والنماذج، الطباعة والمسح، المزامنة والنسخ، الرواتب، حول) — W83 account (name read-only from the setup file, photo, change password with strength meter, auto-lock 1–120 minutes), W84 profile photo (add/change/remove, round preview, square crop dialog with zoom; shown in the top bar; flagged for phone sync), W85 appearance (light/dark/system as preview cards, three font sizes, comfortable/compact density), W86 alerts and thresholds (overdue/near/stale days with explanations, monthly report reminder days, calendar alerts: meeting alert switch + time list 5/10/15/30/60 minutes/2 hours/1 day/custom minutes + send meeting alerts to the phone through sync (switch, on by default) + today's tasks alert + dues within 24 hours alert — item 56 —, sounds and the alert tone), W87 search/OCR/models (models folder path with open-folder, discovered models with states suitable/unsuitable with reason/preparing N%/active, OCR languages, re-index), W88 printing/scanning/sync/backup (default printer, scanner, packages folder, backup reminder, device name, paired devices — computers and phones — with un-pair for a phone), W89 payroll and bonuses (default salary components, bonus types, deductions, the note that edits never touch committed sheets), W90 about and the financial cycle (version, organisation/office/device, cycle start day and numbering format read-only, the guide PDF link, health centre link). bUnit tests for every tab and state, each setting saved/read/applied, password change flow, crop dialog. Host run on port 9336 with a seeded temporary installation: screenshot every tab light and dark, compare side by side (table in notes), kill.`,
  },
  syncScreens: {
    key: 'b6-sync-screens', title: 'B6-1b/2b (مركز المزامنة والمعاينة والتعارضات والنسخ الاحتياطي والاستعادة — W75–W77, W81, W82)', model: 'opus', port: 9333,
    paths: 'src/Wakeel.UI/Pages/Sync (new), src/Wakeel.UI/Pages/Backup (new), src/Wakeel.UI/Services/Sync (new), src/Wakeel.UI/Routes.razor and Layout (route/sidebar-footer wiring only), src/Wakeel.Design/Text/Ar.Sync.cs and Ar.Backup.cs (new), src/Wakeel.Design/Components (new components only if the spec needs them; reuse WDiffPair from the custody conflict centre), src/Wakeel.Desktop (file dialogs, DI lines, the .wakeel-sync/.wakeel-backup open-with argument), tests/Wakeel.UI.Tests',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'nothing is applied before the preview is confirmed, the conflict centre never writes automatically, the backup password fields are cleared after use and never logged, restore shows the replacement warning and requires explicit confirmation, errors are plain Arabic without codes',
    extraCheck: 'W75–W77, W81 and W82 match their previews side by side; export card (from/to dates, scope full/custody-only, destination; signed with the device key — no password) and import card; the sync history with the stale-sync state; the preview table with the four classes and collapsible groups; per-conflict and for-all choices; backup progress; the sequence-integrity card after restore with both outcomes',
    spec: `Build W75 (sync centre: export and import cards + the history with the other device and the «آخر مزامنة … منذ N أيام» state), W76 (package preview before applying: summary table to add / to update / conflicts / excluded with collapsible details, «تطبيق» and «إلغاء»), W77 (conflict centre: list with record, field, ours, theirs, date; choose per conflict or for all; the no-automatic-write note), W81 (backup: destination, backup password with strength, include the vault, last backups, reminder schedule, progress), W82 (restore and sequence check: file and password, preview, replacement warning, then the «فحص سلامة التسلسل الرسمي» card with its result and the confirm-to-release-numbering action). Wire the routes and the sidebar footer entries. bUnit tests for every screen and state, preview classes, conflict choices, password strength, restore gating. Host run on port 9333 with two seeded temporary installations (export from one, preview/apply on the other): screenshot every screen light and dark, compare side by side (table in notes), kill.`,
  },
  phoneWindows: {
    key: 'b7-phone-windows', title: 'B7-1 (الهاتف على ويندوز: النقل عبر USB والاقتران ومزامنة الهاتف — W78–W80)', model: 'opus', port: 9337,
    paths: 'src/Wakeel.Sync (Phone/* new: PhonePairingService, PhoneSyncService, the phone package mapping), src/Wakeel.Core (Services/Sync interfaces, CoreAr.Phone.cs, DI lines), src/Wakeel.Desktop/Services (MtpTransport over Windows Portable Devices through the pinned MediaDevices package, device arrival/removal watcher — new files) and src/Wakeel.Desktop (csproj PackageReference MediaDevices, App.xaml.cs DI lines), src/Wakeel.UI/Pages/Phone (new), src/Wakeel.UI/Services/Phone (new), src/Wakeel.UI/Routes.razor (route lines), src/Wakeel.Design/Text/Ar.Phone.cs (new), tests/Wakeel.Core.Tests, tests/Wakeel.UI.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Sync && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build && dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'the pairing token is 32 random bytes valid for ten minutes and single use, the short code is six digits bound to the same session; the phone certificate is signed only after the pairing-request signature and the token HMAC verify (chain authority → computer → phone); the session key and the office key travel sealed to the phone key; un-pairing revokes the phone; every to-phone package excludes «للمستلم فقط» unless the phone belongs to the recipient; a package is applied whole or not at all; a cable cut stops safely and loses nothing (item 35); USB file transfer only — no network, no ADB, no developer mode (item 9)',
    extraCheck: 'W78–W80 match their previews side by side; the transport is an interface (IPhoneTransport) with the MTP implementation in the desktop host and a folder implementation for tests, so the whole pairing + sync scenario runs in tests against a local folder using the same files the Android app reads (compare with android/core: names pairing-<code>.wakeel-phone, pairing-request, pairing-accept, to-phone-<seq>, from-phone-<seq>, the acknowledgement); packages are ≤ 20 MB and numbered; phone expenses arrive as «بانتظار التأكيد» and their state flows back; synced settings include the meeting alert time and the send-to-phone switch (item 56)',
    spec: `Build section «B7-1»: IPhoneTransport + MtpTransport (Windows Portable Devices through MediaDevices: discover USB-connected devices, choose the paired phone by name/id, reach the Wakeel folder in shared storage and create it when missing, stream write/read/delete with progress, watch attach/detach; on a cable cut stop safely and expose the W80 state — what completed and what remains, «متابعة عند إعادة التوصيل»), PhonePairingService (session: 32-byte token + six-digit short code valid ten minutes, QR through the WQrCode component and the code, write pairing-<code>.wakeel-phone when the phone is connected, wait for pairing-request, verify and sign the phone certificate, write pairing-accept, register the device in devices as paired; un-pair from settings; re-pair after the phone lost its data), PhoneSyncService (prepare to-phone-<seq>.wakeel-phone: summarised correspondence, tasks, meetings, appointments, decisions, commitments, parties and structure, pending notifications, pinned files in full, synced settings, phone expense states; apply from-phone-<seq>.wakeel-phone: expenses, document photos with OCR text, text and voice notes, tasks, decisions, commitments, follow-ups, report notes, inventory scan results; ≤ 20 MB numbered packages, whole-package application, acknowledgement with the last sequence; the outbox to the phone shown in W78), and the screens W78 (paired devices with last sync and USB state, «مزامنة الآن», step progress, the outbox), W79 (pairing dialog: large QR + short code + the phone steps + waiting/paired states), W80 (USB interruption state). Tests: pairing happy path and failures (expired token, wrong HMAC, replay), certificate chain of the phone, to-phone content and exclusions, from-phone application incl. a pending phone expense, 20 MB split and numbering, interruption and resume over the folder transport, un-pair revocation; if android/core/build/vectors-out exists, read the Android-made samples. bUnit tests for W78–W80 states. Host run on port 9337 (no phone attached: the «لا هاتف موصول» state, the pairing dialog, and W80 through a test hook or seeded state): screenshot light and dark, compare side by side, kill.`,
  },
  walkthrough: {
    key: 'b6-walkthrough', title: 'B6-WALKTHROUGH (قاعدتان: مزامنة وتعارض ونسخ واستعادة + لقطات E2E)', model: 'sonnet', port: 9333,
    paths: 'tests/Wakeel.Walkthrough.Tests, tests/Wakeel.E2E',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Walkthrough.Tests --no-dependencies && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet build tests/Wakeel.E2E --no-dependencies && dotnet test tests/Wakeel.E2E --no-build',
    security: 'tests use temporary folders only and never touch the real installation',
    extraCheck: 'the walkthrough covers every step of the B6 acceptance list on two crafted databases plus a third for the restore, asserting counts, conflicts, files and the sequence check; E2E screenshots of W75–W90 are compared with the design PNGs with per-screen upper bounds',
    spec: `Build section «القبول» of docs/build/packages/B6-sync-backup-settings.md: (1) tests/Wakeel.Walkthrough.Tests on two crafted installations (manager and secretary): crossing edits → export from–to → the preview shows the right counts → apply → one deliberate conflict appears and is resolved manually → a backup → restore onto a third installation → the sequence check detects a later number used in an exported package; plus the phone scenario over the folder transport (pair → to-phone → a from-phone expense arrives as pending → confirmed → its state returns). (2) tests/Wakeel.E2E: screenshot tests for W75–W90 at 1366x768 light and dark compared with design/exports/{light,dark}/W/*.png (the test code may read the PNGs; you look only at the small previews and your own screenshots), diff percentages recorded in tests/Wakeel.E2E/artifacts/ with per-screen upper bounds; host on port 9333 with temporary folders seeded through the walkthrough helpers. Report the diff percentages per screen in notes.`,
  },
}

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}; your CDP port: ${p.port}). ${COMMON}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (the per-screen side-by-side comparison table and design decisions).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}; your CDP port: ${p.port}). ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output). VISUAL FIDELITY CHECK: launch the host yourself on a seeded temporary installation, screenshot every screen the package covers in light and dark, open each screenshot together with its preview, and report every visible deviation as a finding (high: a hand-rolled component where a Wakeel.Design one exists or should exist; medium: layout, alignment, overlap, clipping, missing control/column/chip/state; low: spacing nuance). Also check: (1) every spec item and screen is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. From round 2 on, first confirm that each finding of the previous round is really closed, then look for regressions; do not reopen supervisor rulings. Do NOT modify any file (you may not write the progress file either). Kill the host when done. Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}; your CDP port: ${p.port}) returning to apply review findings. ${COMMON}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green, re-screenshot what you changed and compare again. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
const ORDER = ['build', 'review1', 'fix', 'verify', 'fix2', 'verify2', 'fix3', 'verify3']
const PHASE = { build: 'Build', review1: 'Review', fix: 'Fix', verify: 'Verify', fix2: 'Fix2', verify2: 'Verify2', fix3: 'Fix3', verify3: 'Verify3' }
function isReview(st) { return st.startsWith('review') || st.startsWith('verify') }
// An agent that dies on a connection error returns null: start it again (the restart protocol makes the
// new attempt continue from the working tree and the progress file). Three attempts, then give up.
async function tryAgent(prompt, opts) {
  for (let n = 1; n <= 3; n++) {
    const r = await agent(prompt, n === 1 ? opts : { ...opts, label: `${opts.label}#${n}` })
    if (r) return r
    log(`${opts.label}: attempt ${n} returned nothing${n < 3 ? ' — starting it again' : ' — giving up'}`)
  }
  return null
}
// args = { skip: [package keys already accepted], resume: { '<package key>': { stage, file, state, rulings, extraPaths } } }
//   stage: build|review1|fix|verify|fix2|verify2|fix3|verify3 — the stage to run first;
//   file: archived report of the stage before it (handed over as {see: file}).
async function chainFrom(p0, r0) {
  const r = r0 || {}
  const p = { ...p0,
    paths: p0.paths + (r.extraPaths ? ', ' + r.extraPaths : ''),
    spec: p0.spec + (r.state ? '\n\nSTATE OF THIS PACKAGE: ' + r.state : '') + (r.rulings ? '\n\nSUPERVISOR RULINGS FOR THIS ROUND (final, do not reopen): ' + r.rulings : '') }
  const s = { key: p.key, accepted: false }
  let prev = r.file ? { see: r.file } : null
  for (let i = r.stage ? ORDER.indexOf(r.stage) : 0; i < ORDER.length; i++) {
    const st = ORDER[i]
    if (isReview(st)) {
      const round = ORDER.slice(0, i + 1).filter(isReview).length
      const rev = await tryAgent(reviewPrompt(p, prev, round), { label: `${st}:${p.key}`, phase: PHASE[st], schema: REVIEW_SCHEMA, model: 'opus', effort: round === 1 ? 'high' : 'medium' })
      s[st] = rev
      if (!rev) return { ...s, failed: st }
      if (rev.verdict === 'accept') return { ...s, accepted: true }
      prev = rev
    } else {
      const prompt = st === 'build' ? builderPrompt(p) : fixPrompt(p, prev)
      const out = await tryAgent(prompt, { label: `${st}:${p.key}`, phase: PHASE[st], schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s[st] = out
      if (!out) return { ...s, failed: st }
      prev = out
    }
  }
  return s
}
const RESUME = (args && args.resume) || {}
const SKIP = (args && args.skip) || []
async function sequence(name, names) {
  const out = []
  for (const n of names) {
    const p = PACKAGES[n]
    if (SKIP.includes(p.key)) { out.push({ key: p.key, skipped: true, accepted: true }); continue }
    log(`${name}: ${p.key}${RESUME[p.key] ? ' (resuming at ' + RESUME[p.key].stage + ')' : ''}`)
    const s = await chainFrom(p, RESUME[p.key])
    out.push(s)
    if (!s.accepted) { log(`${name}: ${p.key} not accepted${s.failed ? ' (stage ' + s.failed + ' died)' : ''} — stopping this sequence for the supervisor`); break }
  }
  return out
}
log('B6: sync services → backup services ∥ settings screens; then sync/backup screens ∥ phone link on Windows; then the walkthrough')
const first = await parallel([
  () => sequence('sync and backup services', ['syncServices', 'backupServices']),
  () => sequence('settings', ['settingsScreens']),
])
const need1 = [2, 1]
if (!first.every((seq, i) => seq && seq.length === need1[i] && seq.every(s => s.accepted))) { log('B6: the first stage was not accepted — waiting for the supervisor'); return { first } }
const second = await parallel([
  () => sequence('sync and backup screens', ['syncScreens']),
  () => sequence('phone link on Windows', ['phoneWindows']),
])
if (!second.every(seq => seq && seq.length === 1 && seq.every(s => s.accepted))) { log('B6: the second stage was not accepted — the walkthrough waits for the supervisor'); return { first, second } }
const walk = await sequence('walkthrough', ['walkthrough'])
return { first, second, walkthrough: walk }
