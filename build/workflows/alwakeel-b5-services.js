export const meta = {
  name: 'alwakeel-b5-services',
  description: 'B5 services (no screens): assets and custody, employees/payroll/bonuses, finance with the internal double-entry ledger, monthly report draft/readiness/issue and the other reports — one sequential chain of four Opus packages, each reviewed, fixed and re-verified by Opus; resumable through args.resume/args.skip',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }, { title: 'Fix3' }, { title: 'Verify3' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a service, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports; this package has no screens — if you need a screen's intent, read docs/design/SCREENS.md or view the small preview under design/exports/preview/light/W/ once. (5) Any screenshot you take must be JPEG or PNG at most 1366 px wide and viewed once. (6) When a report is handed to you as {"see": "<path>"}, read that JSON file first — it is the archived report of the previous stage.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain PackageReferences to pinned packages and ProjectReferences). Never run git commit/checkout/stash/reset/clean. Other agents edit other projects in the same working tree concurrently (design-system and screen packages): build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. Never touch C:\\ProgramData\\Wakeel of the real installation: every test uses a temporary folder. All user-facing text is Arabic with no technical terms or error codes (item 15) — Core-layer strings live in the static partial class CoreAr (src/Wakeel.Core/Services/CoreAr*.cs, ratified in ARCHITECTURE §12), other layers' strings in their own Ar file; identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or memory longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B5-employees-assets-finance-reports.md (the B5 specification), docs/AGREEMENT.md (items 13, 37, 44–48, 50–53, 55 and §6 inherited policies), docs/design/SCREENS.md (rows W51–W74 — what each screen shows, so that the view models carry it), docs/build/ARCHITECTURE.md (§4 containers inventory/transfer, §5, §9 printing, §12), docs/build/DATA-MODEL.md (§0, §7–§10 — the tables already exist in src/Wakeel.Core/Migrations/0001_initial.sql and the entities in src/Wakeel.Core/Data/Entities/Employees.cs, Assets.cs, Finance.cs, Reports.cs), docs/build/BUILD-PLAN.md (acceptance criteria). Foundation (read the public API of what you actually use, when you use it): src/Wakeel.Core (WakeelDb with audit stamps and enforced soft delete, DbSession, AuditService, SettingsService, InstallationService, FinancialCycleService (item 52 — extend it, never fork it), IClock, IdGenerator, ArabicText incl. ArabicText.Plural, ArabicRelativeTime, CoreAr — a static partial class, one file per area: add CoreAr.<Area>.cs —, the ACCEPTED services you must stay consistent with: AttentionService/BadgeService (pending phone expenses, tab counts), NotificationService, QuickCaptureService (writes expenses and report notes), Services/Correspondence (CorrespondenceService for «إرسال إلى الجهة الأعلى», ExchangeService .wakeel-msg, TemplateService), Services/Documents (IDocumentStore/VaultStore — every generated docx/PDF/receipt is stored there), Services/FollowUp, Services/Meetings, Services/Cases, Services/Parties (the report sections read them)), src/Wakeel.Crypto (containers .wakeel-inventory / .wakeel-transfer: signed with the device key, encrypted where the architecture says so; Sha256; DeviceCertificate), src/Wakeel.Reports (OpenXML helpers and the HTML → PDF path the letter composer established: Letters/LetterOpenXml.cs, LetterHtmlRenderer.cs, WordSession.cs — reuse, do not duplicate), tests/Wakeel.Core.Tests (worlds and seeds, TestClock). Money is stored as integer agorot (minor units of the shekel) — never floating point; every service returns Arabic-ready view models for its screens.\n\n${PROTOCOL}`

const PACKAGES = [
  {
    key: 'b5-assets-services', title: 'B5-2a (خدمات الأصول والعُهد: الأصول، حركات العُهد، النقل بين مكتبين، الجرد، تعارضات العُهد)', model: 'opus',
    paths: 'src/Wakeel.Core (Services/Assets/* new, Services/CoreAr.Assets.cs new, DI lines, Data/Entities additions only if a column is missing against DATA-MODEL §8, Migrations/000N_*.sql only for a missing index/column), src/Wakeel.Reports (Assets/* new: the handover receipt HTML for print/PDF), tests/Wakeel.Core.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Reports && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build',
    security: 'the inventory number <office code>-<sequence> is issued inside a transaction and unique across the authority (item 45); custody movements are append-only; .wakeel-transfer and .wakeel-inventory packages are signed and verified before anything is applied, a rejected or tampered package changes nothing; conflicts are never resolved automatically (item 44); QR tokens are random and carry no data',
    extraCheck: 'handover → receive/confirm → return keeps a cumulative log with «بانتظار تأكيد الاستلام»; exporting a transfer puts the asset «قيد التسليم» and accepting an incoming package moves ownership while keeping origin_office_code; an inventory session records present/missing/damaged with progress, accepts phone QR results and exports a signed result file (item 47); the conflict resolver shows both versions field by field and applies a chosen version or a per-field merge',
    spec: `Build section «B5-2 الأصول والعُهد» services: AssetService (assets with the inventory number of item 45, category, location, custodian, state, QR token; custody movements as an append-only log — handover/receive/confirm/return — with a printable handover receipt (HTML in Wakeel.Reports/Assets stored through IDocumentStore); the «بانتظار تأكيد الاستلام» query; W58 list and W59 details-and-movements view models, the W60 handover dialog validation), AssetTransferService (item 46: export a signed .wakeel-transfer package — receiving office from the structure, the assets, a summary — which marks the assets in_transfer; preview an incoming package then accept or reject it — acceptance moves ownership and keeps the origin; W61 view models), InventoryService (item 47: a session listing the office's assets with present/missing/damaged and progress, results arriving from the phone by QR scan (via=phone_qr), ending the session, exporting signed results as .wakeel-inventory for the higher authority; W62 view models), CustodyConflictResolver (item 44: conflicts on the same asset between the office and the custody employee from sync_conflicts — both versions side by side with differences, choose one or merge field by field, never automatic; W63 view models). Expose for the employees package: the open-custody query per employee. Tests: inventory number uniqueness under concurrent issue, the cumulative custody log and pending confirmation, transfer export/accept/reject round trip between two temporary installations incl. a tampered package, inventory session progress/export/verification, conflict resolution by version and by field. Register everything in the Core DI extension.`,
  },
  {
    key: 'b5-employees-services', title: 'B5-1a (خدمات الموظفين والرواتب والمكافآت)', model: 'opus',
    paths: 'src/Wakeel.Core (Services/Employees/* new, Services/CoreAr.Employees.cs new, DI lines, Data/Entities additions only if a column is missing against DATA-MODEL §7, Migrations/000N_*.sql only for a missing index/column), src/Wakeel.Reports (Payroll/* new: the payroll sheet HTML for print/PDF with the organisation letterhead and signature lines), tests/Wakeel.Core.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Reports && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build',
    security: 'salary data never reaches logs or audit text (audit rows name the action and the employee, not the amounts), a committed payroll run is frozen, imported department sheets (.wakeel-inventory kind payroll, item 48) are verified before preview and never merged when the signature fails, termination disables the account and never deletes it',
    extraCheck: 'net pay = basic + allowances + bonuses − deductions with components chosen by their validity dates for the period; in-line edits live in overrides and survive recomputation; the pre-commit summary warns about an employee without a basic salary; termination is refused while custody is open and the message names the remaining assets; the payroll sheet HTML mirrors W55',
    spec: `Build section «B5-1 الموظفون والرواتب» services: EmployeeService (employees of the office scope: data, unit, job title, state, photo through IDocumentStore, the assets in their custody via the assets package's query; termination forbidden until every custody item is settled — a clear Arabic message listing the remaining assets — and it disables the account without deleting; W51 list and W52 file view models incl. salary components, custody, payroll history and the warning), PayrollService (salary components basic/allowance/deduction/bonus_rule editable at any time with validity dates; a monthly run per unit/section: one line per employee with basic, allowances, deductions, bonuses and net, in-line edit stored in overrides, the pre-commit summary — total net, bonuses, deductions, head count, warnings such as an employee without a basic salary —, commit freezes the run, the printable payroll sheet (HTML → PDF/printer) with the organisation letterhead and signature lines; import of department sheets from a signed file (item 48) with preview and merge into the directorate's sheet recorded in payroll_imports; W53–W56 view models), BonusService (bonuses granted in the cycle: employee, type, amount, reason, date; approved rules from settings; W57 view models and the grant dialog validation). Tests: net pay across validity dates and overrides, recomputation keeps overrides, commit freezes, summary warnings, termination refused/allowed, import of a good and a tampered department sheet and the merge totals, bonus rules. Register everything in the Core DI extension.`,
  },
  {
    key: 'b5-finance-services', title: 'B5-3a (خدمات المالية: الحركات والدفتر المزدوج ومصروفات الهاتف وجرد النقدية والدورات)', model: 'opus',
    paths: 'src/Wakeel.Core (Services/Finance/* new, Services/FinancialCycleService.cs extension, Services/CoreAr.Finance.cs new, DI lines, Data/Entities additions only if a column is missing against DATA-MODEL §9, Migrations/000N_*.sql only for a missing index/column; Services/FollowUp/CommitmentService only to link a payment to a transaction id if it left a hook), tests/Wakeel.Core.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Reports && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build',
    security: 'every transaction writes balanced ledger entries in the same database transaction; an issued cycle is locked — edits and deletes are refused and a correction goes to the first open cycle with original_at kept (item 51); free edit/delete before issue is audited (the delete without a reason, as agreed); amounts are integer agorot; phone expense confirmation is idempotent',
    extraCheck: 'sum(debit) = sum(credit) always, per transaction and overall; totals and the cash balance come from the ledger; a phone expense confirmed after its cycle was issued lands in the first open cycle with the phone date kept as original_at; batch confirm and reject with an optional reason; the cash count stores the denominations and the semantic difference; the cycles table shows states and totals with «بانتظار الإصدار»; AttentionService/BadgeService counts stay correct',
    spec: `Build section «B5-3 المالية» services: FinanceService (expenses and income in shekels stored as agorot, categories, purpose, a date inside an open cycle only — when the date falls in an issued cycle warn and book it in the first open cycle with a reference to the original date (item 51) —, receipt document through IDocumentStore, free edit/delete until issue with the delete recorded in the audit log without a reason, double-entry ledger_entries for every movement incl. corrections (correction_of_id), totals and the cash balance; W64 tabs/table/total, W65 add dialog validation and W69 income view models), PhoneExpenseService (item 50: pending phone expenses — edit amount/purpose/category before confirming, confirm one or all, reject with an optional reason, confirmation creates the transaction with the phone's date and time or in the first open cycle when its cycle is issued, the state flows back to the phone at the next sync through the existing columns; reconcile with how AttentionService/W08 already confirm and reject — one implementation only; W66 cards view models and the empty state), CashCountService (book balance from the ledger, counted by denominations, difference with its semantic colour, note, history; W68 view models), FinancialCycleService extension (cycles table with state and totals, «بانتظار الإصدار», locking an issued cycle, the read-only start-day card; W67 view models). Tests: balanced ledger on every path (create, edit, delete, correction, phone confirmation), issued-cycle lock and the correcting entry in the next open cycle, phone expense lifecycle incl. batch and late confirmation, cash count difference, cycle states across TestClock dates with a start day other than 1, totals by category. Register everything in the Core DI extension.`,
  },
  {
    key: 'b5-reports-services', title: 'B5-4a (خدمات التقارير: مسودة التقرير الشهري والجاهزية والإصدار والتقارير الأخرى)', model: 'opus',
    paths: 'src/Wakeel.Core (Services/Reports/* new: interfaces, ReportDraftService, ReadinessService, OtherReports queries, CoreAr.Reports.cs new, DI lines, Data/Entities additions only if a column is missing against DATA-MODEL §10, Migrations/000N_*.sql only for a missing index/column), src/Wakeel.Reports (Monthly/* new: docx generation through OpenXML from the built-in default report template or the organisation template delivered by the setup file, HTML rendering for the preview and the WebView2 PDF fallback, Other/* new: the other reports as HTML + CSV), src/Wakeel.Desktop/Services (only a PDF hook if the letter one cannot be reused) and App.xaml.cs (DI lines only), tests/Wakeel.Core.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Reports && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build' + ' && dotnet build src/Wakeel.Desktop',
    security: 'issuing is one confirmed action that locks the cycle, stores a frozen signed copy with its SHA-256 in the vault and can never be repeated for the same cycle; an addendum is numbered and never rewrites the issued report; generated sentences are deterministic templates (no model, no network); report templates are opened as zip/XML only; CSV export escapes formulas; salaries appear only in the payroll section and never in logs',
    extraCheck: 'the draft refreshes from live data for the current cycle (item 53 a) and honours the per-record report options and the default inclusion rules from settings; the Arabic template sentences are correct for 0, 1, 2, 3–10 and 11+ through ArabicText.Plural; the executive summary compares with the previous cycle with a trend arrow and a simple bar chart drawn without external libraries; readiness lists each gap with a link target; «إرسال إلى الجهة الأعلى» creates an outgoing correspondence with an official number through CorrespondenceService and queues the .wakeel-msg',
    spec: `Build section «B5-4 التقارير» services: ReportDraftService (an automatic draft per cycle that updates live — sections: cover, executive summary with indicators, comparison with the previous cycle, trend arrow and a simple bar chart, correspondence, meetings and decisions, tasks/commitments/obstacles, cases, flagged notes, finance by category, assets and custody, employees and payroll, needs and recommendations, the director's word; default inclusion rules changeable from settings; deterministic Arabic template sentences with singular/dual/plural through ArabicText.Plural; editing the generated text per section while keeping the generated original; hiding and reordering sections; W70 three-column and W71 section-editor view models), ReadinessService (the readiness percentage and the gaps with links: phone expenses pending confirmation, cash count not recorded, tasks past due without an update, meetings without minutes or decisions, decisions without a state, referrals past their deadline, stale sync — this is the service the W08 banner was waiting for: expose the same summary AttentionService/W08 expects), ReportIssueService (issue with one confirmed action: the phone-expenses warning with confirm/reject from the dialog, lock the cycle, generate the docx from the template through OpenXML, PDF through Word or the WebView2 fallback, store a frozen signed copy with SHA-256 in the vault, the option «إرسال إلى الجهة الأعلى» creating an outgoing correspondence with an official number and a .wakeel-msg for the next sync; a numbered correcting addendum after issue; W72 dialog and W73 issued-report view models), OtherReports (correspondence register from–to, payroll sheet, custody and inventory, late tasks, meetings and decisions; quick filters; PDF/CSV export; W74 view models). Tests: draft content from seeded data for every section, inclusion rules and per-record options, template sentences for 0/1/2/3–10/11+, previous-cycle comparison, hide/reorder/edit persistence, readiness gaps and percentage, issue locks the cycle and cannot repeat, the docx opens with OpenXML and contains no placeholder text, the frozen copy hash, send-to-higher-authority creates the numbered outgoing letter, addendum numbering, CSV escaping. Register everything in the Core DI extension and the desktop host.`,
  },
]

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, measurements, the public API the screen packages will consume).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output). Check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies; tests that skip must say why), warnings-as-errors clean, (6) Arabic-only user text without technical terms, RTL/bidi-safe strings. Do NOT modify any file (you may not write the progress file either). Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${COMMON}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
const ORDER = ['build', 'review1', 'fix', 'verify', 'fix2', 'verify2', 'fix3', 'verify3']
const PHASE = { build: 'Build', review1: 'Review', fix: 'Fix', verify: 'Verify', fix2: 'Fix2', verify2: 'Verify2', fix3: 'Fix3', verify3: 'Verify3' }
function isReview(st) { return st.startsWith('review') || st.startsWith('verify') }
// An agent that dies on a connection error returns null: start it again (the restart protocol makes the
// new attempt continue from the working tree and the progress file). Three attempts, then give up.
async function tryAgent(prompt, opts) {
  for (let n = 1; n <= 3; n++) {
    const r = await agent(prompt, n === 1 ? opts : { ...opts, label: `${opts.label}#${n}` })
    if (r) return r
    log(`${opts.label}: attempt ${n} returned nothing${n < 3 ? ' — starting it again' : ' — giving up'}`)
  }
  return null
}
// args = { skip: [keys already accepted], resume: { '<key>': { stage, file, state, rulings, extraPaths } } }
//   stage: build|review1|fix|verify|fix2|verify2|fix3|verify3 — the stage to run first;
//   file: archived report of the stage before it (handed over as {see: file}).
async function chainFrom(p0, r0) {
  const r = r0 || {}
  const p = { ...p0,
    paths: p0.paths + (r.extraPaths ? ', ' + r.extraPaths : ''),
    spec: p0.spec + (r.state ? '\n\nSTATE OF THIS PACKAGE: ' + r.state : '') + (r.rulings ? '\n\nSUPERVISOR RULINGS FOR THIS ROUND (final, do not reopen): ' + r.rulings : '') }
  const s = { key: p.key, accepted: false }
  let prev = r.file ? { see: r.file } : null
  for (let i = r.stage ? ORDER.indexOf(r.stage) : 0; i < ORDER.length; i++) {
    const st = ORDER[i]
    if (isReview(st)) {
      const round = ORDER.slice(0, i + 1).filter(isReview).length
      const rev = await tryAgent(reviewPrompt(p, prev, round), { label: `${st}:${p.key}`, phase: PHASE[st], schema: REVIEW_SCHEMA, model: 'opus', effort: round === 1 ? 'high' : 'medium' })
      s[st] = rev
      if (!rev) return { ...s, failed: st }
      if (rev.verdict === 'accept') return { ...s, accepted: true }
      prev = rev
    } else {
      const prompt = st === 'build' ? builderPrompt(p) : fixPrompt(p, prev)
      const out = await tryAgent(prompt, { label: `${st}:${p.key}`, phase: PHASE[st], schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s[st] = out
      if (!out) return { ...s, failed: st }
      prev = out
    }
  }
  return s
}
const RESUME = (args && args.resume) || {}
const SKIP = (args && args.skip) || []
log('B5 services: assets and custody → employees and payroll → finance → reports')
const results = []
for (const p of PACKAGES) {
  if (SKIP.includes(p.key)) { results.push({ key: p.key, skipped: true }); continue }
  log(`B5 services: ${p.key}${RESUME[p.key] ? ' (resuming at ' + RESUME[p.key].stage + ')' : ''}`)
  const s = await chainFrom(p, RESUME[p.key])
  results.push(s)
  if (!s.accepted) { log(`B5 services: ${p.key} not accepted${s.failed ? ' (stage ' + s.failed + ' died)' : ''} — stopping the sequence for the supervisor`); break }
}
return results
