export const meta = {
  name: 'alwakeel-line-admin-b2',
  description: 'Resumable line for the admin tool and the daily shell: admin-2 → admin-3 → admin consistency audit ∥ B2 screens → walkthrough/E2E → app consistency audit. Every package is built, reviewed, fixed and re-verified by Opus (up to three fix rounds); args.resume restarts any package at any stage from its archived report, args.skip drops accepted packages',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }, { title: 'Fix3' }, { title: 'Verify3' }],
}
// args = {
//   skip:   ['admin-2', ...]                         packages already accepted
//   resume: { '<key>': { stage, file, state, rulings, extraPaths } }
//           stage  one of build|review1|fix|verify|fix2|verify2|fix3|verify3 — the stage to run first
//           file   archived report of the stage before it (docs/build/reviews/...), handed over as {see: file}
//           state / rulings / extraPaths   appended to the package specification and paths
// }
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a component, a screen, a test file, a green build, one closed finding) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (1–2 MB each; they overload the connection). View the small JPEG previews under design/exports/preview/light/<group>/<name>.jpg and design/exports/preview/dark/<group>/<name>.jpg (same file names as the PNGs, .jpg extension; groups A, W, M, I), only the screens of your own package, each at most once per comparison, right before you build or check that screen. (5) Screenshots you take must be JPEG or PNG at most 1366 px wide and viewed once. (6) When a report is handed to you as {"see": "<path>"}, read that JSON file first — it is the archived report of the previous stage.

DESIGN CONSISTENCY (owner's rule, 2026-09-16 — «انتبه للتصميم: لا يوجد تناسق وتنظيم في المكونات»): every screen is composed from Wakeel.Design components (src/Wakeel.Design/Components: WButton, WInput, WSelect, WCard, WChip, WBadge, WTabs, WTable, WKvRow, WStateCard, WDialog, WMenu, WStepper, WTree/WTreeToolbar, WBanner, WHealthCard, …). Hand-rolled lists, trees, tables, tabs, chips, forms, cards or dialogs inside a page are forbidden; page CSS may only arrange design components on the page grid and set page-specific spacing from the tokens. If a component is missing, add it to Wakeel.Design first (component + css + gallery entry + bUnit test), then use it. The screen is done only when its host screenshot, put beside the preview, shows the same anatomy: same regions and columns, same controls in the same places, same chips and states, no overlap, no clipping, no staircase indentation, aligned rows, consistent spacing. A builder writes the side-by-side comparison of every screen in its notes (deviation → fixed / accepted with reason). A reviewer opens the builder's screenshot and the preview together and reports every visible deviation as a finding: high for a hand-rolled component, medium for a layout, alignment, overlap or missing-control deviation, low for a spacing nuance.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset/clean. Other agents edit other projects in the same working tree concurrently (a B3 services agent edits src/Wakeel.Core, src/Wakeel.Reports, src/Wakeel.Ocr, src/Wakeel.Search and tests/Wakeel.Core.Tests): build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. A package that touches a WPF host or Blazor pages is not done until the host actually runs: launch the built exe (Wakeel.Desktop: src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe; Wakeel.Admin: src/Wakeel.Admin/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Admin.exe) with --remote-debugging-port=<port> (9333 for Wakeel.Desktop, 9334 for Wakeel.Admin), --window-size=1366x768, --data-folder=<a temporary folder> and --start-url=/route where useful, confirm http://127.0.0.1:<port>/json/version answers (the endpoint may close after a minute of idling while the process stays healthy — drive the host in tight batches and relaunch when it stops answering), screenshot it over CDP (Page.captureScreenshot with a 1366x768 device-metrics override), LOOK at the screenshot, read the host log in <data-folder>/logs for errors, then kill the process immediately — a host left running is a defect, and so is relaunching it more often than the work needs (the owner sees every window). All user-facing text is Arabic with no technical terms or error codes (item 15), numerals are always Western digits 0–9 and never Arabic-Indic (design guide «الأرقام غربية», item 20), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere. Identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or UI state longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const HEAD = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true).`

const COMMON_ADMIN = `${HEAD} Governing documents: docs/build/packages/B1-admin-firstrun.md (the B1 specification, section B1-A), docs/build/ARCHITECTURE.md (§2–§5, §9, §10, §12 and the .wakeel-setup format paragraph in §4; the org-root certificate decision in §3), docs/build/DATA-MODEL.md (§1, §13), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items referenced by the spec), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Crypto (keys, wraps, certificates, containers, Setup/SetupPackageWriter+Reader+PackagePassword, pairing), src/Wakeel.Core (WakeelDb, DbSession, SchemaMigrator, services), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast services), src/Wakeel.Admin.UI and src/Wakeel.Admin (what admin-1 built — docs/build/progress/admin-1.md — and what admin-2 built — docs/build/progress/admin-2.md).\n\n${PROTOCOL}`

const COMMON_B2 = `${HEAD} Governing documents: docs/build/packages/B2-daily-shell.md (the B2 specification), docs/build/ARCHITECTURE.md (§9–§12), docs/build/DATA-MODEL.md (§1, §5), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items 15, 20, 21, 23, 26, 32, 41, 50, 52, 56), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Core (WakeelDb, DbSession, services, ClockCheckService, ArabicText, InstallationService, and the ACCEPTED B2 services: AttentionService, BadgeService, NotificationService, ReminderScheduler, ClockGuard, HealthService, QuickCaptureService, BackgroundPass, CoreAr — docs/build/progress/b2-services.md), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast), src/Wakeel.UI (shell, first-run/login/lock/recovery from B1 under Pages/FirstRun, Pages/Account and Services/Account; Services/Shell and the B2 pages from the b2-screens package — docs/build/progress/b2-screens.md), src/Wakeel.Desktop (host; flags --remote-debugging-port, --window-size, --start-url, --data-folder; boots into /first-run on an empty data folder and /login otherwise; Windows probes under Services/), tests/Wakeel.Walkthrough.Tests (FirstRunWorld activates a temporary installation) and tests/Wakeel.E2E (Playwright-over-CDP harness). Arabic strings only in src/Wakeel.Design/Text/Ar.*.cs partial files (UI) and src/Wakeel.Core/Services/CoreAr.cs (Core layer, ratified in ARCHITECTURE §12).\n\n${PROTOCOL}`

const COMMON_DESIGN = `${HEAD} Governing documents: docs/design/DESIGN-GUIDE.md (tokens, components, states, the admin-tool shell), docs/design/SCREENS.md (screen-by-screen content), docs/build/ARCHITECTURE.md §10 and §12, docs/AGREEMENT.md items 15 and 55, and the package specs docs/build/packages/B1-admin-firstrun.md and B2-daily-shell.md for what each screen must do. The design system is src/Wakeel.Design (components, tokens/css/js, Bidi, Ar strings); the gallery page src/Wakeel.UI/Pages/Gallery.razor shows every component and its states (route /gallery of Wakeel.Desktop). Hosts: Wakeel.Desktop (app; boots into /first-run on an empty --data-folder and /login on an activated one; tests/Wakeel.Walkthrough.Tests' FirstRunWorld and tests/Wakeel.UI.Tests/Shell/HostDataFolderSeed.cs activate and seed a temporary folder) and Wakeel.Admin (admin tool; --data-folder, --start-url).\n\n${PROTOCOL}`

const ADMIN_COMMON = `The admin tool «مدير نظام الوكيل» is built in three sequential sub-packages (admin-1: A01–A03 — ACCEPTED; admin-2: A04–A07; admin-3: export, maintenance, distribution, audit, dialogs, A08–A12). Shared conventions: src/Wakeel.Admin (WPF + BlazorWebView host: BlazorWebViewInitializing user-data folder, Serilog file log, --remote-debugging-port, --data-folder, --start-url, --window-size, titled «مدير نظام الوكيل», 1366x768, DPAPI protector, file dialogs, print-to-PDF, ProgramData folder C:\\ProgramData\\WakeelAdmin\\ with admin.db, keys, exports, staging, logs). src/Wakeel.Admin.UI (Blazor pages/components/services) references Wakeel.Design, Wakeel.Core and Wakeel.Crypto; admin.db is SQLCipher with the DATA-MODEL §13 schema as its own migration set (Migrations/). Arabic strings live in src/Wakeel.Admin.UI/Text/ as partial static class AdminAr split by area (add AdminAr.Export.cs, AdminAr.Maintenance.cs, … for your own areas). Services are registered per area through extension methods called from AddWakeelAdmin() (src/Wakeel.Admin.UI/ServiceCollectionExtensions.cs); routes are declared in src/Wakeel.Admin.UI/AdminRoutes.cs — add your routes to its Built set when the page exists. Shell: dark top bar + top tabs, no sidebar. Roles are manager|secretary|custodian (SetupRoles). Tests in tests/Wakeel.Admin.Tests use AdminTestContext (temp folder, TestClock, cheap Argon2). KNOWN, DO NOT REDO: the A05 structure tree is hand-rolled and will be rebuilt on the shared WTree component by the admin-consistency package that follows — an admin-2 reviewer records it as one low finding and does not block acceptance on it; admin-3 must not add any hand-rolled list/tree/table (WTree exists now). SUPERVISOR RULINGS (final, do not reopen): where the A previews disagree with the specification, the specification wins — one 21-character recovery code plus QR, no «تذكر هذا الجهاز» option, lock-out 30 s doubling; the previews will be corrected in the design later.`

const ADMIN_BUILD = 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build'
const APP_BUILD = 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build'

const PACKAGES = {
  'admin-2': {
    key: 'admin-2', title: 'B1-A2 (مدير النظام: المؤسسة والهيكلية والمكاتب والأجهزة والمفاتيح A04–A07)', model: 'opus', common: COMMON_ADMIN,
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: ADMIN_BUILD,
    security: 'device certificate issuance with a key seed that is stored only until the first export, office key issue/rotation, revocation list signing with the org root, logo size/type validation, letter/report template validation (docx only, placeholders detected by reading the XML — no macro execution), no secrets in admin.db clear text, logs or UI state, and the six admin-1 low findings closed',
    extraCheck: 'A04–A07 match their previews (light and dark) apart from the known A05 tree, structure rules (4 fixed levels, no duplicates per level, no delete with active devices), device numbering 1–9 unique per office, the letter template upload lists detected/unknown @ placeholders and shows an A4 and A5 PDF preview, the A03 dashboard reflects the real tables, and the Wakeel.Admin host was launched and rendered',
    spec: `${ADMIN_COMMON}\nScope: (a) A04 «المؤسسة والهوية»: org name, logo upload ≤ 2 MB with preview and square crop (a real crop implementation registered in the host), cycle start day 1–28 (item 52), numbering format YYYYMMDD/DESSS with a danger warning (item 5), optional report template (item 53), official letter template per item 57 with the nine placeholders, unknown ones flagged, A4/A5 preview, built-in default when none uploaded; (b) A05 «الهيكلية»: four fixed levels, add/edit/move/disable, head title and name, office inventory code (item 45), no duplicate names per level, no delete with active devices; (c) A06 «المكاتب والأجهزة»: devices 1–9 per office, roles, employee name/number, sync scope, phones read-only; (d) A07 «الحسابات والمفاتيح»: device account creation with seed kept until first export, custodian activation, account recovery, office key issue/rotate with warning, revoke (item 24) → signed revocation list; (e) routes registered, A03 counts live; (f) tests; (g) the six admin-1 lows.`,
  },
  'admin-3': {
    key: 'admin-3', title: 'B1-A3 (مدير النظام: التصدير والصيانة والتوزيع والسجل والحوارات A08–A12)', model: 'opus', common: COMMON_ADMIN,
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: ADMIN_BUILD,
    security: 'setup package password shown once and never logged, staging directory under the product folder (never %TEMP%) wiped after export, key seed removed from admin.db after the first export, AdminWrap use when opening a Wakeel backup or ProgramData, audit log has no secrets, CSV export escapes formulas',
    extraCheck: 'A08–A12 match their previews (light and dark), the exported .wakeel-setup opens with Wakeel.Crypto SetupPackageReader with every check Ok (including letter-template.docx when set), distribution regenerates files for every affected office, the whole admin tool A01–A12 works end to end in the running host (launched and rendered), no hand-rolled component, and nothing from admin-1/admin-2 regressed',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-3) builds on admin-1 and admin-2 (read docs/build/progress/admin-1.md and admin-2.md and their code before extending): (a) SetupExportService + A08 «تصدير ملف الإعداد»: 4-step wizard (WStepper) — office and device ← content (structure, logo, office key, device certificate and its seed, cycle day, numbering format, report template, letter template as letter-template.docx via SetupContent.includes.letterTemplate, guide PDF, revocation list) ← package password (PackagePassword.New(), 16 random characters shown once with a copy button and a warning) ← save <office>-<device>-<date>.wakeel-setup written by Wakeel.Crypto SetupPackageWriter with a staging directory under ProgramData\\WakeelAdmin\\staging; record the export in setup_exports and delete the device key seed from admin.db after the first export; provisional guide: if docs/guide/دليل-الوكيل.pdf is absent generate a one-page placeholder PDF at runtime into the exports folder (never write under docs/); (b) MaintenanceService + A09 «الصيانة»: open a Wakeel copy (ProgramData folder or a .wakeel-backup file) with the org key (AdminWrap) for viewing and verification, open a .wakeel-msg, recover a user account (new password wrap delivered through a recovery setup file), every dangerous action behind a red button and confirmation, with a log; (c) DistributionService + A10 «توزيع التحديثات»: list of undistributed structure/office changes (AdminPendingChanges from admin-2), regenerate updated setup files for all affected offices in one batch (each with its own password); (d) A11 «سجل العمليات»: WTable who/what/when without secrets, local search, CSV export; (e) A12: every remaining state and dialog (device revocation danger confirm, numbering-format change warning, corrupt file / wrong password error, export success, logo too large); register the routes in AdminRoutes.Built and enable the A03 header actions; (f) tests: export produces a file that SetupPackageReader opens with all checks Ok (with and without the letter template), seed removed after the first export, distribution regenerates files for affected offices only, maintenance opens a backup produced by Wakeel.Crypto test helpers, audit CSV has no secrets, A12 dialogs render. Final pass: launch Wakeel.Admin.exe (port 9334, temp --data-folder), walk A01→A12 once, screenshot A08/A09/A10/A11 and two A12 states light and dark, compare with the previews side by side (table in notes), kill it.`,
  },
  'admin-consistency': {
    key: 'admin-consistency', title: 'ADMIN-CONSISTENCY (A05 on WTree; A01–A12 audited against their previews)', model: 'opus', common: COMMON_DESIGN,
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: ADMIN_BUILD,
    security: 'no behaviour of the accepted account, key, structure, device, export, maintenance or distribution services changes; only presentation is rewritten',
    extraCheck: 'every admin screen A01–A12 put beside its preview shows the same anatomy; no page contains a hand-rolled list/tree/table/tabs/chip/card/dialog; the host was launched and every screen screenshotted light and dark',
    spec: `The owner rejected the A05 structure screen as built (2026-09-16 23:25: overlapping level chips, staircase indentation, clipped names, stray empty boxes, none of the preview's columns) and ordered consistency across all components. (1) Rebuild the A05 tree pane on the Wakeel.Design WTree (see docs/build/progress/design-tree.md and the gallery; A06's office rail already uses it): header «شجرة الهيكل التنظيمي» with the count badge, «توسيع الكل» / «طيّ الكل», search «ابحث باسم الوحدة أو رئيسها…» with the Ctrl+F hint, rows with handle / caret / icon / name / level chip / head name, states (selected, out of service dimmed, «بلا رئيس معيّن» warning), the footer hint «اسحب الوحدة داخل الشجرة لتغيير تبعيتها، أو استخدم زر «نقل وحدة».»; the page header with «إضافة وحدة تنظيمية» (primary) and «نقل وحدة» (secondary) and the subtitle line «هيئة … — N دوائر · N أقسام · N وحدات · آخر تعديل <date>»; the detail pane as the preview: title «قسم: <name>» with the level chip and the «تعديل غير محفوظ» chip, the path line «المسار: … · الرمز …», fields «اسم الوحدة *», «المستوى *», «التبعية (الأصل) *», «المسمى الوظيفي للرئيس», «رئيس الوحدة *», «الرمز» with the helper lines, the «التحقق من صحة الهيكل» card listing rule checks with warning/ok/info icons, and the footer actions «حفظ التعديل» / «إلغاء التعديل» / «حذف الوحدة» (danger) with the blocked-save explanation. Delete the hand-rolled AdminStructureNode component once nothing uses it. Keep every accepted service call and test behaviour; update AdminAreaScreenTests for the new anatomy. (2) Then audit EVERY admin screen on disk (A01–A12 — check src/Wakeel.Admin.UI/Pages): launch Wakeel.Admin.exe on a temporary --data-folder, walk the screens, screenshot each in light and dark, put each screenshot beside its preview (design/exports/preview/<theme>/A/…), list every visible deviation, and fix it: replace hand-rolled markup by Wakeel.Design components, align regions and columns, restore missing controls, chips and states, fix spacing from the tokens. Where the preview and the specification disagree, the specification wins (the supervisor rulings on A01/A02 stand: one recovery code + QR, no «تذكر هذا الجهاز», 30 s doubling lock-out) — say so in the notes instead of copying the preview. If a deviation comes from a Wakeel.Design component itself, do not patch it with page CSS: describe it in open_issues for the app-consistency package, which owns src/Wakeel.Design. (3) Report in notes a per-screen table: screen, deviations found, fixed / accepted (reason). Kill the host.`,
  },
  'b2-screens': {
    key: 'b2-screens', title: 'B2-SCREENS (W08–W12, W91, W92, W94)', model: 'opus', common: COMMON_B2,
    paths: 'src/Wakeel.UI, src/Wakeel.Design (Text/Ar.Shell.cs; component fixes named by the review; new components only if the spec needs them), tests/Wakeel.UI.Tests, src/Wakeel.Desktop (only: opening ms-settings:dateandtime, saving a file the user chose, Ctrl+N global shortcut wiring, DI lines in App.xaml.cs)',
    build: APP_BUILD,
    security: 'no MarkupString with user data, no technical text in any state card or banner, health report saved only where the user chooses, background passes dispatched to the UI through ComponentBase.InvokeAsync and stopped on sign-out and lock',
    extraCheck: 'each of the eight screens matches its preview (light and dark) in anatomy, texts and states; empty/loaded/error states exist; no hand-rolled component; every icon-only button has a tooltip; ClockGuard and ReminderScheduler actually run in the host (a seeded meeting reminder appears in the bell); the host was launched on each route with an activated temporary installation and screenshots viewed and compared side by side',
    spec: `Section «الشاشات (Wakeel.UI/Pages)» of docs/build/packages/B2-daily-shell.md on top of the accepted B2 services: W08 attention center with live data (KPI cards open W09 on the right tab; monthly-report banner from ReadinessService if it exists else 0; «يحتاج إجراءً اليوم» mixed rows with type chip, subject/number, party, assignee, semantic due colour, local search and badge tabs; phone expenses pending confirmation with employee name and direct confirm/reject (item 50); side column: today's meetings, last sync, backup state; «إدخال سريع» and «تحديث»), W09 overdue/near/stale/pending tabs with badges, local search, WTable and «معالجة», W10 notification panel (380 px dropdown from the bell, tabs all/unread N from the real counts, items with type icon and relative time, click opens the record, mark all read, settings link to W86), W11 clock banner (WBannerClock with the exact texts, «تصحيح الساعة» opens ms-settings:dateandtime through the desktop host, «تجاهل مؤقتًا» with tooltip), W12 health center (WHealthCard grid from HealthService, «فحص شامل الآن», «تصدير تقرير الصحة» saving an Arabic text file, filter tabs, local search), W91 standard states reference page, W92 standard dialogs reference page, W94 quick capture dialog on Ctrl+N (task/note/report-note/expense/appointment, validation, immediate save, toast with «تراجع» — a design-system toast-with-action, not a page-local one). Shell wiring: ClockGuard/ReminderScheduler start on sign-in/unlock and stop on lock/sign-out with ComponentBase.InvokeAsync as the dispatcher, badge subscription, bell, Ctrl+N. Also the three low findings of docs/build/reviews/B2/verify-b2-carryover.json. Every screen from Wakeel.Design components only. The W08 banner in the preview says «دورة أكتوبر»; the correct text follows the current financial cycle.`,
  },
  'b2-walkthrough': {
    key: 'b2-walkthrough', title: 'B2-WALKTHROUGH (service scenario + E2E screenshots)', model: 'sonnet', common: COMMON_B2,
    paths: 'tests/Wakeel.Walkthrough.Tests, tests/Wakeel.E2E',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Walkthrough.Tests --no-dependencies && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet build tests/Wakeel.E2E --no-dependencies && dotnet test tests/Wakeel.E2E --no-build',
    security: 'tests use temporary folders only and never touch C:\\ProgramData\\Wakeel of the real installation',
    extraCheck: 'the walkthrough asserts real counts and timings through the public services, and the E2E screenshots of W08–W12 are produced against the design PNGs with recorded diff percentages and per-screen upper bounds',
    spec: `Build section «القبول» of docs/build/packages/B2-daily-shell.md: (1) tests/Wakeel.Walkthrough.Tests scenario on a temporary activated installation (reuse FirstRunWorld): create overdue, near-due and stale records plus pending phone expenses → AttentionService/BadgeService return the exact counts → a meeting with a reminder fires the notification in the bell at the right minute (drive the scheduler clock; check the item-56 default and a per-meeting override) → the clock guard blocks numbering when the clock is set back and releases after correction → quick capture saves and undo removes. (2) tests/Wakeel.E2E: extend the harness with screenshot tests for W08, W09, W10 (panel open), W11 (banner shown via the seeded skewed clock of tests/Wakeel.UI.Tests/Shell/HostDataFolderSeed.cs or an equivalent seed), W12, W91, W92, W94 at 1366x768 light and dark, comparing programmatically with design/exports/{light,dark}/W/*.png (the test code may read the PNGs; you yourself look only at the small previews and at your own screenshots) and recording diff percentages in the test output and tests/Wakeel.E2E/artifacts/ (git-ignored); assert an upper bound per screen; the host honours --data-folder — point it at a temporary folder activated and seeded through the walkthrough helpers and sign in over CDP (port 9333). Report the diff percentages per screen in notes.`,
  },
  'app-consistency': {
    key: 'app-consistency', title: 'APP-CONSISTENCY (W02–W12, W91, W92, W94, gallery audited against their previews)', model: 'opus', common: COMMON_DESIGN,
    paths: 'src/Wakeel.UI, src/Wakeel.Design (component fixes only where a deviation comes from the component itself), tests/Wakeel.UI.Tests',
    build: APP_BUILD,
    security: 'no behaviour of the accepted account, activation, lock, recovery or daily-shell services changes; only presentation is corrected',
    extraCheck: 'every app screen that exists (first run W02–W04, login/lock/recovery W05–W07, daily shell W08–W12, W91, W92, W94, and the gallery) put beside its preview shows the same anatomy; no page contains a hand-rolled list/tree/table/tabs/chip/card/dialog; the host was launched on an activated temporary installation and every screen screenshotted light and dark',
    spec: `Audit every screen of the app that exists on disk (check src/Wakeel.UI/Pages, Pages/FirstRun, Pages/Account): activate a temporary data folder (tests/Wakeel.UI.Tests/Shell/HostDataFolderSeed.cs or tests/Wakeel.Walkthrough.Tests' FirstRunWorld), launch Wakeel.Desktop.exe on it, sign in over CDP, open each route (and the first-run routes on an empty folder), screenshot each screen in light and dark, put each screenshot beside its preview (design/exports/preview/<theme>/W/…), list every visible deviation and fix it: replace hand-rolled markup by Wakeel.Design components (or fix the component when the deviation comes from it, with a gallery check), align regions and columns, restore missing controls, chips and states, fix spacing from the tokens, keep RTL/bidi correct. Where the preview and the specification or a supervisor ruling disagree, the specification wins (e.g. the W08 banner follows the current financial cycle, not «دورة أكتوبر») — say so in the notes. Update bUnit tests that pinned the old markup. ALSO (a) close the four low findings of docs/build/reviews/DESIGN/verify2-design-tree.json on the accepted WTree component (keyboard keys prevented from scrolling the page, hint hidden in the no-match state, gallery section heading «الشجرة», depth-2 level chip variant matching the A05 preview); (b) give WDialog a ConfirmDisabled parameter so that pages stop disabling the confirm button through CSS, make WCard's Neutral variant honour Desc, and stop WTable painting both scroll tracks when only one overflows — each with a gallery state and a bUnit test; (c) read the open_issues of the admin-consistency report if docs/build/progress/admin-consistency.md exists and fix the component-level deviations it lists. Report in notes a per-screen table: screen, deviations found, fixed / accepted (reason). Kill the host.`,
  },
}

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${p.common}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, measurements, the per-screen side-by-side comparison table).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${p.common}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output). VISUAL FIDELITY CHECK: launch the host yourself (once per theme where possible), screenshot every screen the package covers in light and dark, open each screenshot together with its preview, and report every visible deviation as a finding (high: a hand-rolled component where a Wakeel.Design one exists or should exist; medium: layout, alignment, overlap, clipping, missing control/column/chip/state; low: spacing nuance). Also check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. From round 2 on, first confirm that each finding of the previous round is really closed, then look for regressions; do not reopen supervisor rulings. Do NOT modify any file (you may not write the progress file either). Kill the host when done. Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${p.common}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green, re-screenshot what you changed and compare again. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}

const ORDER = ['build', 'review1', 'fix', 'verify', 'fix2', 'verify2', 'fix3', 'verify3']
const PHASE = { build: 'Build', review1: 'Review', fix: 'Fix', verify: 'Verify', fix2: 'Fix2', verify2: 'Verify2', fix3: 'Fix3', verify3: 'Verify3' }
function isReview(st) { return st.startsWith('review') || st.startsWith('verify') }
// An agent that dies on a connection error returns null: start it again (the restart protocol makes the
// new attempt continue from the working tree and the progress file). Three attempts, then give up.
async function tryAgent(prompt, opts) {
  for (let n = 1; n <= 3; n++) {
    const r = await agent(prompt, n === 1 ? opts : { ...opts, label: `${opts.label}#${n}` })
    if (r) return r
    log(`${opts.label}: attempt ${n} returned nothing${n < 3 ? ' — starting it again' : ' — giving up'}`)
  }
  return null
}
async function chainFrom(p0, r0) {
  const r = r0 || {}
  const p = { ...p0,
    paths: p0.paths + (r.extraPaths ? ', ' + r.extraPaths : ''),
    spec: p0.spec + (r.state ? '\n\nSTATE OF THIS PACKAGE: ' + r.state : '') + (r.rulings ? '\n\nSUPERVISOR RULINGS FOR THIS ROUND (final, do not reopen): ' + r.rulings : '') }
  const s = { key: p.key, accepted: false }
  let prev = r.file ? { see: r.file } : null
  for (let i = r.stage ? ORDER.indexOf(r.stage) : 0; i < ORDER.length; i++) {
    const st = ORDER[i]
    if (isReview(st)) {
      const round = ORDER.slice(0, i + 1).filter(isReview).length
      const rev = await tryAgent(reviewPrompt(p, prev, round), { label: `${st}:${p.key}`, phase: PHASE[st], schema: REVIEW_SCHEMA, model: 'opus', effort: round === 1 ? 'high' : 'medium' })
      s[st] = rev
      if (!rev) return { ...s, failed: st }
      if (rev.verdict === 'accept') return { ...s, accepted: true }
      prev = rev
    } else {
      const prompt = st === 'build' ? builderPrompt(p) : fixPrompt(p, prev)
      const out = await tryAgent(prompt, { label: `${st}:${p.key}`, phase: PHASE[st], schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s[st] = out
      if (!out) return { ...s, failed: st }
      prev = out
    }
  }
  return s
}
const RESUME = (args && args.resume) || {}
const SKIP = (args && args.skip) || []
async function sequence(name, keys) {
  const out = []
  for (const key of keys) {
    if (SKIP.includes(key)) { out.push({ key, skipped: true }); continue }
    log(`${name}: ${key}${RESUME[key] ? ' (resuming at ' + RESUME[key].stage + ')' : ''}`)
    const s = await chainFrom(PACKAGES[key], RESUME[key])
    out.push(s)
    if (!s.accepted) { log(`${name}: ${key} not accepted${s.failed ? ' (stage ' + s.failed + ' died)' : ''} — stopping this sequence for the supervisor`); break }
  }
  return out
}
log('admin (admin-2 → admin-3 → admin consistency) ∥ app (B2 screens → walkthrough → app consistency)')
const results = await parallel([
  () => sequence('admin', ['admin-2', 'admin-3', 'admin-consistency']),
  () => sequence('app', ['b2-screens', 'b2-walkthrough', 'app-consistency']),
])
return { admin: results[0], app: results[1] }
