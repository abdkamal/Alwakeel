export const meta = {
  name: 'alwakeel-b2b-design-consistency',
  description: 'Design consistency pass ordered by the owner (2026-09-16): a shared WTree component in Wakeel.Design, then every built screen of the admin tool and of the app audited side by side against its preview and corrected; hand-rolled markup replaced by design-system components; Opus builds and reviews',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a component, a screen, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (1–2 MB each; they overload the connection). View the small JPEG previews under design/exports/preview/light/<group>/<name>.jpg and design/exports/preview/dark/<group>/<name>.jpg (same file names as the PNGs, .jpg extension; groups A, W, M, I), only the screens of your own package, each at most once per comparison, right before you build or check that screen. (5) Screenshots you take must be JPEG or PNG at most 1366 px wide and viewed once.

DESIGN CONSISTENCY (owner's rule, 2026-09-16 — «انتبه للتصميم: لا يوجد تناسق وتنظيم في المكونات»): every screen is composed from Wakeel.Design components (src/Wakeel.Design/Components: WButton, WInput, WSelect, WCard, WChip, WBadge, WTabs, WTable, WKvRow, WStateCard, WDialog, WMenu, WStepper, WTree, …). Hand-rolled lists, trees, tables, tabs, chips, forms, cards or dialogs inside a page are forbidden; page CSS may only arrange design components on the page grid and set page-specific spacing from the tokens. If a component is missing, add it to Wakeel.Design first (component + css + gallery entry + bUnit test), then use it. The screen is done only when its host screenshot, put beside the preview, shows the same anatomy: same regions and columns, same controls in the same places, same chips and states, no overlap, no clipping, no staircase indentation, aligned rows, consistent spacing. A builder writes the side-by-side comparison of every screen in its notes (deviation → fixed / accepted with reason). A reviewer opens the builder's screenshot and the preview together and reports every visible deviation as a finding: high for a hand-rolled component, medium for a layout, alignment, overlap or missing-control deviation, low for a spacing nuance.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset/clean. Another agent may edit other projects in the same working tree concurrently: build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. A package that touches a WPF host or Blazor pages is not done until the host actually runs: launch the built exe (Wakeel.Desktop: src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe; Wakeel.Admin: src/Wakeel.Admin/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Admin.exe) with --remote-debugging-port=<port> (9333 for Wakeel.Desktop, 9334 for Wakeel.Admin), --window-size=1366x768, --data-folder=<a temporary folder> and --start-url=/route where useful, confirm http://127.0.0.1:<port>/json/version answers, screenshot it over CDP (Page.captureScreenshot), LOOK at the screenshot, read the host log in <data-folder>/logs for errors, then kill the process immediately — a host left running is a defect. All user-facing text is Arabic with no technical terms or error codes (item 15), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere. Identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or UI state longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/design/DESIGN-GUIDE.md (tokens, components, states, the admin-tool shell), docs/design/SCREENS.md (screen-by-screen content), docs/build/ARCHITECTURE.md §10 and §12, docs/AGREEMENT.md items 15 and 55, and the package specs docs/build/packages/B1-admin-firstrun.md and B2-daily-shell.md for what each screen must do. The design system is src/Wakeel.Design (components, tokens/css/js, Bidi, Ar strings); the gallery page src/Wakeel.UI/Pages/Gallery.razor shows every component and its states (route /gallery of Wakeel.Desktop). Hosts: Wakeel.Desktop (app; boots into /first-run on an empty --data-folder and /login on an activated one; tests/Wakeel.Walkthrough.Tests' FirstRunWorld activates a temporary folder) and Wakeel.Admin (admin tool; --data-folder, --start-url).\n\n${PROTOCOL}`

const PACKAGES = {
  tree: {
    key: 'design-tree', title: 'DESIGN-TREE (shared WTree component in Wakeel.Design)', model: 'opus',
    paths: 'src/Wakeel.Design/Components (new WTree*.razor/.css and, only if needed, additions to Enums.cs), src/Wakeel.Design/Text/Ar.Common.cs (additions only), src/Wakeel.UI/Pages/Gallery.razor and Gallery.razor.css (a new WTree section only), tests/Wakeel.UI.Tests/Components/WTreeTests.cs',
    build: 'dotnet build src/Wakeel.Design --no-dependencies && dotnet build src/Wakeel.UI --no-dependencies && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build && dotnet build src/Wakeel.Desktop',
    security: 'no MarkupString with caller data; search highlighting built from text nodes, never from concatenated HTML',
    extraCheck: 'the gallery section renders a four-level sample tree in light and dark exactly with the row anatomy of the A05 preview (design/exports/preview/light/A/A05 — الهيكلية.jpg): handle, caret, icon, name, level chip, trailing head name, states; keyboard navigation works in the running host',
    spec: `Build a generic tree component for the design system, modelled on the tree pane of the A05 preview and reusable by the organisation structure (A05), unit pickers, document folders and any later hierarchy. API: WTree<TItem> with Items (roots), ChildrenOf, KeyOf, a Row render fragment or the built-in row anatomy — trailing drag handle (optional, shown when Movable), caret for branches (hidden for leaves, rotates when open), leading icon (per node), name (Bidi-wrapped), level chip (WChip variant per level), trailing meta text (e.g. the head's name), optional trailing WBadge/state chip (e.g. «بلا رئيس معيّن» in the warning colour, out-of-service dimmed), Selected key + SelectedChanged, Expanded keys + ExpandAll()/CollapseAll(), Search text with match highlighting and a «لا عنصر بهذا الاسم» state from Ar strings, RTL indentation by depth through one CSS custom property (no inline pixel maths), hover/selected/focus states from the tokens, full-width rows aligned on one grid (no staircase), ARIA tree/treeitem/group roles, keyboard: ArrowUp/Down move, ArrowLeft/Right open/close in RTL, Enter selects, Home/End. Also a small WTreeToolbar or documented pattern for the header row of the preview: title + count badge, «توسيع الكل» / «طيّ الكل» text buttons, search input with the Ctrl+F kbd hint. Add the gallery section («الشجرة») with a four-level sample (هيئة → دائرة → قسم → وحدة) showing every state, in light and dark. bUnit tests: rendering roots and nested groups, expand/collapse and ExpandAll/CollapseAll, selection callback, search filter keeps ancestors of matches and highlights the match, empty-search state text, keyboard moves selection, mixed Arabic/English names isolated, level chip per depth. Launch Wakeel.Desktop with --start-url=/gallery on an empty temp --data-folder (the gallery stays reachable without an installation), screenshot the tree section light and dark, compare with the A05 preview's tree pane, kill.`,
  },
  admin: {
    key: 'admin-consistency', title: 'ADMIN-CONSISTENCY (A05 on WTree; A01–A12 audited against their previews)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'no behaviour of the accepted account, key, structure, device or export services changes; only presentation is rewritten',
    extraCheck: 'every admin screen that exists (A01 up to whatever admin-3 has built) put beside its preview shows the same anatomy; no page contains a hand-rolled list/tree/table/tabs/chip/card/dialog; the host was launched and every screen screenshotted light and dark',
    spec: `The owner rejected the A05 structure screen as built (2026-09-16 23:25: overlapping level chips, staircase indentation, clipped names, stray empty boxes, none of the preview's columns) and ordered consistency across all components. (1) Rebuild the A05 tree pane on the new Wakeel.Design WTree (see docs/build/progress/design-tree.md and the gallery): header «شجرة الهيكل التنظيمي» with the count badge, «توسيع الكل» / «طيّ الكل», search «ابحث باسم الوحدة أو رئيسها…» with the Ctrl+F hint, rows with handle / caret / icon / name / level chip / head name, states (selected, out of service dimmed, «بلا رئيس معيّن» warning), the footer hint «اسحب الوحدة داخل الشجرة لتغيير تبعيتها، أو استخدم زر «نقل وحدة».»; the page header with «إضافة وحدة تنظيمية» (primary) and «نقل وحدة» (secondary) and the subtitle line «هيئة … — N دوائر · N أقسام · N وحدات · آخر تعديل <date>»; the detail pane as the preview: title «قسم: <name>» with the level chip and the «تعديل غير محفوظ» chip, the path line «المسار: … · الرمز …», fields «اسم الوحدة *», «المستوى *», «التبعية (الأصل) *», «المسمى الوظيفي للرئيس», «رئيس الوحدة *», «الرمز» with the helper lines, the «التحقق من صحة الهيكل» card listing rule checks with warning/ok/info icons, and the footer actions «حفظ التعديل» / «إلغاء التعديل» / «حذف الوحدة» (danger) with the blocked-save explanation. Keep every accepted service call and test behaviour; update AdminAreaScreenTests for the new anatomy. (2) Then audit EVERY admin screen that exists on disk (A01–A07 built; A08–A12 if admin-3 has landed them — check src/Wakeel.Admin.UI/Pages): launch Wakeel.Admin.exe on a temporary --data-folder, walk the screens, screenshot each in light and dark, put each screenshot beside its preview (design/exports/preview/<theme>/A/…), list every visible deviation, and fix it: replace hand-rolled markup by Wakeel.Design components, align regions and columns, restore missing controls, chips and states, fix spacing from the tokens. Where the preview and the specification disagree, the specification wins (the supervisor rulings on A01/A02 stand: one recovery code + QR, no «تذكر هذا الجهاز», 30 s doubling lock-out) — say so in the notes instead of copying the preview. (3) Report in notes a per-screen table: screen, deviations found, fixed / accepted (reason). Kill the host.`,
  },
  app: {
    key: 'app-consistency', title: 'APP-CONSISTENCY (W02–W12, W91, W92, W94, gallery audited against their previews)', model: 'opus',
    paths: 'src/Wakeel.UI, src/Wakeel.Design (component fixes only where a deviation comes from the component itself), tests/Wakeel.UI.Tests',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'no behaviour of the accepted account, activation, lock, recovery or daily-shell services changes; only presentation is corrected',
    extraCheck: 'every app screen that exists (first run W02–W04, login/lock/recovery W05–W07, daily shell W08–W12, W91, W92, W94 as far as built, and the gallery) put beside its preview shows the same anatomy; no page contains a hand-rolled list/tree/table/tabs/chip/card/dialog; the host was launched on an activated temporary installation and every screen screenshotted light and dark',
    spec: `Audit every screen of the app that exists on disk (check src/Wakeel.UI/Pages, Pages/FirstRun, Pages/Account): activate a temporary data folder with tests/Wakeel.Walkthrough.Tests' FirstRunWorld (or a small helper), launch Wakeel.Desktop.exe on it, sign in over CDP, open each route (and the first-run routes on an empty folder), screenshot each screen in light and dark, put each screenshot beside its preview (design/exports/preview/<theme>/W/…), list every visible deviation and fix it: replace hand-rolled markup by Wakeel.Design components (or fix the component when the deviation comes from it, with a gallery check), align regions and columns, restore missing controls, chips and states, fix spacing from the tokens, keep RTL/bidi correct. Where the preview and the specification or a supervisor ruling disagree, the specification wins (e.g. the W08 banner follows the current financial cycle, not «دورة أكتوبر») — say so in the notes. Update bUnit tests that pinned the old markup. Report in notes a per-screen table: screen, deviations found, fixed / accepted (reason). Kill the host.`,
  },
}

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (the per-screen side-by-side comparison table and design decisions).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below, then read every file under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output). VISUAL FIDELITY CHECK (the core of this review): launch the host yourself, screenshot every screen the package covers in light and dark, open each screenshot together with its preview, and report every visible deviation as a finding (high: a hand-rolled component where a Wakeel.Design one exists or should exist; medium: layout, alignment, overlap, clipping, missing control/column/chip/state; low: spacing nuance). Also check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. Do NOT modify any file (you may not write the progress file either). Kill the host when done. Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${COMMON}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green, re-screenshot the screens you changed and compare again. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
function accepted(s) { const last = s && (s.verify2 || s.verify || s.review1); return !!(last && last.verdict === 'accept') }
async function chain(p) {
  const build = await agent(builderPrompt(p), { label: `build:${p.key}`, phase: 'Build', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
  if (!build) return { key: p.key, failed: 'build' }
  const review1 = await agent(reviewPrompt(p, build, 1), { label: `review1:${p.key}`, phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })
  const s = { key: p.key, build, review1 }
  if (review1 && review1.verdict === 'fix') {
    s.fix = await agent(fixPrompt(p, review1), { label: `fix:${p.key}`, phase: 'Fix', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
    s.verify = await agent(reviewPrompt(p, s.fix, 2), { label: `verify:${p.key}`, phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    if (s.verify && s.verify.verdict === 'fix') {
      s.fix2 = await agent(fixPrompt(p, s.verify), { label: `fix2:${p.key}`, phase: 'Fix2', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s.verify2 = await agent(reviewPrompt(p, s.fix2, 3), { label: `verify2:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    }
  }
  return s
}
log('design consistency: WTree component → admin screens ∥ app screens')
const tree = await chain(PACKAGES.tree)
if (!accepted(tree)) { log('WTree not accepted — stopping for the supervisor'); return { tree } }
const both = await parallel([() => chain(PACKAGES.admin), () => chain(PACKAGES.app)])
return { tree, admin: both[0], app: both[1] }
