export const meta = {
  name: 'alwakeel-b1-admin-firstrun',
  description: 'B1: the administration tool (A01–A12) and the first run / login / lock / recovery screens (W02–W07) built in parallel by Opus, plus three small polish packages (crypto, design, core) by Sonnet; each reviewed, fixed and re-verified by Opus',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }],
}
const REPO = 'D:/AI/Administration2'
const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents you MUST read first: docs/build/packages/B1-admin-firstrun.md (the B1 specification), docs/build/ARCHITECTURE.md (§2–§5, §9, §10, §12 and the .wakeel-setup format paragraph in §4; the org-root certificate decision in §3), docs/build/DATA-MODEL.md (§1, §13), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items referenced by the spec), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md; the reference screenshots are PNGs under design/exports/light and design/exports/dark — VIEW every screen you build (light and dark) and match it. Foundation you build on (read the public API before designing): src/Wakeel.Crypto (keys, wraps, certificates, containers, Setup/SetupPackageWriter+Reader+PackagePassword, pairing), src/Wakeel.Core (WakeelDb, DbSession, SchemaMigrator, services), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast services, AddWakeelDesign()), src/Wakeel.UI (app shell), src/Wakeel.Desktop (host; flags --remote-debugging-port, --window-size, --start-url), tests/Wakeel.E2E (Playwright-over-CDP harness). Never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, stop and report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/, or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset. Other agents edit other projects in the same working tree concurrently: build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a transient file lock caused by another agent's concurrent build, wait 60 seconds and retry. A package that touches a WPF host or Blazor pages is not done until the host actually runs: launch the built exe (Wakeel.Desktop: src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe; Wakeel.Admin: src/Wakeel.Admin/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Admin.exe) with --remote-debugging-port=9333 (Wakeel.Desktop also accepts --start-url=/route and --window-size=1366x768), confirm http://127.0.0.1:9333/json/version answers, screenshot it (Playwright over CDP, or PowerShell CopyFromScreen), LOOK at the screenshot, read the host log for errors, then kill the process — a build that compiles but does not render is a failure. All user-facing text is Arabic with no technical terms or error codes (item 15), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere. Identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or UI state longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const PACKAGES = [
  {
    key: 'admin', title: 'B1-A (مدير نظام الوكيل: A01–A12)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'admin password and recovery code handling (Argon2id wraps via Wakeel.Crypto, no clear-text secrets in admin.db, logs or UI state), org key generation and storage, org-root certificate and device certificate issuance, revocation list signing, setup package password shown once, DPAPI use, staging directory under the product folder (never %TEMP%), file dialogs and paths',
    extraCheck: 'every screen A01–A12 matches its PNG (light and dark) with the admin-tool shell (dark top bar + top tabs, no sidebar), all texts Arabic via the AdminAr class, the exported .wakeel-setup opens with Wakeel.Crypto SetupPackageReader with every check Ok, and the Wakeel.Admin host was actually launched and rendered',
    spec: `Build section «B1-A» of docs/build/packages/B1-admin-firstrun.md completely (A01–A12) in src/Wakeel.Admin.UI (Blazor pages/components/services) and src/Wakeel.Admin (WPF + BlazorWebView host modelled on Wakeel.Desktop — same BlazorWebViewInitializing user-data-folder pattern, Serilog file log, --remote-debugging-port flag — titled «مدير نظام الوكيل», 1366x768, DPAPI IPlatformProtector, file open/save dialogs, print-to-PDF through CoreWebView2.PrintToPdfAsync, ProgramData folder C:\\ProgramData\\WakeelAdmin\\ with admin.db, keys, exports, staging, logs). Wakeel.Admin.UI references Wakeel.Design (components + Ar), Wakeel.Core (reuse DbConnectionFactory/SchemaMigrator/SQLCipher helpers; add the admin schema of DATA-MODEL §13 as its own migration set inside Wakeel.Admin.UI) and Wakeel.Crypto. Admin-tool Arabic strings live in src/Wakeel.Admin.UI/Text/AdminAr.cs (static class AdminAr, grouped like Ar). Deliverables: the 12 screens with their states, services (AdminDb, OrgService, StructureService, DeviceService, KeyService (org root certificate via DeviceCertificate.IssueOrgRoot, device certificates, revocation list), SetupExportService using Wakeel.Crypto SetupPackageWriter with PackagePassword.New() and a staging directory under ProgramData\\WakeelAdmin\\staging, MaintenanceService (open a Wakeel backup/ProgramData with AdminWrap, open a .wakeel-msg), DistributionService (A10), AuditService (A11 with CSV export)), lock-out after 5 failed logins (30 s doubling), recovery by code or QR image (ZXing already pinned), provisional guide PDF: if docs/guide/دليل-الوكيل.pdf is absent generate a one-page placeholder PDF at runtime into the exports folder (do not write under docs/). Roles are manager|secretary|custodian (SetupRoles). A04 also accepts the official letter template (docx with @ placeholders per AGREEMENT item 57; show the detected/unknown placeholders and a PDF preview; include it in the setup export as letter-template.docx once the crypto-polish package adds SetupContent.includes.letterTemplate — read src/Wakeel.Crypto/Setup before wiring and fall back to omitting it if the field is not there yet, reporting that in open_issues). Tests in tests/Wakeel.Admin.Tests (bUnit for screens/states, service tests on a temp admin.db): account creation + recovery sheet, login lock-out, structure rules (4 fixed levels, no duplicates per level, no delete with active devices), device numbering 1–9 unique per office, certificate issuance + revocation list, setup export produces a file that SetupPackageReader opens with all checks Ok and the seed is removed from admin.db after the first export, distribution regenerates files for affected offices, audit log has no secrets. Launch Wakeel.Admin.exe, screenshot A01/A03/A08 light and dark, and report the screen-by-screen fidelity in notes.`,
  },
  {
    key: 'firstrun', title: 'B1-W (التشغيل الأول والدخول: W02–W07)', model: 'opus',
    paths: 'src/Wakeel.UI, src/Wakeel.Desktop, src/Wakeel.Core (additions only: services/entities the activation needs), src/Wakeel.Design/Text/Ar.FirstRun.cs (new partial file only), tests/Wakeel.Walkthrough.Tests, tests/Wakeel.Core.Tests (additions only), tests/Wakeel.UI.Tests (additions only)',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Walkthrough.Tests && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet test tests/Wakeel.Core.Tests --no-build && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'account password → PasswordWrap/RecoveryWrap/MachineWrap (DPAPI)/AdminWrap creation exactly as ARCHITECTURE §3, DbKey/VaultKey never written in clear, setup package verified before anything is trusted (org key pinned on activation, staging directory under ProgramData\\Wakeel\\staging never %TEMP%), lock-out counters, recovery code entry, temp setup file deletion, no secrets in logs or UI state',
    extraCheck: 'W02–W07 match their PNGs (light and dark), the first-run window has no sidebar and shows the approved brand, the walkthrough test drives the whole path on a temporary ProgramData folder, and the desktop host boots into W02 when no installation exists and into W05 otherwise (launched and screenshotted)',
    spec: `Build section «B1-W» of docs/build/packages/B1-admin-firstrun.md completely (W02–W07). Placement: Wakeel.UI gains a ProjectReference to Wakeel.Crypto; the account services live in src/Wakeel.UI/Services/Account (SetupInspectionService wrapping SetupPackageReader.Inspect for W02–W03 with a staging directory under the product folder, ActivationService creating the database from the setup content — installation (role manager|secretary|custodian), org_units, devices, settings, pinned org key — and the InstallationKeyFile with the three wraps + AdminWrap, LoginService with attempt counter and temporary lock-out, LockService with the idle timer from settings (default 10 min) and the MachineWrap unlock, RecoveryService re-creating PasswordWrap from the recovery code with an optional new recovery sheet); Wakeel.Core gets only what is generic (e.g. an InstallationService/SettingsService if missing). Wakeel.Desktop supplies DpapiPlatformProtector (IPlatformProtector), ProgramData paths (C:\\ProgramData\\Wakeel\\{data,vault,keys,models,packages,backups,logs,staging}, created with user write access; honour a --data-folder=<path> flag so tests and the E2E harness can point the host at a temporary folder), PrintToPdf via CoreWebView2 for the recovery sheet, drag-and-drop of a .wakeel-setup file onto W02, and the route decision at startup (no installation → /first-run, else → /login; the gallery stays reachable at /gallery). Screens: W02 (setup file + package password + «فحص الحزمة» + «أين أجد الملف؟» tooltip), W03 (live check list from SetupCheckResult, org card with logo, «تفعيل هذه النسخة», all error states listed in the spec), W04 (account password with strength meter + confirm, recovery sheet shown once with code + QR (QRCoder), print/PDF, «طبعتها وحفظتها» checkbox gating «ابدأ العمل»), W05 (login with org logo/name/office, employee name/avatar, password show/hide, «نسيت كلمة المرور؟ استخدم ورقة الاسترداد», clock status line from ClockCheckService), W06 (lock overlay over the current screen after idle, password, «متابعة», drafts-saved note), W07 (wrong password state + attempt counter + temporary lock, recovery dialog with manual code or QR image via ZXing, new password, new PasswordWrap, optional new recovery sheet). Arabic strings in src/Wakeel.Design/Text/Ar.FirstRun.cs as partial class Ar. Tests: tests/Wakeel.Walkthrough.Tests drives the full path on a temporary folder: a setup package produced with Wakeel.Crypto SetupPackageWriter (standing in for the admin tool, which is built in parallel) → inspection shows every check Ok → activation creates the database and key file → login succeeds/fails/locks out → lock and unlock → recovery with the code sets a new password and the old one stops working; each step asserts database rows, files on disk and audit_log entries, and that no secret appears in the log file or the database in clear text. bUnit tests for the six screens and their states (including the mixed Arabic/English lines). Launch the host with --data-folder pointing at an empty temp folder (W02) and at an activated one (W05), screenshot light and dark, and report the screen-by-screen fidelity in notes.`,
  },
  {
    key: 'crypto-polish', title: 'B1-POLISH-CRYPTO (setup reader/writer hardening)', model: 'sonnet',
    paths: 'src/Wakeel.Crypto, tests/Wakeel.Crypto.Tests',
    build: 'dotnet build src/Wakeel.Crypto && dotnet test tests/Wakeel.Crypto.Tests',
    security: 'no payload of a refused setup file can be read, cleartext staging never lands in %TEMP%, KDF cost cannot be weakened in production, manifest reads are bounded',
    extraCheck: 'every item closed with a real test; all 212 existing tests still green; public API changes are minimal and documented in notes for the B1 admin/first-run builders',
    spec: `Apply findings 1, 2, 5, 6, 7 and 8 of docs/build/reviews/B0.5/review1-setup-format.json (read the file for the precise text; findings 3 and 4 are already settled in the docs): (1) SetupPackage.Read/ReadLogo/ReadGuide/ReadReportTemplate/ExtractTo call Checks.EnsureAcceptable() first; expose an explicitly named ReadLogoPreviewFromRejectedFile only if you judge W03 needs it (document why); (2) StagingDirectory becomes required for SetupPackageReader and SetupPackageWriter (CryptoException(Corrupt) when null/empty) with XML docs saying the host must pass its own protected folder; (5) SetupPackageWriter clamps the effective Argon2 parameters up to Argon2Params.CreateDefault() unless the internal test hook is used; (6) optional entries are streamed via ReadTo(entryName, Stream) and not cached beyond a documented size threshold; (7) a chain error of ErrorCode.Expired is reported as SetupCheckItem.Package/FutureDate with the Signature item Ok; (8) ContainerReader.Open bounds manifest.json and signature.bin reads (1 MB) and throws CryptoException(Corrupt) beyond it. Tests for each. Keep every existing test green. (9) Setup format extension per AGREEMENT item 57 / ARCHITECTURE §4: SetupContent.includes gains letterTemplate and the optional entry letter-template.docx (written/read exactly like report-template.docx, check item ReportTemplate gets a sibling LetterTemplate); round-trip test with and without it.`,
  },
  {
    key: 'design-polish', title: 'B1-POLISH-DESIGN (masked date input + W08 constant)', model: 'sonnet',
    paths: 'src/Wakeel.Design/Components/WInput.razor, src/Wakeel.Design/Components/WInput.razor.css, src/Wakeel.Design/wwwroot/js/app.js, src/Wakeel.UI/Pages/W08AttentionCenter.razor, tests/Wakeel.UI.Tests',
    build: 'dotnet build src/Wakeel.Design --no-dependencies && dotnet build src/Wakeel.UI --no-dependencies && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'no unsafe JS interop; the caret helper only touches the element reference it is given',
    extraCheck: 'masked date input behaves correctly for null initial value, rejected characters and middle insertion (bUnit + a manual check in the running host on the gallery), and the W08 tab count uses the shared constant',
    spec: `Apply findings 1, 2 and 3 of docs/build/reviews/B0.5/verify-design-split.json (read the file for the precise text): (1) OnDateInputAsync guard compares against (Value ?? string.Empty); (2) the «الكل» tab in W08AttentionCenter uses Ar.AttentionCenter.TodayActionCount instead of the literal 9; (3) the caret position is stored on every OnDateInputAsync and re-applied from OnAfterRenderAsync through a caret-only JS helper so middle insertion keeps the caret. bUnit tests for the null-value first keystroke, a rejected character, and middle insertion (assert the emitted value and the caret helper call). Launch the desktop host, open the gallery date field, type into it and confirm visually; keep all 143 tests green.`,
  },
  {
    key: 'core-watermark', title: 'B1-POLISH-CORE (numbering watermark inside the retry loop)', model: 'sonnet',
    paths: 'src/Wakeel.Core/Services/OfficialNumberService.cs, tests/Wakeel.Core.Tests/NumberingTests.cs',
    build: 'dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests',
    security: 'no official number can be issued with a date earlier than the last issued one, even after a lost race',
    extraCheck: 'the interleaving described in the finding is reproduced by a test and now refused; all 174 tests green',
    spec: `Apply the single low finding of docs/build/reviews/B0.5/verify2-core-polish.json: re-check the «date earlier than the last issued number» watermark inside the retry loop of OfficialNumberService.IssueAsync (after the row is obtained or reloaded, before LastSeq is incremented), keeping the existing cross-year MaxAsync pre-check. Add a test that reproduces the interleaving (context B reads the watermark, context A issues a later-dated number, B loses the race, reloads, and must now be refused instead of issuing an earlier-dated number).`,
  },
]

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21. ${COMMON}\nAllowed paths (create/edit only here): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, screen-by-screen fidelity).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21. ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches, VIEW the reference PNGs of every screen involved, and run exactly: ${p.build} (read the full output). Check: (1) every spec item and screen is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. Do NOT modify any file. Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 returning to apply review findings. ${COMMON}\nAllowed paths: ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
async function chain(p) {
  const build = await agent(builderPrompt(p), { label: `build:${p.key}`, phase: 'Build', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
  if (!build) return { key: p.key, failed: 'build' }
  const review1 = await agent(reviewPrompt(p, build, 1), { label: `review1:${p.key}`, phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })
  const s = { key: p.key, build, review1 }
  if (review1 && review1.verdict === 'fix') {
    s.fix = await agent(fixPrompt(p, review1), { label: `fix:${p.key}`, phase: 'Fix', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
    s.verify = await agent(reviewPrompt(p, s.fix, 2), { label: `verify:${p.key}`, phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    if (s.verify && s.verify.verdict === 'fix') {
      s.fix2 = await agent(fixPrompt(p, s.verify), { label: `fix2:${p.key}`, phase: 'Fix2', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s.verify2 = await agent(reviewPrompt(p, s.fix2, 3), { label: `verify2:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    }
  }
  return s
}
log('B1: admin tool ∥ first run ∥ crypto/design/core polish')
const results = await parallel(PACKAGES.map(p => () => chain(p)))
return results.filter(Boolean)
