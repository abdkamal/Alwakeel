export const meta = {
  name: 'alwakeel-b1b-admin-b2',
  description: 'Admin tool continuation (admin-1 final fix + verify, then admin-2 and admin-3) in parallel with B2 daily shell (carry-over low findings, services, screens, walkthrough/E2E); every package reviewed, fixed and re-verified by Opus; restart-safe prompts with progress files and small design previews; at most two agents at a time',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a service, a screen, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports/light or design/exports/dark (1–2 MB each; they overload the connection). View the small JPEG previews under design/exports/preview/light/<group>/<name>.jpg and design/exports/preview/dark/<group>/<name>.jpg (same file names as the PNGs, .jpg extension; groups A, W, M, I), only the screens of your own package, each at most once, right before you build or check that screen. (5) Screenshots you take must be JPEG or PNG at most 1366 px wide and viewed once.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain ProjectReferences only). Never run git commit/checkout/stash/reset/clean. Another agent edits other projects in the same working tree concurrently: build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. A package that touches a WPF host or Blazor pages is not done until the host actually runs: launch the built exe (Wakeel.Desktop: src/Wakeel.Desktop/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Desktop.exe; Wakeel.Admin: src/Wakeel.Admin/bin/Debug/net10.0-windows10.0.19041.0/Wakeel.Admin.exe) with --remote-debugging-port=<port> (use 9333 for Wakeel.Desktop and 9334 for Wakeel.Admin so the two concurrent packages never collide), --window-size=1366x768, --data-folder=<a temporary folder> and --start-url=/route where useful, confirm http://127.0.0.1:<port>/json/version answers, screenshot it over CDP (Page.captureScreenshot), LOOK at the screenshot, read the host log in <data-folder>/logs for errors, then kill the process immediately — a build that compiles but does not render is a failure, and a host left running is a defect. All user-facing text is Arabic with no technical terms or error codes (item 15), every icon-only button has a tooltip, full RTL with bdi/isolate on Latin/numeric tokens (item 55), no mention of servers, ports, internet or PostgreSQL anywhere. Identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or UI state longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON_ADMIN = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B1-admin-firstrun.md (the B1 specification, section B1-A), docs/build/ARCHITECTURE.md (§2–§5, §9, §10, §12 and the .wakeel-setup format paragraph in §4; the org-root certificate decision in §3), docs/build/DATA-MODEL.md (§1, §13), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items referenced by the spec), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Crypto (keys, wraps, certificates, containers, Setup/SetupPackageWriter+Reader+PackagePassword, pairing), src/Wakeel.Core (WakeelDb, DbSession, SchemaMigrator, services), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast services), src/Wakeel.Admin.UI and src/Wakeel.Admin (what admin-1 built — see docs/build/progress/admin-1.md).\n\n${PROTOCOL}`

const COMMON_B2 = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B2-daily-shell.md (the B2 specification), docs/build/ARCHITECTURE.md (§9–§12), docs/build/DATA-MODEL.md (§1, §5), docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items 15, 20, 21, 23, 26, 32, 41, 50, 52, 56), docs/design/DESIGN-GUIDE.md and docs/design/SCREENS.md. Foundation you build on (read the public API of what you actually use, when you use it): src/Wakeel.Core (WakeelDb, DbSession, services, ClockCheckService, ArabicText, InstallationService), src/Wakeel.Design (components, Ar partial strings, bidi, theme/toast), src/Wakeel.UI (shell, first-run/login/lock/recovery from B1 under Pages/FirstRun, Pages/Account and Services/Account), src/Wakeel.Desktop (host; flags --remote-debugging-port, --window-size, --start-url, --data-folder; the host boots into /first-run when the data folder has no installation and into /login otherwise), tests/Wakeel.Walkthrough.Tests (FirstRunWorld helper that activates a temporary installation) and tests/Wakeel.E2E (Playwright-over-CDP harness). Arabic strings only in src/Wakeel.Design/Text/Ar.*.cs partial files.\n\n${PROTOCOL}`

const ADMIN_COMMON = `The admin tool «مدير نظام الوكيل» is built in three sequential sub-packages (admin-1: host, shell, storage, account, A01–A03 — DONE and under final verification; admin-2: organisation, structure, offices, devices, keys, A04–A07; admin-3: export, maintenance, distribution, audit, dialogs, A08–A12). Shared conventions: src/Wakeel.Admin (WPF + BlazorWebView host: BlazorWebViewInitializing user-data folder, Serilog file log, --remote-debugging-port, --data-folder, --start-url, --window-size, titled «مدير نظام الوكيل», 1366x768, DPAPI protector, file dialogs, print-to-PDF, ProgramData folder C:\\ProgramData\\WakeelAdmin\\ with admin.db, keys, exports, staging, logs). src/Wakeel.Admin.UI (Blazor pages/components/services) references Wakeel.Design, Wakeel.Core and Wakeel.Crypto; admin.db is SQLCipher with the DATA-MODEL §13 schema as its own migration set (Migrations/). Arabic strings live in src/Wakeel.Admin.UI/Text/ as partial static class AdminAr split by area (AdminAr.Common.cs, AdminAr.Account.cs, AdminAr.Dashboard.cs exist; add AdminAr.Organisation.cs, AdminAr.Structure.cs, AdminAr.Devices.cs, AdminAr.Keys.cs, AdminAr.Export.cs, … for your own areas). Services are registered per area through extension methods called from AddWakeelAdmin() (src/Wakeel.Admin.UI/ServiceCollectionExtensions.cs); routes are declared in src/Wakeel.Admin.UI/AdminRoutes.cs — add your routes to its Built set when the page exists. Shell: dark top bar + top tabs, no sidebar. Roles are manager|secretary|custodian (SetupRoles). Tests in tests/Wakeel.Admin.Tests use AdminTestContext (temp folder, TestClock, cheap Argon2). SUPERVISOR RULINGS (final, do not reopen): where the A01/A02 previews disagree with the specification, the specification wins — one 21-character recovery code plus QR (not twelve short codes), no «تذكر هذا الجهاز» option, lock-out 30 s doubling after 5 failures with the screen saying what it actually does; the previews will be corrected in the design later.`

const PACKAGES = {
  'admin-1-final': {
    key: 'admin-1', title: 'B1-A1 (مدير النظام: المضيف والقشرة والتخزين والحساب A01–A03) — final round', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'admin password and recovery code handling (Argon2id wraps via Wakeel.Crypto, no clear-text secrets in admin.db, logs or UI state), the key-file backup never keeps a wrap of a password that was replaced, org key generation and storage, org-root certificate issuance, DPAPI use, lock-out after 5 failed logins (30 s doubling) recorded in the operations log',
    extraCheck: 'A01, A02 and A03 match their previews (light and dark) with the admin-tool shell (dark top bar + top tabs, no sidebar), all texts Arabic via AdminAr, the Wakeel.Admin host was actually launched and rendered, and the extension points for admin-2 and admin-3 are clean',
    spec: `${ADMIN_COMMON}\nSUB-PACKAGE admin-1 is built (see docs/build/progress/admin-1.md) and passed two review rounds; the third verification (docs/build/reviews/B1/verify2-admin-1.json) returned one medium finding, six low findings and one missing item. THIS ROUND applies all of them: (medium) AdminKeyFile.Save keeps admin.key.bak with the OLD password wrap after a recovery or creation — give Save an explicit keepBackup choice, pass false and delete any existing backup whenever the wrap set changed (Create and Recover), with a test that recovers and proves the old password no longer opens the tool from any file on disk; (missing) the temporary lock-out must leave audit rows (carry the served lock-out rounds in the key file and write them with AdminAr.AccountAudit.LockedOut once the database opens); (lows) exit guard on A01 while the once-only sheet is unconfirmed (danger confirm dialog + test), A03 header «تصدير ملف الإعداد» and «عرض الكل» disabled with an Arabic tooltip while their routes are not built, A01 MachineLine through AdminOptions.MachineName, A01 strength meter silent while the password is empty, AdminDashboardService offices/devices counts consistent with disabled units (choose and document one rule, with a test), and delete the dead strings named in the finding. Read the finding text in the JSON file for the precise fixes. Keep every existing test green, launch Wakeel.Admin.exe (port 9334, temp --data-folder), screenshot A01 and A03, kill it, report in notes.`,
  },
  'admin-2': {
    key: 'admin-2', title: 'B1-A2 (مدير النظام: المؤسسة والهيكلية والمكاتب والأجهزة والمفاتيح A04–A07)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'device certificate issuance with a key seed that is stored only until the first export, office key issue/rotation, revocation list signing with the org root, logo size/type validation, letter/report template validation (docx only, placeholders detected by reading the XML — no macro execution), no secrets in admin.db clear text, logs or UI state',
    extraCheck: 'A04–A07 match their previews (light and dark), structure rules (4 fixed levels, no duplicates per level, no delete with active devices), device numbering 1–9 unique per office, the letter template upload lists detected/unknown @ placeholders and shows an A4 and A5 PDF preview, and the Wakeel.Admin host was launched and rendered',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-2) builds on admin-1 (accepted — read docs/build/progress/admin-1.md and its code before extending): (a) OrgService + A04 «المؤسسة والهوية»: full org name, logo upload (PNG/JPG ≤ 2 MB with preview and square crop; «شعار كبير جدًا» state), financial-cycle start day 1–28 with explanation (item 52), numbering format (default YYYYMMDD/DESSS) with a danger warning on change (item 5), optional monthly report template (docx, item 53), official letter template (docx with @ placeholders per AGREEMENT item 57: on upload list the detected placeholders against the nine known ones — @التاريخ الهجري @التاريخ الميلادي @رقم الصادر @اسم رئيس المكتب @اسم مكتب المستقبل @اسم الموضوع @نص المراسلة @اسم المرسل @اسم مكتب المرسل — flag unknown ones, render a PDF preview in A4 and A5 through the host's print-to-PDF of an HTML rendering or through Word COM if available (fall back gracefully), and use the built-in default docs/templates/correspondence/نموذج-المراسلة.docx when none is uploaded — copy it into the Wakeel.Admin.UI wwwroot or an embedded resource, do not read docs/ at runtime); (b) StructureService + A05 «الهيكلية»: editable tree with four fixed levels (هيئة ← دائرة ← قسم ← وحدة): add/edit/move/disable, head job title and name, office inventory code (item 45), no duplicate names within a level, no delete of a unit with active devices; (c) DeviceService + A06 «المكاتب والأجهزة»: per office the devices (device number 1–9 unique inside the office, role manager|secretary|custodian, employee name and number 1–9, sync scope full|custody-only), phones shown read-only once paired; (d) KeyService + A07 «الحسابات والمفاتيح»: create a device account (issues the device certificate with a key seed kept until the first export), activate a custodian employee account, recover an account (re-issue certificate + new wrap), office key issue/rotate with the warning that every device needs a new setup file, revoke a specific device (item 24) through a danger dialog → signed revocation list; (e) register the four routes in AdminRoutes.Built and make the A03 dashboard counts and alerts reflect the real tables; (f) tests: structure rules, device numbering, certificate issuance + revocation list verification, template placeholder detection on the default template (nine placeholders, none unknown) and on a docx with a misspelt placeholder, logo validation. Launch Wakeel.Admin.exe (port 9334, temp --data-folder), screenshot A04/A05/A06/A07 light and dark, kill it, report fidelity in notes.`,
  },
  'admin-3': {
    key: 'admin-3', title: 'B1-A3 (مدير النظام: التصدير والصيانة والتوزيع والسجل والحوارات A08–A12)', model: 'opus',
    paths: 'src/Wakeel.Admin.UI, src/Wakeel.Admin, tests/Wakeel.Admin.Tests',
    build: 'dotnet build src/Wakeel.Admin.UI --no-dependencies && dotnet build src/Wakeel.Admin --no-dependencies && dotnet build tests/Wakeel.Admin.Tests --no-dependencies && dotnet test tests/Wakeel.Admin.Tests --no-build',
    security: 'setup package password shown once and never logged, staging directory under the product folder (never %TEMP%) wiped after export, key seed removed from admin.db after the first export, AdminWrap use when opening a Wakeel backup or ProgramData, audit log has no secrets, CSV export escapes formulas',
    extraCheck: 'A08–A12 match their previews (light and dark), the exported .wakeel-setup opens with Wakeel.Crypto SetupPackageReader with every check Ok (including letter-template.docx when set), distribution regenerates files for every affected office, the whole admin tool A01–A12 works end to end in the running host (launched and rendered), and nothing from admin-1/admin-2 regressed',
    spec: `${ADMIN_COMMON}\nTHIS SUB-PACKAGE (admin-3) builds on admin-1 and admin-2 (accepted — read docs/build/progress/admin-1.md and admin-2.md and their code before extending): (a) SetupExportService + A08 «تصدير ملف الإعداد»: 4-step wizard — office and device ← content (structure, logo, office key, device certificate and its seed, cycle day, numbering format, report template, letter template as letter-template.docx via SetupContent.includes.letterTemplate, guide PDF, revocation list) ← package password (PackagePassword.New(), 16 random characters shown once with a copy button and a warning) ← save <office>-<device>-<date>.wakeel-setup written by Wakeel.Crypto SetupPackageWriter with a staging directory under ProgramData\\WakeelAdmin\\staging; record the export in setup_exports and delete the device key seed from admin.db after the first export; provisional guide: if docs/guide/دليل-الوكيل.pdf is absent generate a one-page placeholder PDF at runtime into the exports folder (never write under docs/); (b) MaintenanceService + A09 «الصيانة»: open a Wakeel copy (ProgramData folder or a .wakeel-backup file) with the org key (AdminWrap) for viewing and verification, open a .wakeel-msg, recover a user account (new password wrap delivered through a recovery setup file), every dangerous action behind a red button and confirmation, with a log; (c) DistributionService + A10 «توزيع التحديثات»: list of undistributed structure/office changes, regenerate updated setup files for all affected offices in one batch (each with its own password); (d) A11 «سجل العمليات»: table who/what/when without secrets, local search, CSV export; (e) A12: every remaining state and dialog (device revocation danger confirm, numbering-format change warning, corrupt file / wrong password error, export success, logo too large); register the routes in AdminRoutes.Built and enable the A03 header actions; (f) tests: export produces a file that SetupPackageReader opens with all checks Ok (with and without the letter template), seed removed after the first export, distribution regenerates files for affected offices only, maintenance opens a backup produced by Wakeel.Crypto test helpers, audit CSV has no secrets, A12 dialogs render. Final pass: launch Wakeel.Admin.exe (port 9334, temp --data-folder), walk A01→A12 once, screenshot A08/A09/A10/A11 and two A12 states light and dark, kill it, report fidelity in notes.`,
  },
  carryover: {
    key: 'b2-carryover', title: 'B2-CARRYOVER (low findings left open by the accepted B1 and E2E packages)', model: 'sonnet',
    paths: 'src/Wakeel.UI/Pages/Account, src/Wakeel.UI/Services/Account, src/Wakeel.Design/Components/WInput.razor, src/Wakeel.Design/wwwroot/js/app.js, src/Wakeel.Desktop/App.xaml.cs (comments only), tests/Wakeel.UI.Tests, tests/Wakeel.Walkthrough.Tests, tests/Wakeel.E2E',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build && dotnet build tests/Wakeel.Walkthrough.Tests --no-dependencies && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet build tests/Wakeel.E2E --no-dependencies && dotnet test tests/Wakeel.E2E --no-build',
    security: 'rollback leaves no orphaned vault blobs, the recovery counter forgets history after success, no secret reaches logs or UI state',
    extraCheck: 'every listed finding is closed by a code change and a test where the finding asks for one; the E2E suite still passes against the running host; nothing else in those files changed',
    spec: `Apply every finding of these three archived reviews exactly as their "fix" text prescribes (read the JSON files for the full text): (1) docs/build/reviews/B1/verify2-firstrun.json — five low findings on W07RecoveryDialog (RecoveredNotSignedIn sentence rendered with the replacement sheet; confirm button closes when recovered without a session), ActivationService.RollBack deleting the vault blobs written before the failure, RecoveryService clearing _lastCountedCode on success, W05Login re-evaluating the lock-out on a timer; (2) docs/build/reviews/B1/verify3-design-verify.json — two low findings on WInput (post-render DOM write conditional on the element still holding the processed text, passing the expected raw text to wakeelUi.setInputValue) and its out-of-order test waiting on a monotone condition; (3) docs/build/reviews/B0.5/verify-e2e.json — the low findings on tests/Wakeel.E2E (percentage assertion that is not self-referential: assert against a tolerance around the recorded baseline, CDP session detached in finally, processWasKilled reported truthfully, the diff percentage tolerance documented) and the comment on the unconditional WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS clear in App.xaml.cs (documentation only, keep the behaviour). Run the full build/test command; launch Wakeel.Desktop.exe (port 9333, empty temp --data-folder) once to confirm it still boots into W02, screenshot, kill.`,
  },
  services: {
    key: 'b2-services', title: 'B2-SERVICES (Wakeel.Core: attention, badges, notifications, reminders, clock guard, health, quick capture)', model: 'opus',
    paths: 'src/Wakeel.Core, tests/Wakeel.Core.Tests, src/Wakeel.Desktop/Services (only new Windows probe implementations: Word presence/version, WIA scanner, disk space, WebView2/runtime version) and src/Wakeel.Desktop/App.xaml.cs (DI registration lines only)',
    build: 'dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests && dotnet build src/Wakeel.Desktop',
    security: 'no secrets in notifications/audit, health report contains no key material or full paths of the vault content, background timers cannot run work on the UI thread, clock guard cannot be bypassed silently',
    extraCheck: 'the service interfaces are directly consumable by the B2 screens (view models with Arabic-ready values: counts, relative times via ArabicText, entity references for navigation) and the queries stay under 200 ms on a 10k-row seeded database (measure and report)',
    spec: `Build section «الخدمات (Wakeel.Core.Services)» of docs/build/packages/B2-daily-shell.md completely: AttentionService (four indicators from settings thresholds across correspondence, tasks, commitments, cases, decisions; «يحتاج إجراءً اليوم» list ordered by priority; today's meetings; last sync; backup state), BadgeService (sidebar item and group counts without double-counting the same record, bell count, inner tab counts, change event), NotificationService (create/read/dismiss on the notifications table, grouping today/yesterday/older, mark-all-read, Arabic relative time «قبل 10 دقائق» / «أمس 16:40», optional sound flag from settings), ReminderScheduler (a minute tick via an abstraction so tests drive it: meeting reminders at meeting time − reminder_minutes or the settings default (item 56), task/commitment due dates, financial-cycle reminders (item 52: N days before, the last day, then daily until the report is issued), backup reminder; idempotent via a unique key per event), ClockGuard (runs ClockCheckService at startup, every 10 minutes and on import; raises the clock banner state and blocks numbering until corrected; «تجاهل مؤقتًا» hides for the session only), HealthService (checks listed in the spec through small probe interfaces implemented in Wakeel.Desktop/Services for Word, WIA scanner, disk space and runtime; database integrity via PRAGMA integrity_check, vault folder/count/corrupt-file check, models folder state, clock, last backup, last sync per device, version; each card = status ok/warning/fault + Arabic message + action id; a summary «كل شيء سليم» / «هناك N مشكلة تحتاج تدخلًا»; exportable Arabic text report), QuickCaptureService (task/note/report-note/expense/appointment from short fields with immediate save and undo token). Seed helper for tests that creates 10k mixed rows with overdue/near/stale/pending items; measure AttentionService and BadgeService under 200 ms on it and report the timings. Tests: thresholds on crafted data, badge counts without double counting, reminder idempotency and the item-56 lead time (default + per-meeting override), relative time formatting incl. mixed digits, clock guard blocking numbering, health checks on a crafted environment (fake probes), quick capture + undo. Register everything in the Core DI extension and the desktop host (App.xaml.cs registration lines only), and confirm the desktop host still boots (port 9333, empty temp --data-folder → W02; screenshot, kill).`,
  },
  screens: {
    key: 'b2-screens', title: 'B2-SCREENS (W08–W12, W91, W92, W94)', model: 'opus',
    paths: 'src/Wakeel.UI, src/Wakeel.Design (Text/Ar.Shell.cs new partial file; new components only if the spec needs them, e.g. WNotificationPanel, WBanner, WHealthCard), tests/Wakeel.UI.Tests, src/Wakeel.Desktop (only: opening ms-settings:dateandtime, saving the health report file, Ctrl+N global shortcut wiring)',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build',
    security: 'no MarkupString with user data, no technical text in any state card or banner, health report saved only where the user chooses',
    extraCheck: 'each of the eight screens matches its preview (light and dark) in layout, texts and states; empty/loaded/error states exist; every icon-only button has a tooltip; the host was launched on each route with an activated temporary installation and screenshots viewed',
    spec: `Build section «الشاشات (Wakeel.UI/Pages)» of docs/build/packages/B2-daily-shell.md completely on top of the B2 services: W08 attention center with live data (replace the B0 placeholder; KPI cards open W09 on the right tab; monthly-report banner from ReadinessService if it exists else 0; «يحتاج إجراءً اليوم» mixed rows with type chip, subject/number, party, assignee, semantic due colour, local search and badge tabs; phone expenses pending confirmation with direct confirm/reject (item 50); side column: today's meetings, last sync, backup state; «إدخال سريع» and «تحديث» buttons), W09 overdue/near/stale/pending tabs with badges, local search, table and «معالجة» opening the record, W10 notification panel (380 px dropdown from the bell, tabs all/unread N, items with type icon and relative time, click opens the record, mark all read, settings link to W86), W11 clock banner (Banner.Clock above content with the exact texts, «تصحيح الساعة» opens ms-settings:dateandtime through the desktop host, «تجاهل مؤقتًا» with tooltip), W12 health center (status card grid from HealthService, «فحص شامل الآن», «تصدير تقرير الصحة» saving an Arabic text file, filter tabs, local search), W91 standard states reference page (every WStateCard with final Ar.States texts), W92 standard dialogs reference page, W94 quick capture dialog on Ctrl+N (sections task/note/report-note/expense/appointment, validation, immediate save, toast with «تراجع»). bUnit tests for the eight screens and their states (empty/loaded/error) with mixed Arabic/English content; navigation from KPI cards/rows/notifications asserted. To see the screens in the host you need an activated installation: use tests/Wakeel.Walkthrough.Tests' FirstRunWorld (or a small console/test helper) to activate a temporary data folder, then launch the host on it with --start-url for each route (port 9333), sign in over CDP, screenshot light and dark, compare with the previews, kill, and report the fidelity per screen. The W08 banner in the preview says «دورة أكتوبر»; the correct text follows the current financial cycle (this month), which the design will be corrected to later.`,
  },
  walkthrough: {
    key: 'b2-walkthrough', title: 'B2-WALKTHROUGH (service scenario + E2E screenshots)', model: 'sonnet',
    paths: 'tests/Wakeel.Walkthrough.Tests, tests/Wakeel.E2E',
    build: 'dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Walkthrough.Tests --no-dependencies && dotnet test tests/Wakeel.Walkthrough.Tests --no-build && dotnet build tests/Wakeel.E2E --no-dependencies && dotnet test tests/Wakeel.E2E --no-build',
    security: 'tests use temporary folders only and never touch C:\\ProgramData\\Wakeel of the real installation',
    extraCheck: 'the walkthrough asserts real counts and timings through the public services, and the E2E screenshots of W08–W12 are produced against the design PNGs with recorded diff percentages',
    spec: `Build section «القبول» of docs/build/packages/B2-daily-shell.md: (1) tests/Wakeel.Walkthrough.Tests scenario on a temporary activated installation (reuse FirstRunWorld from the B1 walkthrough): create overdue, near-due and stale records plus pending phone expenses → AttentionService/BadgeService return the exact counts → a meeting with a reminder fires the notification in the bell at the right minute (drive the scheduler clock; check the item-56 default and a per-meeting override) → the clock guard blocks numbering when the clock is set back and releases after correction → quick capture saves and undo removes. (2) tests/Wakeel.E2E: extend the harness with screenshot tests for W08, W09, W10 (panel open), W11 (banner shown via a test hook or seeded state), W12, W91, W92, W94 at 1366x768 light and dark, comparing programmatically with design/exports/{light,dark}/W/*.png (the test code may read the PNGs; you yourself look only at the small previews and at your own screenshots) and recording diff percentages in the test output and tests/Wakeel.E2E/artifacts/ (git-ignored); the host already honours --data-folder — point it at a temporary folder activated and seeded through the walkthrough helpers and sign in over CDP. Report the diff percentages per screen in notes.`,
  },
}

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p, common) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${common}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, measurements, screen-by-screen fidelity).`
}
function reviewPrompt(p, common, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${common}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches, view the small previews of every screen involved (light and dark), and run exactly: ${p.build} (read the full output). Check: (1) every spec item and screen is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies), warnings-as-errors clean, (6) Arabic-only user text without technical terms, tooltips on icon-only buttons, RTL/bidi compliance. Do NOT modify any file (you may not write the progress file either). Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, common, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${common}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
function accepted(s) { const last = s && (s.verify2 || s.verify || s.review1); return !!(last && last.verdict === 'accept') }
async function chain(p, common, rounds) {
  const build = await agent(builderPrompt(p, common), { label: `build:${p.key}`, phase: 'Build', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
  if (!build) return { key: p.key, failed: 'build' }
  const review1 = await agent(reviewPrompt(p, common, build, 1), { label: `review1:${p.key}`, phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })
  const s = { key: p.key, build, review1 }
  if (review1 && review1.verdict === 'fix') {
    s.fix = await agent(fixPrompt(p, common, review1), { label: `fix:${p.key}`, phase: 'Fix', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
    s.verify = await agent(reviewPrompt(p, common, s.fix, 2), { label: `verify:${p.key}`, phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    if (rounds > 1 && s.verify && s.verify.verdict === 'fix') {
      s.fix2 = await agent(fixPrompt(p, common, s.verify), { label: `fix2:${p.key}`, phase: 'Fix2', schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s.verify2 = await agent(reviewPrompt(p, common, s.fix2, 3), { label: `verify2:${p.key}`, phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
    }
  }
  return s
}
async function adminSequence() {
  const out = []
  // admin-1: apply the third verification's findings, verify, one more round if needed.
  const p1 = PACKAGES['admin-1-final']
  const prev = { see: 'docs/build/reviews/B1/verify2-admin-1.json' }
  const s1 = { key: 'admin-1' }
  s1.fix3 = await agent(fixPrompt(p1, COMMON_ADMIN, prev), { label: 'fix3:admin-1', phase: 'Fix', schema: BUILD_SCHEMA, model: 'opus', effort: 'high' })
  s1.verify3 = await agent(reviewPrompt(p1, COMMON_ADMIN, s1.fix3, 4), { label: 'verify3:admin-1', phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
  if (s1.verify3 && s1.verify3.verdict === 'fix') {
    s1.fix4 = await agent(fixPrompt(p1, COMMON_ADMIN, s1.verify3), { label: 'fix4:admin-1', phase: 'Fix2', schema: BUILD_SCHEMA, model: 'opus', effort: 'high' })
    s1.verify4 = await agent(reviewPrompt(p1, COMMON_ADMIN, s1.fix4, 5), { label: 'verify4:admin-1', phase: 'Verify2', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
  }
  out.push(s1)
  const last1 = s1.verify4 || s1.verify3
  if (!last1 || last1.verdict !== 'accept') { log('admin tool: admin-1 still not accepted — stopping for the supervisor'); return out }
  for (const key of ['admin-2', 'admin-3']) {
    log(`admin tool: starting ${key}`)
    const r = await chain(PACKAGES[key], COMMON_ADMIN, 2)
    out.push(r)
    if (!accepted(r)) { log(`admin tool: ${key} not accepted — stopping the sequence for the supervisor`); break }
  }
  return out
}
async function b2Sequence() {
  const out = []
  for (const [key, rounds] of [['carryover', 1], ['services', 2], ['screens', 2], ['walkthrough', 2]]) {
    log(`B2: starting ${key}`)
    const r = await chain(PACKAGES[key], COMMON_B2, rounds)
    out.push(r)
    if (!accepted(r)) { log(`B2: ${key} not accepted — stopping the sequence for the supervisor`); break }
  }
  return out
}
log('admin tool (admin-1 final → admin-2 → admin-3) ∥ B2 (carry-over → services → screens → walkthrough)')
const results = await parallel([() => adminSequence(), () => b2Sequence()])
return { admin: results[0], b2: results[1] }
