export const meta = {
  name: 'alwakeel-b3-services',
  description: 'B3 services (no screens): correspondence services, official-letter composer (item 57), vault + documents + OCR, hybrid search — one sequential chain of four Opus packages, each reviewed, fixed and re-verified by Opus; restart-safe prompts',
  phases: [{ title: 'Build' }, { title: 'Review' }, { title: 'Fix' }, { title: 'Verify' }, { title: 'Fix2' }, { title: 'Verify2' }, { title: 'Fix3' }, { title: 'Verify3' }],
}
const REPO = 'D:/AI/Administration2'
const PROTOCOL = `RESTART PROTOCOL (mandatory — the connection sometimes drops and an agent is then restarted from scratch, while the working tree keeps everything written so far): (1) FIRST run 'git status --short' and read docs/build/progress/<your package key>.md if it exists — it lists the steps a previous attempt of THIS package completed; continue from the first unfinished step instead of starting over, and never delete or rewrite files that already implement a step correctly (read them and extend them). Uncommitted files that belong to another package's paths are that package's live work — leave them alone. (2) Do not read everything up front: read the spec lines and the code you need for the CURRENT step, write the code, build it, then move on; make your first code change within your first ten tool calls. (3) After every completed step (a service, a test file, a green build) append one line to docs/build/progress/<your package key>.md (create it; it is the only file under docs/ you may write). (4) IMAGES: never open the full-size PNGs under design/exports; this package has no screens — if you need a screen's intent, read docs/design/SCREENS.md or view the small preview under design/exports/preview/light/W/ once. (5) Any screenshot you take must be JPEG or PNG at most 1366 px wide and viewed once. (6) When a report is handed to you as {"see": "<path>"}, read that JSON file first — it is the archived report of the previous stage.

RULES: never add NuGet packages that are not already pinned in Directory.Packages.props (pinned for B3: Tesseract, PDFtoImage, SkiaSharp, Microsoft.ML.OnnxRuntime, Microsoft.ML.Tokenizers, DocumentFormat.OpenXml; if one more is truly required, report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx, docs/ (except your progress file), or any path outside your allowed paths (csproj files inside your allowed paths may gain PackageReferences to pinned packages and ProjectReferences). Never run git commit/checkout/stash/reset/clean. Other agents edit other projects in the same working tree concurrently (design-system and screen packages): build ONLY with the exact commands given ('dotnet test --no-dependencies' is rejected by this SDK; use 'dotnet build <tests> --no-dependencies && dotnet test <tests> --no-build'); when a build fails on a file lock or on a compile error inside a project you do not own, wait 60 seconds and retry (up to five times), then report it in open_issues instead of editing that project. Never touch C:\\ProgramData\\Wakeel of the real installation: every test uses a temporary folder. All user-facing text is Arabic with no technical terms or error codes (item 15) — Core-layer strings live in src/Wakeel.Core/Services/CoreAr.cs (ratified in ARCHITECTURE §12), other layers' strings in their own Ar file; identifiers, comments and XML docs in English. Passwords, keys and seeds must never reach logs, the database in clear text, or memory longer than needed. Work until the package is complete and its build/tests are green; do not stop early.`

const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents: docs/build/packages/B3-correspondence-documents.md (the B3 specification), docs/AGREEMENT.md (items 5, 7, 10, 11, 18, 19, 22, 30–36, 41, 49, 55, 57 and §6), docs/build/ARCHITECTURE.md (§4, §5, §8, §9, §12), docs/build/DATA-MODEL.md (§0, §2, §3, §12 — the tables already exist in src/Wakeel.Core/Migrations/0001_initial.sql and the entities in src/Wakeel.Core/Data/Entities), docs/build/BUILD-PLAN.md (acceptance criteria). Foundation (read the public API of what you actually use, when you use it): src/Wakeel.Core (WakeelDb with audit stamps and enforced soft delete, DbSession, OfficialNumberService — transaction required, kind in/out, concurrency token —, AuditService, SettingsService, InstallationService, ClockGuard, NotificationService, AttentionService/BadgeService which will read the new correspondence states, ArabicText, CoreAr), src/Wakeel.Crypto (containers .wakeel-*, Aead, Sha256, DeviceCertificate, pairing), src/Wakeel.UI/Services/Account/VaultStore.cs (the current vault writer used by activation — B3-2 moves it into Core), src/Wakeel.Reports, src/Wakeel.Ocr, src/Wakeel.Search (empty class libraries referencing Core with their packages already referenced), tests/Wakeel.Core.Tests (DailyShellWorld/DailyShellSeed helpers, TestClock), tests/Wakeel.Walkthrough.Tests (FirstRunWorld activates a temporary installation). Local models for development and tests (git-ignored, fetched by build/get-models.ps1): tools/tessdata/ara.traineddata + eng.traineddata (tessdata_best) and tools/models/multilingual-e5-small/ (model_quantized.onnx int8, tokenizer.json, sentencepiece.bpe.model, config.json) — tests that need them locate them relative to the repo root and SKIP with a clear message when the files are absent. Word 2019 is installed on this machine (COM works); no scanner is attached.\n\n${PROTOCOL}`

const PACKAGES = [
  {
    key: 'b3-corr-services', title: 'B3-1a (خدمات المراسلات في Wakeel.Core)', model: 'opus',
    paths: 'src/Wakeel.Core (Services/Correspondence/* new, Services/CoreAr.cs additions, Services/ServiceCollectionExtensions.cs registration lines, Data/Entities additions only if a column is missing against DATA-MODEL §3, Migrations/000N_*.sql only for indexes), tests/Wakeel.Core.Tests',
    build: 'dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests',
    security: 'numbers issued only inside the approval/registration transaction (never on a draft), «للمستلم فقط» (item 7) enforced at the service level, cancellation keeps the number and records the reason, every state transition audited, no clear-text secrets, exchange containers encrypted to the recipient public key and signed',
    extraCheck: 'the state machine matches the spec exactly (draft, new, in-progress, awaiting reply, done, closed, cancelled, archived), duplicate detection thresholds behave on real Arabic subjects, AttentionService/BadgeService counts pick up the new states, and every service returns Arabic-ready view models for the B3 screens',
    spec: `Build the services of section «B3-1 المراسلات» EXCEPT the letter composer (next package): CorrespondenceService (draft in/out, field validation, register incoming with OfficialNumberService kind=in in ONE transaction plus a snapshot of the party name, outgoing in steps — data, recipient, template, document, review, approval — with a readiness checklist (item 18) and an approval that issues the number and freezes the document; states with allowed transitions; permanent delete of an unnumbered draft, cancellation of a numbered one with a reason and the number kept (item 19); manual close with a note; archive; «للمستلم فقط» (item 7); links to correspondence/case/meeting; an audit row per transition), DuplicateDetector (exact match party number + party refused; suspected: normalised subject Jaccard ≥ 0.8 on words, or same number with another party — surfaced as a review with «ليست مكررة»), ReferralService (referral text to a unit/person with a deadline; derived print copy request handed to the composer's DerivedDocumentBuilder interface — define the interface here, implement it in the letter package; the original never changes), FollowUpService (call/visit/reply/note, next date, reminder through ReminderScheduler, state change, «بانتظار رد منذ N أيام» card data), CorrectionService (after numbering: old and new fields with reason, both kept in corrections), ExchangeService (export an approved correspondence as .wakeel-msg — Wakeel.Crypto container kind msg encrypted to the recipient's public key from the structure or from a party profile — and import a file as incoming with signature/sender preview then registration with automatic incoming numbering as internal (item 49) or external (item 22)). View models with Arabic-ready values (CoreAr for state names, relative days via ArabicRelativeTime). Tests: every transition allowed/refused, numbering only at approval/registration and inside the transaction (a failure after numbering rolls back), exact and suspected duplicates on real Arabic subjects (diacritics, ة/ه, ى/ي, tatweel), cancellation keeps the number, correction keeps both versions, follow-up card days, exchange round trip between two activated temporary installations, attention/badge counts reflecting overdue replies. Register everything in the Core DI extension.`,
  },
  {
    key: 'b3-letter', title: 'B3-1b (قالب الكتاب الرسمي وملؤه — البند 57؛ Word وPDF)', model: 'opus',
    paths: 'src/Wakeel.Reports (letter composer, template inspector, derived-document builder, HTML rendering for the WebView2 fallback), src/Wakeel.Core (Services/Correspondence interfaces + TemplateService only), src/Wakeel.Desktop/Services (WordAutomation late-bound COM, PrintToPdf hook — new files only) and src/Wakeel.Desktop/App.xaml.cs (DI lines only), tests/Wakeel.Core.Tests, tests/Wakeel.Reports.Tests (new xunit project: add it to Wakeel.slnx is FORBIDDEN — instead put the tests in tests/Wakeel.Core.Tests with a ProjectReference to Wakeel.Reports)',
    build: 'dotnet build src/Wakeel.Reports && dotnet build src/Wakeel.Core && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build && dotnet build src/Wakeel.Desktop',
    security: 'templates are opened as zip/XML only (no macro execution, no external relationships followed), the generated letter never contains placeholder text in the final PDF, Word automation is late-bound and never leaves Word processes behind, files are written under the installation folders only',
    extraCheck: 'the conformance test regenerates docs/templates/correspondence/مثال-مراسلة-المالك.docx from نموذج-المراسلة.docx and compares paragraph text (all parts: body, text boxes incl. mc:Fallback, header, tables) with zero differences apart from whitespace; A5 letters keep the text boxes anchored to the margins; a Word-less environment still produces a PDF through the HTML rendering',
    spec: `Implement the TemplateService + LetterComposer row of section «B3-1» per AGREEMENT item 57 and ARCHITECTURE §9 (read docs/templates/correspondence/README.md and the memory of the template convention: nine data-only placeholders — @التاريخ الهجري, @التاريخ الميلادي, @رقم الصادر, @اسم رئيس المكتب, @اسم مكتب المستقبل, @اسم الموضوع, @نص المراسلة, @اسم المرسل, @اسم مكتب المرسل; the letterhead and every fixed phrase are literal template content): TemplateService in Core (the office's template = letter-template.docx stored by activation in the vault/models area, else the built-in default identical to docs/templates/correspondence/نموذج-المراسلة.docx embedded as a resource in Wakeel.Reports; template check on upload listing known/unknown placeholders; «فتح قالب المراسلة في Word» edits the office's local copy with the warning that a new setup file replaces it; page size per letter A4 default / A5), LetterComposer in Wakeel.Reports (OpenXML: merge runs, then replace placeholders in every part — paragraphs, text boxes in both mc:Choice and mc:Fallback, headers/footers, tables; Hijri date through UmAlQuraCalendar with Arabic month names; Gregorian date; «مسودة» for the number before approval; @نص المراسلة receives paragraphs, numbered/bulleted lists and bold from a small rich-text model — inserted at the placeholder keeping the text before it in the same paragraph and removing surplus empty paragraphs; sectPr/pgSz and margins for A5 with text boxes anchored relative to the margins), DerivedDocumentBuilder (referral text placed in the free area at the bottom of the last page or on an added page — item 31 — and the caller told which before saving), WordAutomation in Wakeel.Desktop (late-bound COM: open the generated docx, wait for close, import the changes, «Word غير متوفر» state), PDF through Word or through an HTML rendering of the letter for WebView2.PrintToPdf (the HTML layout mirrors the template: letterhead area, right/left blocks, the fixed phrases, A4/A5). Tests: the conformance test above; placeholder replacement inside text boxes and header; Hijri/Gregorian formatting for known dates; rich body insertion (paragraphs, lists, bold) and empty-paragraph cleanup; A5 page size; template check reporting an unknown placeholder and a missing one; derived copy on the last page vs an added page; HTML rendering contains no placeholder text. Word COM: a test that runs only when Word is present generates a PDF from the docx and asserts the file exists and Word exited.`,
  },
  {
    key: 'b3-docs-services', title: 'B3-2a (الخزنة والوثائق وOCR — الخدمات)', model: 'opus',
    paths: 'src/Wakeel.Core (Services/Documents/* incl. IDocumentStore + VaultStore moved from Wakeel.UI, CoreAr additions, DI lines), src/Wakeel.UI/Services/Account (only to make ActivationService/W03/W04 use the Core store instead of the local VaultStore, then delete the local one), src/Wakeel.Ocr, src/Wakeel.Desktop/Services (WiaScanner probe/implementation — new files) and src/Wakeel.Desktop/App.xaml.cs (DI lines only), tests/Wakeel.Core.Tests, tests/Wakeel.UI.Tests (only the tests that pinned the old VaultStore), tests/Wakeel.Walkthrough.Tests (only if the move breaks an existing test)',
    build: 'dotnet build src/Wakeel.Core && dotnet build src/Wakeel.Ocr && dotnet build src/Wakeel.Desktop && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build && dotnet build tests/Wakeel.UI.Tests --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-build && dotnet build tests/Wakeel.Walkthrough.Tests --no-dependencies && dotnet test tests/Wakeel.Walkthrough.Tests --no-build',
    security: 'originals stored byte-for-byte under their SHA-256, AES-GCM with a per-file sub key (HKDF from VaultKey + hash), atomic writes, integrity verified on every read, no clear-text copy left on disk after OCR, the OCR queue never blocks the UI thread, file size and type limits enforced before reading',
    extraCheck: 'the vault move leaves activation and the first-run walkthrough green; OCR on the test image (Arabic + English) returns the expected words with boxes; a scanned PDF is rasterised through PDFtoImage and OCRed page by page; the queue reports progress and survives a restart (pending rows resume)',
    spec: `Build section «B3-2» services: IDocumentStore + VaultStore in Wakeel.Core (move the implementation from src/Wakeel.UI/Services/Account/VaultStore.cs: hash → vault\\ab\\<sha>.bin, AES-GCM sub key, atomic write, streaming read, integrity check on read, states «الخزنة غير متاحة» and «الملف تالف — الأصل محفوظ»; make ActivationService use it and delete the UI copy), DocumentService (import PDF/JPG/PNG/DOCX ≤ 20 MB, scan through the WiaScanner abstraction — device choice, resolution, colour, preview, multi-page → PDF — implemented in Wakeel.Desktop with WIA COM late-bound and a graceful «لا ماسح ضوئي» state, link to correspondence/meeting/case/party, derived copies, OCR state, pin for the phone), OcrService in Wakeel.Ocr (Tesseract ara+eng from tessdata in the app folder or the models folder — for tests tools/tessdata; PDF pages rasterised with PDFtoImage into PNG streams then Pix.LoadFromMemory; background queue with low priority and progress; per-page result with word boxes stored in document_pages; reprocess on model change; Arabic normalisation helper shared with search), CoreAr strings. Tests: vault round trip, tamper detection, unavailable folder, dedup by hash; import limits; OCR of a generated test image (draw Arabic and English words with SkiaSharp onto a PNG, run Tesseract, assert the words and box count — skip when tools/tessdata is absent); PDF rasterisation of a generated two-page PDF (produce it with a tiny hand-written PDF or SkiaSharp's SKDocument) then OCR page by page; queue ordering, progress and resume. Register in Core DI and the desktop host.`,
  },
  {
    key: 'b3-search-services', title: 'B3-3a (البحث الهجين — الفهرسة والاستعلام ومراقب النماذج)', model: 'opus',
    paths: 'src/Wakeel.Search, src/Wakeel.Core (Services/Search interfaces + CoreAr additions + DI lines + Migrations/000N_search_fts.sql for the FTS5 virtual table if 0001 lacks it), src/Wakeel.Desktop/App.xaml.cs (DI lines only), tests/Wakeel.Core.Tests (a ProjectReference to Wakeel.Search is allowed)',
    build: 'dotnet build src/Wakeel.Search && dotnet build src/Wakeel.Core && dotnet build tests/Wakeel.Core.Tests --no-dependencies && dotnet test tests/Wakeel.Core.Tests --no-build && dotnet build src/Wakeel.Desktop',
    security: 'search never reveals «للمستلم قط» items to other users, the models folder watcher only reads files (no execution), ONNX inputs validated before use, background indexing cannot block the UI thread',
    extraCheck: 'FTS-only search answers real Arabic queries and simple questions with correct filters when no model is present; with the local model present the embedding path produces 384-dimensional vectors and RRF merges both lists; the models folder watcher classifies a good/bad model correctly',
    spec: `Build section «B3-3» services in Wakeel.Search + Core interfaces: SearchIndexer (chunks of 300 words with 50 overlap over correspondence subject+body+referrals+follow-ups, document OCR text per page, tasks, decisions, meetings, cases, parties; Arabic normalisation — alef forms, ta marbuta, alef maqsura, tatweel, diacritics; search_chunks + an FTS5 virtual table with unicode61 remove_diacritics 2 (add the migration if 0001 lacks it); ONNX embeddings through Microsoft.ML.OnnxRuntime with the multilingual-e5-small int8 model and the SentencePiece tokenizer through Microsoft.ML.Tokenizers (query: prefix «query: », passage: «passage: »; float32 vectors in the embeddings table; cosine in C#); without a model the text path alone works and a state card says so; incremental background job), HybridSearch (simple question analysis — من/ما/متى/أين/بانتظار/متأخر/بين تاريخين/الجهة X — into filters; bm25 from FTS5 + cosine → RRF; results grouped by type with a highlighted snippet and a «مطابقة نصية/دلالية» badge; open the result at page and position), ModelsFolderWatcher (item 30: detect files after their size is stable, validate ONNX inputs input_ids/attention_mask and output dimension 384 and tessdata languages, states — suitable / unsuitable with reason / preparing N% / active —, switch while search continues on the old model). Tests: normalisation cases, chunking overlap, FTS queries with real Arabic phrases and diacritics, question analysis into filters, RRF merge order, embeddings + cosine when tools/models is present (skip otherwise), watcher classification with a crafted bad ONNX and the real one, confidentiality filter. Register in Core DI and the desktop host.`,
  },
]

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_changed: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_changed', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }
const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

function builderPrompt(p) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nAllowed paths (create/edit only here, plus docs/build/progress/${p.key}.md): ${p.paths}.\nBuild/test command: ${p.build}\n\nSPECIFICATION:\n${p.spec}\n\nWhen done, return: status, files_changed, tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing, measurements, the public API the screen packages will consume).`
}
function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}). ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below and the governing docs, then read every file under ${p.paths} that the package touches and run exactly: ${p.build} (read the full output). Check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.security}, (4) ${p.extraCheck}, (5) tests assert real behaviour (not tautologies; tests that skip must say why), warnings-as-errors clean, (6) Arabic-only user text without technical terms, RTL/bidi-safe strings. Do NOT modify any file (you may not write the progress file either). Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, review) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 (package key: ${p.key}) returning to apply review findings. ${COMMON}\nAllowed paths (plus docs/build/progress/${p.key}.md): ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding and every missing spec item; fix low ones too unless genuinely out of scope): ${JSON.stringify(review)}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green. Return status, files_changed, tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}
const ORDER = ['build', 'review1', 'fix', 'verify', 'fix2', 'verify2', 'fix3', 'verify3']
const PHASE = { build: 'Build', review1: 'Review', fix: 'Fix', verify: 'Verify', fix2: 'Fix2', verify2: 'Verify2', fix3: 'Fix3', verify3: 'Verify3' }
function isReview(st) { return st.startsWith('review') || st.startsWith('verify') }
// An agent that dies on a connection error returns null: start it again (the restart protocol makes the
// new attempt continue from the working tree and the progress file). Three attempts, then give up.
async function tryAgent(prompt, opts) {
  for (let n = 1; n <= 3; n++) {
    const r = await agent(prompt, n === 1 ? opts : { ...opts, label: `${opts.label}#${n}` })
    if (r) return r
    log(`${opts.label}: attempt ${n} returned nothing${n < 3 ? ' — starting it again' : ' — giving up'}`)
  }
  return null
}
// args = { skip: [keys already accepted], resume: { '<key>': { stage, file, state, rulings, extraPaths } } }
//   stage: build|review1|fix|verify|fix2|verify2|fix3|verify3 — the stage to run first;
//   file: archived report of the stage before it (handed over as {see: file}).
async function chainFrom(p0, r0) {
  const r = r0 || {}
  const p = { ...p0,
    paths: p0.paths + (r.extraPaths ? ', ' + r.extraPaths : ''),
    spec: p0.spec + (r.state ? '\n\nSTATE OF THIS PACKAGE: ' + r.state : '') + (r.rulings ? '\n\nSUPERVISOR RULINGS FOR THIS ROUND (final, do not reopen): ' + r.rulings : '') }
  const s = { key: p.key, accepted: false }
  let prev = r.file ? { see: r.file } : null
  for (let i = r.stage ? ORDER.indexOf(r.stage) : 0; i < ORDER.length; i++) {
    const st = ORDER[i]
    if (isReview(st)) {
      const round = ORDER.slice(0, i + 1).filter(isReview).length
      const rev = await tryAgent(reviewPrompt(p, prev, round), { label: `${st}:${p.key}`, phase: PHASE[st], schema: REVIEW_SCHEMA, model: 'opus', effort: round === 1 ? 'high' : 'medium' })
      s[st] = rev
      if (!rev) return { ...s, failed: st }
      if (rev.verdict === 'accept') return { ...s, accepted: true }
      prev = rev
    } else {
      const prompt = st === 'build' ? builderPrompt(p) : fixPrompt(p, prev)
      const out = await tryAgent(prompt, { label: `${st}:${p.key}`, phase: PHASE[st], schema: BUILD_SCHEMA, model: p.model, effort: 'high' })
      s[st] = out
      if (!out) return { ...s, failed: st }
      prev = out
    }
  }
  return s
}
const RESUME = (args && args.resume) || {}
const SKIP = (args && args.skip) || []
log('B3 services: correspondence → letter composer → vault/documents/OCR → hybrid search')
const results = []
for (const p of PACKAGES) {
  if (SKIP.includes(p.key)) { results.push({ key: p.key, skipped: true }); continue }
  log(`B3 services: ${p.key}${RESUME[p.key] ? ' (resuming at ' + RESUME[p.key].stage + ')' : ''}`)
  const s = await chainFrom(p, RESUME[p.key])
  results.push(s)
  if (!s.accepted) { log(`B3 services: ${p.key} not accepted${s.failed ? ' (stage ' + s.failed + ' died)' : ''} — stopping the sequence for the supervisor`); break }
}
return results
