export const meta = {
  name: 'alwakeel-b0-resume',
  description: 'Finish the B0 foundation packages after the session restart: apply the Opus review to Core, rebuild the design system + shell, polish the low crypto findings; each package is re-reviewed by Opus',
  phases: [
    { title: 'Build', detail: 'ui builder (Sonnet) resumes from the partial tree; core fixer applies review 1; crypto polish' },
    { title: 'Review', detail: 'Opus review of the ui package' },
    { title: 'Fix', detail: 'ui builder applies findings' },
    { title: 'Verify', detail: 'Opus verify of core, ui and crypto' },
  ],
}

const REPO = 'D:\\AI\\Administration2'
const ARGS = 'C:\\Users\\abdka\\AppData\\Local\\Temp\\claude\\D--AI-Administration\\36ef789c-3174-4973-bff4-169dfeb1a469\\scratchpad\\b0-resume-args.json'
const COMMON = `Repository: ${REPO} (branch main, .NET 10 SDK 10.0.401, solution Wakeel.slnx, central package versions in Directory.Packages.props, TreatWarningsAsErrors=true). Governing documents you MUST read first: docs/build/ARCHITECTURE.md, docs/build/DATA-MODEL.md, docs/build/BUILD-PLAN.md (acceptance criteria), docs/AGREEMENT.md (items referenced by the spec), docs/design/DESIGN-GUIDE.md. Never add NuGet packages that are not already pinned in Directory.Packages.props (if one is truly required, stop and report it in open_issues). Never edit Directory.Build.props, Directory.Packages.props, Wakeel.slnx or any project outside your allowed paths. Never run git commit/checkout/stash/reset — leave your changes in the working tree. Other agents are editing other projects in the same working tree at the same time: build ONLY your own projects with the exact commands given. All user-facing text must be Arabic with no technical terms or error codes; code identifiers, comments and XML docs in English. Work until the package is complete and its tests pass; do not stop early. IMPORTANT: a previous session was interrupted mid-work, so your allowed paths may already contain partial or complete files from an earlier attempt — inspect them first, keep what is good, and continue from there instead of starting over.`

const CORE_SPEC = `Implement src/Wakeel.Core and tests/Wakeel.Core.Tests per DATA-MODEL.md sections 0–12 (the Wakeel database only; not the admin or phone databases) and ARCHITECTURE.md §2, §5, §6 (columns only), §9 (clock check rules), §12. Do NOT reference Wakeel.Crypto (keys arrive as byte[]). Packages already referenced: Microsoft.EntityFrameworkCore.Sqlite.Core, SQLitePCLRaw.bundle_e_sqlcipher, Microsoft.Extensions.Logging.Abstractions, Microsoft.Extensions.DependencyInjection.Abstractions.
1. WakeelDb : DbContext with one entity class per table (namespace Wakeel.Core.Data.Entities, DbSet per table), snake_case table and column names via a naming convention, SyncedEntity base class for the synced tables (id, created_at, updated_at, origin_device, row_version, base_version, deleted_at), enums stored as strings, DateTime stored as ISO-8601 UTC text via a value converter, money as long agorot, ids Guid via Guid.CreateVersion7(). Global query filter deleted_at IS NULL with an IgnoreDeleted() opt-out. SaveChanges must maintain the SyncedEntity columns automatically (created_at/updated_at from IClock, origin_device from the installation, row_version incremented on every local modification).
2. SQLCipher: DbConnectionFactory calling SQLitePCL.Batteries_V2.Init() once and opening Microsoft.Data.Sqlite connections with the 32-byte key (PRAGMA key = "x'<hex>'"), journal_mode=WAL, foreign_keys=ON, busy_timeout 5000. A wrong key must fail on the first read of sqlite_master.
3. Schema management without the dotnet-ef tool: SchemaMigrator applying embedded SQL scripts Migrations/0001_initial.sql… in order, tracked in schema_versions (refuse to open a database whose recorded version is newer than the build knows); 0001 creates ALL tables, indexes and foreign keys from DATA-MODEL.md (column names exactly as documented, no _json suffixes), unique indexes filtered on deleted_at IS NULL, the change_log AFTER INSERT/UPDATE triggers on every synced table (table_name,row_id,op,at,device where device = the local installation device, not origin_device) and the FTS5 virtual table search_fts (unicode61, remove_diacritics 2) kept in sync with search_chunks by triggers. The EF model must match the SQL schema exactly (test: migrate then query every DbSet).
4. Services (namespace Wakeel.Core.Services, interfaces + DI registration AddWakeelCore(options)): IClock over TimeProvider; IdGenerator; AuditService.Log(actor, action, entityType, entityId, summaryAr, details); SettingsService typed get/set with defaults (attention thresholds late=1, near=3, stale=7 days; report reminder 3 days; meeting reminder 15 minutes; phone reminders on; sounds on; theme system); OfficialNumberService.Issue(kind, now) per ARCHITECTURE §5 (format from installation.numbering_format, default YYYYMMDD/DESSS with D=device_no, E=employee_no, SSS 001–999, yearly reset, separate in/out sequences, refuse a date earlier than the last issued date across years, warning flag at 900, transactional, runs the clock check itself and refuses on a bad verdict); FinancialCycleService (boundaries from start day 1–28, Arabic name "دورة <شهر> <سنة>" named by the END month, EnsureCurrentCycle, open→awaiting_issue at end date, examples in AGREEMENT item 52); ClockCheckService (ARCHITECTURE §9 verdict rules); ArabicText (Normalize: alef variants→ا, ى→ي, ة→ه, strip tashkeel and tatweel; Plural(count, singular, dual, plural, pluralOver10) following Arabic agreement rules for counts 0,1,2,3–10,11+); UserFacingError (TitleAr, MessageAr, ActionAr, Reference) and ErrorMapper turning SqliteException (also when wrapped in DbUpdateException)/IOException/UnauthorizedAccessException/ArgumentException/unknown into Arabic messages without technical terms or codes (a short reference id is allowed).
5. DbSession (Open(paths, keyBytes) → migrate → ready; Lock()/Unlock() hooks) and WakeelPaths (ProgramData layout from ARCHITECTURE §2, overridable root for tests).
Tests (xunit, ≥50): schema creation on a temp SQLCipher file; wrong key rejected; every DbSet queryable; change_log trigger fires on insert and update; numbering (format, yearly reset, separate kinds, monotonic date refusal across years, 900 warning); cycle math (start day 20 → 20/09/2026–19/10/2026 named "دورة أكتوبر 2026"; start day 1; February; year end); ArabicText normalization and plural forms; settings defaults; error mapper output contains no digits-only codes or English words; SyncedEntity columns maintained by SaveChanges.`

const UI_SPEC = `Implement the design system and the application shell in src/Wakeel.UI (Razor Class Library), src/Wakeel.Desktop (WPF host) and tests/Wakeel.UI.Tests (bUnit). Sources of truth: docs/design/DESIGN-GUIDE.md (tokens, the full component list with variants, "بنية الشاشة القياسية", RTL and Bidi rules), design/tokens.json (each color = [light, dark]), ARCHITECTURE.md §10, AGREEMENT items 15, 21, 26, 27, 41, 55, and the reference images which you must view with the Read tool: design/exports/light/W/W08 — مركز الانتباه.png, W01 — المراسلات — القائمة.png, W91 — لوحة الحالات القياسية.png, W92 — الحوارات القياسية.png, W20 — مراسلة — نظرة عامة.png and their dark twins in design/exports/dark/W. Measure sizes and spacing from the images (1366×768 canvas).
1. wwwroot/css/tokens.css: every token as CSS custom properties on :root (light) and [data-theme="dark"], with prefers-color-scheme handling when data-theme="system"; wwwroot/css/app.css: reset, RTL base, type scale (12/14/16/22/26), logical properties only (no left/right), focus rings, thin scrollbars, print styles.
2. Font: bundle Noto Sans Arabic (OFL) woff2 weights 400/500/600/700 into wwwroot/fonts with @font-face (use npm package @fontsource/noto-sans-arabic or the Google Fonts source; npm is available). Icons: Lucide — vendor the SVG set used by the design (search, bell, calendar, user, inbox, mail, check-square, users, folder-open, file-text, building, id-card, package, wallet, bar-chart-3, settings, refresh-cw, usb, lock, unlock, shield, alert-triangle, alert-circle, info, check, check-circle, x, x-circle, plus, minus, chevrons, more-horizontal, more-vertical, printer, scan, upload, download, camera, qr-code, key, eye, eye-off, clock, filter, pencil, trash-2, save, copy, external-link, link, paperclip, phone, landmark, database, hard-drive, cpu, activity, zap, star, flag, tag, list, layout-grid, log-out, help-circle, volume-2, moon, sun, arrow-left, arrow-right, globe) as a WIcon component (Name, Size, auto-mirror of directional icons under RTL).
3. Components (namespace Wakeel.UI.Components, one .razor + scoped .razor.css each, English XML-doc on parameters): WButton (Primary/Secondary/Danger/Text/Icon/Split; Disabled; Loading with a double-click guard; icon placed on the RIGHT of the label), WTooltip, WInput (text, password with show/hide, number, date, textarea; label, required star, hint, error state with Arabic message), WSelect, WSearch (local search box with Ctrl+F binding and Kbd hint "Ctrl+F"), WCheckbox, WRadio, WToggle, WSegmented, WChip (info/primary/warning/success/neutral/danger with dot), WBadge, WAvatar (initial or photo), WCard (+Info/Warning/Danger/Success with icon/title/desc), WKpiCard, WStateCard (icon/title/desc/action), WTabs + WTabItem (count badge), WStepper, WPager, WBreadcrumb, WProgressBar, WSectionHeader (title/sub/text action), WKvRow, WAutosaveIndicator, WTable (header, rows, attention row with 3px danger edge and attention background, sticky header, footer "عرض 1–20 من N"), WDocumentRow, WTimelineItem, WMenu (popover with items+icons), WCalendarDay, WToast (service + host, semantic variants), WDialog (normal and danger: Cancel on the left, Confirm on the right; focus trap; Esc; scrim), WBannerClock, WSidebar (org logo/name/office header; collapsible groups whose open state persists through an injected IUiStateStore; items with count badges; group badge; selected item style; footer with sync status and version «الوكيل • الإصدار v0.21»), WTopBar (global search, calendar button, bell with badge, user name/role/avatar), WConfidentialityRow, Kbd.
4. Shell: Layout/MainLayout.razor = TopBar + Page (header row: Actions on the left, Titles on the right; Content) + Sidebar exactly as DESIGN-GUIDE; Bidi/Bdi.razor and Bidi.Wrap(string) helper that isolates Latin and numeric tokens; Text/Ar.cs static class holding every Arabic string of the shell (sidebar groups and items, buttons, states, footer); ThemeService (light/dark/system persisted via IUiStateStore) setting data-theme on <html>; Pages/Gallery.razor (route /gallery) rendering every component in every state with a theme toggle; Pages/W08AttentionCenter.razor placeholder reproducing the W08 layout with the sample data (هيئة تنمية المناطق الريفية / مكتب مدير دائرة التخطيط / أحمد الخطيب / PLN-PC-01 / 12/09/2026).
5. Desktop host (src/Wakeel.Desktop): WPF window titled «الوكيل», 1366×768, min 1200×700, FlowDirection RightToLeft, hosting BlazorWebView (wwwroot/index.html with dir="rtl" lang="ar"); DI via Microsoft.Extensions.Hosting; Serilog file log under %ProgramData%\\Wakeel\\logs with fallback to %LocalAppData%\\Wakeel\\logs when not writable; WebView2 user data folder under %LocalAppData%\\Wakeel; a file-based IUiStateStore under the same folder; start route /gallery for now. A debug flag --remote-debugging-port=9222 must enable WebView2 remote debugging (for the E2E tests later).
6. Tests (bUnit, ≥40): every component renders with its key parameters and variants; theme attribute switching; sidebar collapse persistence through the store; Bidi.Wrap isolates tokens; the mixed strings listed in docs/design/bidi-test.html render inside <bdi> or with unicode-bidi isolate; dialog danger variant uses the danger button.
Rules: pixel-faithful to the PNGs (8-px grid, radius 6/10/pill, button and input height 38, table row 44, sidebar and top bar sizes measured from the images), Arabic only in UI text, no technical terms, tooltips on every icon-only button, colors only via tokens, no absolute left/right CSS. Build ONLY with: dotnet build src/Wakeel.UI --no-dependencies ; dotnet build src/Wakeel.Desktop --no-dependencies ; dotnet test tests/Wakeel.UI.Tests --no-dependencies (Wakeel.Core is being edited concurrently by another agent — never touch or build it).`

const BUILD_SCHEMA = { type: 'object', properties: {
  status: { type: 'string', enum: ['done', 'partial'] },
  files_created: { type: 'array', items: { type: 'string' } },
  tests_total: { type: 'number' }, tests_passed: { type: 'number' },
  build_ok: { type: 'boolean' },
  open_issues: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['status', 'files_created', 'tests_total', 'tests_passed', 'build_ok', 'open_issues', 'notes'] }

const REVIEW_SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', enum: ['accept', 'fix'] },
  build_ok: { type: 'boolean' }, tests_ok: { type: 'boolean' },
  findings: { type: 'array', items: { type: 'object', properties: {
    severity: { type: 'string', enum: ['high', 'medium', 'low'] },
    file: { type: 'string' }, issue: { type: 'string' }, fix: { type: 'string' } },
    required: ['severity', 'file', 'issue', 'fix'] } },
  missing_spec_items: { type: 'array', items: { type: 'string' } },
  notes: { type: 'string' } }, required: ['verdict', 'build_ok', 'tests_ok', 'findings', 'missing_spec_items', 'notes'] }

const CORE = { key: 'core', title: 'B0-CORE — Wakeel.Core data layer', paths: 'src/Wakeel.Core, tests/Wakeel.Core.Tests', build: 'dotnet build src/Wakeel.Core && dotnet test tests/Wakeel.Core.Tests', spec: CORE_SPEC }
const UI = { key: 'ui', title: 'B0-UI — design system + shell', paths: 'src/Wakeel.UI, src/Wakeel.Desktop, tests/Wakeel.UI.Tests', build: 'dotnet build src/Wakeel.UI --no-dependencies && dotnet build src/Wakeel.Desktop --no-dependencies && dotnet test tests/Wakeel.UI.Tests --no-dependencies', spec: UI_SPEC }
const CRYPTO = { key: 'crypto', title: 'B0-CRYPTO — Wakeel.Crypto (polish)', paths: 'src/Wakeel.Crypto, tests/Wakeel.Crypto.Tests', build: 'dotnet build src/Wakeel.Crypto && dotnet test tests/Wakeel.Crypto.Tests', spec: 'The package is complete and accepted (168 tests). Apply only the low findings listed in the JSON file (field cryptoVerify.findings) exactly as their fix text describes, keep every existing test green, add a test per fix.' }

function reviewPrompt(p, build, round) {
  return `You are the Opus reviewer (round ${round}) of work package ${p.title} of الوكيل v0.21. ${COMMON}\nThe builder reported: ${JSON.stringify(build)}.\nRead the specification below, the governing docs, then read every file under ${p.paths} and run exactly: ${p.build} (read the full output). Check: (1) every spec item is implemented (list missing ones), (2) correctness bugs and edge cases, (3) security: ${p.key === 'crypto' ? 'misuse of primitives, nonce reuse, weak KDF parameters, signature over the wrong bytes, timing-unsafe comparisons, missing verification before decrypt' : p.key === 'core' ? 'SQL injection surfaces, key handling, migration safety, transaction correctness, soft-delete leaks' : 'XSS through MarkupString, unsafe JS interop, secrets in UI state'}, (4) ${p.key === 'ui' ? 'RTL/bidi compliance (logical CSS only, bdi/isolate on tokens, Arabic-only UI text, tooltips on icon buttons, pixel fidelity to the PNGs you must view)' : 'API shape usable by the other packages as described in ARCHITECTURE.md'}, (5) tests assert real behavior (not tautologies), warnings-as-errors clean. Do NOT modify any file. Return verdict ('accept' only when build+tests pass and there are no high or medium findings), build_ok, tests_ok, findings (severity, file, issue, precise fix), missing_spec_items, notes.\n\nSPECIFICATION:\n${p.spec}`
}
function fixPrompt(p, reviewText) {
  return `You are the builder of work package ${p.title} of الوكيل v0.21 returning to apply review findings. ${COMMON}\nAllowed paths: ${p.paths}. Build/test command: ${p.build}\nReview result to address (fix every high and medium finding, and every missing spec item; fix low ones when cheap): ${reviewText}\n\nOriginal specification for reference:\n${p.spec}\n\nAfter fixing, run the build/test command until green. Return status, files_created (files you changed or added), tests_total, tests_passed, build_ok, open_issues, notes (what you changed per finding).`
}

log('B0 resume: core fix, ui build, crypto polish')
const coreChain = (async () => {
  const fix = await agent(fixPrompt(CORE, `read the JSON file ${ARGS} — field coreReview holds the review (findings, missing_spec_items, notes) and field coreBuild the original builder report. Note: a previous fixer already changed some files (e.g. OfficialNumberService now takes IClockCheckService and tests/Wakeel.Core.Tests currently does not compile) — continue from the current state.`), { label: 'fix:core', phase: 'Build', schema: BUILD_SCHEMA, model: 'sonnet', effort: 'high' })
  const verify = await agent(reviewPrompt(CORE, fix, 2), { label: 'verify:core', phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
  return { package: 'core', fix, verify }
})()
const uiChain = (async () => {
  const build = await agent(`You are the builder of work package ${UI.title} of الوكيل v0.21. ${COMMON}\nAllowed paths (create/edit only here): ${UI.paths}.\nBuild/test command: ${UI.build}\n\nSPECIFICATION:\n${UI.spec}\n\nWhen done, return: status, files_created (paths), tests_total, tests_passed, build_ok, open_issues (anything you could not do, with the reason), notes (design decisions worth knowing).`, { label: 'build:ui', phase: 'Build', schema: BUILD_SCHEMA, model: 'sonnet', effort: 'high' })
  const review = await agent(reviewPrompt(UI, build, 1), { label: 'review1:ui', phase: 'Review', schema: REVIEW_SCHEMA, model: 'opus', effort: 'high' })
  let fix = null, verify = review
  if (review && review.verdict === 'fix') {
    fix = await agent(fixPrompt(UI, JSON.stringify(review)), { label: 'fix:ui', phase: 'Fix', schema: BUILD_SCHEMA, model: 'sonnet', effort: 'high' })
    verify = await agent(reviewPrompt(UI, fix, 2), { label: 'verify:ui', phase: 'Verify', schema: REVIEW_SCHEMA, model: 'opus', effort: 'medium' })
  }
  return { package: 'ui', build, review, fix, verify }
})()
const cryptoChain = (async () => {
  const polish = await agent(fixPrompt(CRYPTO, `read the JSON file ${ARGS} — field cryptoVerify.findings (7 low findings)`), { label: 'polish:crypto', phase: 'Build', schema: BUILD_SCHEMA, model: 'sonnet', effort: 'medium' })
  return { package: 'crypto', polish }
})()
const results = await parallel([() => coreChain, () => uiChain, () => cryptoChain])
return results